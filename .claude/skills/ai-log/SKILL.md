---
name: ai-log
description: docs/ai-log/raw/에 쌓인 프롬프트 원문과 git 기록을 읽어 docs/ai_usage_log.md의 정식 엔트리로 승격한다. 하루 작업을 마칠 때, 또는 AI 산출물이 프로젝트에 반영·폐기됐을 때 사용한다. "ai 로그", "활용 로그", "로그 정리", "오늘 기록" 같은 말에도 반응한다.
---

# ai-log

**절차 정본은 `.agents/skills/ai-log/SKILL.md`다. 그 파일을 읽고 그대로 따른다.**

여기에 복사하지 않는다 — 두 벌이 되면 반드시 어긋나고, 그때 어느 쪽이 맞는지 아무도 모르게 된다.
`CLAUDE.md`가 `AGENTS.md`를 가리키는 것과 같은 이유다.

Codex는 `.claude/`를 읽지 않고 `.agents/skills/`만 읽는다. 그래서 그쪽을 정본으로 둔다.
위 `description`만 여기 남아 있는데, 그것이 Claude Code에서 이 스킬을 띄우는 방아쇠이기 때문이다.
