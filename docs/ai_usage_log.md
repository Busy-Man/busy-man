# AI 활용 로그

제출물 4번(AI 활용 기술 문서)의 **유일한 재료.** 작성 규칙은 `ai_log_guidelines.md`를 따른다.
갱신은 `/ai-log`로 한다. 직접 고쳐도 되지만 §1 색인과 §2 본문을 항상 함께 고친다.

- 최종 갱신: 2026-08-05
- 누적 항목 수: 20

> 이 파일은 **누적**이다. 기존 항목을 지우거나 요약해서 줄이지 않는다.
> 폐기된 산출물도 지우지 않고 `반영`을 `폐기`로 바꾼다.

---

## §0. 사용 도구 목록

새 도구를 처음 쓴 날 행을 추가한다. 모델·버전까지 적는다.

| 도구 | 버전·형태 | 최초 사용 | 주 용도 |
|---|---|---|---|
| Claude Code | Claude Opus 5 | 2026-08-03 | 저장소 초기화, 규약 문서, 로깅 자동화, 폰·모달 구현 |
| Gemini | Nano Banana (이미지 생성) — 모델 버전 미확인 | 2026-08-04 | 질문 폰 UI 목업 이미지 |
| Codex | gpt-5.5 (CLI) | 2026-08-05 | 훅·스킬 동작 확인. 실작업은 아직 없다 |

## §1. 엔트리 색인

§2와 **같은 순서로** 행을 추가한다. 색인과 본문이 어긋나면 안 된다.

| # | 날짜 | 영역 | 도구 | 담당 | 반영 | 한 줄 요약 |
|---|---|---|---|---|---|---|
| L-001 | 2026-08-03 | 기획 | Claude Code | 공동 | 부분 수정 | 소크라테스식 문답으로 저장소 초기화 요구사항을 명세로 확정 |
| L-002 | 2026-08-03 | 코드 | Claude Code | 공동 | 부분 수정 | 파일 소유 경계 스켈레톤과 Pages 진입점 생성 |
| L-003 | 2026-08-03 | 문서 | Claude Code | 공동 | 부분 수정 | AGENTS.md 정본 체계 수립, 저장소 밖 문서 이관·최신화 |
| L-004 | 2026-08-03 | 도구·자동화 | Claude Code | 공동 | 부분 수정 | 프롬프트 원문 포획 훅과 /ai-log 스킬 구축 |
| L-005 | 2026-08-03 | 기획 | Claude Code | 공동 | 부분 수정 | 리뷰 지침·브랜치 전략·PR 규칙 요구사항을 명세로 확정 |
| L-006 | 2026-08-03 | 도구·자동화 | Claude Code | 공동 | 부분 수정 | Copilot 리뷰 지침, PR 템플릿, pr-commit 스킬 구축 |
| L-007 | 2026-08-03 | 문서 | Claude Code | 공동 | 부분 수정 | 브랜치·배포 결정 확정, 실제와 반대로 적힌 담당자 표기 정정 |
| L-008 | 2026-08-04 | 문서 | Claude Code | 공동 | 부분 수정 | raw 보관 정책 확정, 훅 승인 안내, 명세·계획서 저장소 편입 |
| L-009 | 2026-08-04 | 기획 | Claude Code | 공동 | 그대로 | 렌더러를 three.js(저장소 동봉)로 확정하고 문서에 반영 |
| L-010 | 2026-08-04 | 도구·자동화 | Claude Code | 공동 | 부분 수정 | 자정을 넘긴 턴의 반영 커밋이 비던 훅 버그 수정 |
| L-011 | 2026-08-04 | 문서 | Claude Code | 공동 | 부분 수정 | 커밋·PR 본문 형식 고정, rebase 금지와 그 사유 명문화 |
| L-012 | 2026-08-04 | 기획 | Claude Code | B | 부분 수정 | 폰·모달 구현 계획을 Planner/Architect/Critic 합의로 확정 |
| L-013 | 2026-08-04 | 코드 | Claude Code | B | 부분 수정 | 접점 4개를 `state.js`에 실물로 구현 |
| L-014 | 2026-08-04 | 코드 | Claude Code | B | 부분 수정 | `phone.js`·`quiz.js` 1차 구현과 dev 검증 하네스 |
| L-015 | 2026-08-05 | 기획 | Claude Code | 공동 | 부분 수정 | 기획 1차 보완을 기준으로 확정 목록 재조정, 3등급 도입 |
| L-016 | 2026-08-05 | 레벨·밸런싱 | Claude Code | B | 부분 수정 | 오답 패널티 2종 랜덤 부활 — 같은 날 좁혔던 것을 되돌림 |
| L-017 | 2026-08-05 | 도구·자동화 | Claude Code · Codex | 공동 | 부분 수정 | 훅·스킬을 Codex에도 붙이고 정본을 `.agents/`로 이관 |
| L-018 | 2026-08-05 | 코드 | Claude Code | B | 부분 수정 | 스태시 복원과 8/5 기획 동기화, 모달을 폰 형태로 재구현 |
| L-019 | 2026-08-05 | 레벨·밸런싱 | Claude Code | B | 부분 수정 | `day1.json` 실콘텐츠 — 문자 58개, 질문 20문항 |
| L-020 | 2026-08-05 | 문서 | Claude Code | B | 그대로 | 규약·UI 명세가 기획·구현과 어긋난 네 곳을 정정 |

**영역 분포** (균형 점검용 — `코드`·`아트`·`사운드`·`레벨·밸런싱`이 절반 이상이어야 한다)

| 영역 | 건수 |
|---|---|
| 기획 | 5 |
| 코드 | 4 |
| 아트 | 0 |
| 사운드 | 0 |
| 레벨·밸런싱 | 2 |
| 문서 | 5 |
| 도구·자동화 | 4 |

> ⚠️ **`코드`+`레벨·밸런싱`이 6/20(30%)이다.** 1/9(11%)에서 올라왔지만 아직 절반이 아니다.
> 8/3~8/4가 저장소 기반을 세우는 기간이었던 것이 분포에 그대로 남아 있고, 실제 구현이
> 시작된 것은 8/4 밤부터다.
>
> **`아트`와 `사운드`는 여전히 0이다.** 지금 스코프에 에셋 작업이 없어서인데, 그대로 마감하면
> 문서 4번에 그 두 절이 통째로 빈다. 8/6 밸런싱과 8/7 이후 에셋 작업에서 채워져야 한다.

## §2. 엔트리

아래 형식으로 `L-001`부터 이어 붙인다.

> 엔트리가 가리키는 `ai-log/raw/YYYY-MM-DD.md`는 **저장소에 없다. 노션에 보관한다.**
> 각자 로컬에 쌓이고, 문서 작성 시 내려받는다. 사유는 `ai-log/raw/README.md` 참조.

<!--
### L-0NN · YYYY-MM-DD · 영역

| | |
|---|---|
| 도구 | |
| 담당 | |
| 목적 | |
| 반영 | 그대로 / 부분 수정 / 골격만 차용 / 폐기 |

**프롬프트**

> 원문 그대로. 길면 ai-log/raw/의 날짜·시각으로 가리킨다.

**산출물**

**사람이 한 것** — 비워두지 않는다.
-->

### L-001 · 2026-08-03 · 기획

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview |
| 담당 | 공동 |
| 목적 | 초기 상태 저장소를 무엇으로 채울지, 어디까지가 뼈대이고 어디부터가 구현인지 확정 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (2)

에이전트가 한 번에 한 질문씩 던지고 답할 때마다 모호성 점수를 다시 계산하는 방식.
목표·제약·완료판정·맥락 네 축으로 점수를 매겨 임계값(20%) 아래로 내려갈 때까지 반복했다.

**산출물** — `docs/ai-log/specs/2026-08-03-repo-init.md` (182줄)

4라운드 만에 모호성 100% → 14%. 6개 최상위 컴포넌트, 수용 기준 17개,
드러난 가정 6건과 그 해소 과정이 기록되어 있다.

특히 뒤집힌 가정 두 개가 이후 설계를 갈랐다.

- "훅이 로그를 자동으로 채운다" → 가이드라인이 요구하는 `사람이 한 것`·`반영 판정`은
  사후 판단이라 훅이 알 수 없다. 훅의 고유 가치는 **복원 불가능한 원문의 포획**뿐이다
- "Pages는 배포만 되면 된다" → 진짜 리스크는 하위 경로 ES 모듈 해석이다

**사람이 한 것**

- 스켈레톤이 구현 코드를 잠식할 것을 우려해 **"디렉토리 + 빈 파일만"으로 범위를 잘라냄.**
  에이전트가 추천한 "접점만 실물, 나머지는 배선"보다 한 단계 더 보수적인 선택
- 컴포넌트 토폴로지에 **Pages 배포 관통**을 6번째로 추가
- 팀원 도구 환경(Claude Code 주력 + Codex 대비) 정보 제공 → AGENTS.md 정본 체계의 근거
- 훅이 커밋 해시도 남기도록 지시

---

### L-002 · 2026-08-03 · 코드

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 두 사람이 각자 AI로 코드를 뽑아도 서로 덮어쓰지 않도록 파일 경계를 실물로 고정 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (4)

**산출물** — 커밋 `0a27d14` `44afc53` `2f5c1c0` `229bca7` `9ce58cf`

- `.gitignore` — `.omc/` 제외. `docs/ai-log/raw/`의 날짜 파일은 노션 보관 결정에 따라 제외
- `.gitattributes` — LF 통일. 8/5 병합일에 줄바꿈 충돌이 진짜 충돌과 섞이는 것 방지
- `src/{state,main,world,phone,quiz}.js` — 담당자와 착수 전 확인사항만. **실코드 3줄**
- `index.html` — `./src/main.js`를 module로 로드해 Pages 하위 경로 검증

`docs/team-plan.html`이 문서로만 합의했던 파일 분담을 실제 파일로 옮긴 것이다.
그 문서 자체가 "문서로 합의하면 지켜지지 않지만, 파일이 이미 있으면 둘 다 그걸
import 한다"고 적고 있다.

**사람이 한 것**

- **담당 A/B가 실제와 반대임을 지적**하고 `docs/역할 분담.txt`를 근거로 제시.
  에이전트는 team-plan.html만 보고 반대로 배정했었다
- 반전 범위가 **구현 파일에만 해당**함을 다시 정정 — 배포·제출·영상·문서 담당은 원안이 맞다.
  이 정정으로 "8/10 제출 담당이 그날 부재"라는 모순이 해소됨
- 커밋 단위와 메시지 분량 기준 제시

---

### L-003 · 2026-08-03 · 문서

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 저장소 밖에 있어 팀원이 볼 수 없던 규약·대회 요건을 안으로 들이고 정본을 하나로 정리 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (4)

**산출물** — 커밋 `17d9593` `1b77681`

- `AGENTS.md` — 정본. 파일 소유권, 접점 4개, 확정/미결 구분, 커밋 규약, 실행·배포 함정
- `CLAUDE.md` — `@AGENTS.md` 임포트만
- `docs/대회_정보.md` — 상위 디렉토리에서 이관 + 최신화

에이전트 판단으로 상위 `CLAUDE.md`를 그대로 옮기지 않고 **사실(대회 요건)과
행동 규약(AGENTS.md)으로 분리**했다. 매 세션 주입되는 파일에 긴 사실 문서를 넣으면
컨텍스트만 소모하기 때문. 최신화 과정에서 "컨셉별 폴더 분리" 구조 서술과
"아직 결정되지 않은 것" 6항목이 이미 낡았음이 드러나 확정 표로 교체했다.

**사람이 한 것**

- 이관 여부·위치를 애초에 **판단 대상으로 지정**해서 물음 (지시가 아니라 검토 요청)
- **`CLAUDE.md`를 요약본이 아니라 `@import`로 바꾸도록 지시** — 에이전트는 핵심 3줄을
  요약해 뒀는데, 그 요약본도 결국 두 번째 진실이 된다는 판단
- 상위 원본 2개 삭제 결정 (이관 누락 여부를 대조로 확인한 뒤 실행)

---

### L-004 · 2026-08-03 · 도구·자동화

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 제출물 4번의 재료인 프롬프트 원문이 사후 복원 불가능하므로 그 포획을 자동화 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (6)

**산출물** — 커밋 `307c381`, `374b4e3`

`307c381` — 로깅 인프라

- `.claude/hooks/log-prompt.mjs` — 2단계 훅.
  `UserPromptSubmit`이 원문을 적고 `Stop`이 그 사이 늘어난 커밋을 채운다
- `.claude/settings.json` — 훅 등록 (저장소 공유)
- `.claude/skills/ai-log/SKILL.md` — raw와 git 기록을 엔트리로 승격하는 절차
- `docs/ai_log_guidelines.md` — 상위에서 이관 + 원자재/엔트리 2층 구조 반영
- `docs/ai_usage_log.md` — 이 파일의 골격

`374b4e3` — 명세 보존 규칙. 첫 기록을 쓰다가 필요성이 드러나 뒤늦게 추가됐다

- `docs/ai-log/specs/` 신설 → **원자재 / 명세 / 엔트리 3층**으로 확장
- `ai_log_guidelines.md` §2에 트리거 추가, §4를 "원문과 중간 산출물 취급"으로 넓힘.
  절 번호를 재배열하지 않아 기존 참조가 그대로 유효하다
- `SKILL.md` 3단계에 `.omc/`의 명세를 저장소로 옮기는 절차 추가

> 이 로그 파일만 고치는 커밋은 여기 나열하지 않는다 — 갱신할 때마다 늘어나 끝이 없다.
> **인프라나 지침을 함께 고친 커밋만 적는다.**

자동화 범위를 원문 포획으로 한정한 것이 핵심 판단이다. 가이드라인이 요구하는
`사람이 한 것`·`반영` 필드는 사후 판단이라 훅이 알 수 없고, 반대로 원문은
훅만 알 수 있으며 그 순간이 지나면 복원되지 않는다.

검증: 커밋 2개 발생 / 커밋 없음 / 앞 항목 미훼손 / 깨진 JSON / 파일 부재 —
다섯 경우 모두 의도대로 동작하고 세션을 막지 않음(exit 0).

**사람이 한 것**

- **커밋 해시를 "직전"이 아니라 "반영된 커밋"으로 바꾸도록 지시.** 이 한 마디가
  단일 훅을 2단계 구조로 바꿨다. 프롬프트 시점에는 그 커밋이 아직 없기 때문
- **`SKILL.md`가 가이드라인 내용을 중복 서술함을 지적** → 규칙을 전부 참조로 돌리고
  절차만 남김
- **`prompts/` 층 폐지 결정** (§3 참조)
- 훅이 없는 도구(Codex)로 전환할 가능성을 상기시켜, 훅 없이도 성립하는 프로세스를 요구
- 명세 보존을 **일회성 조치가 아니라 지침의 규칙으로** 만들 것을 요구
  ("앞으로도 deep-interview 쓸 텐데, 이거도 지침에 포함해야 하는 거 아님?").
  에이전트는 명세 하나를 옮기는 것으로 끝내려 했다

---

### L-005 · 2026-08-03 · 기획

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview |
| 담당 | 공동 |
| 목적 | 리뷰 지침·브랜치 전략·PR 규칙을 코드 착수 전에 확정 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` (8), (9)

**산출물** — `docs/ai-log/specs/2026-08-03-review-and-conventions.md` (186줄)

3라운드, 모호성 100% → 17.1%. 5개 컴포넌트.

**확인된 외부 사실 두 개가 결정을 갈랐다.**

- **GitHub Pages는 저장소당 게시 소스가 하나다.** develop을 별도 URL로 띄울 수 없으므로
  "develop에서 미리 보고 main으로" 흐름이 성립하지 않는다 → **develop 미도입**
- `.github/copilot-instructions.md`는 코드 리뷰가 읽으며 기본 활성이지만,
  **리뷰 언어 지정은 문서화되어 있지 않다** → 보장으로 취급하지 않고 실증 항목으로 남김

**사람이 한 것**

- 토폴로지를 3개 → 5개로 쪼개도록 지시. 브랜치 전략을 **"develop 여부"와 "Pages 배포 방식"**
  둘로 나누고 PR 템플릿을 별도 컴포넌트로 추가
- develop 미도입 결정. 동결 태그는 8/7로 미룸
- Pages를 branch 방식으로 선택 — **루트 전체가 웹에 노출된다는 것을 알고도 수용**.
  Actions는 실패 지점을 늘려 3.5일 일정에 맞지 않는다는 판단
- skill 범위를 "커밋 분할 → 메시지 → PR 본문 전 과정"으로 확정

---

### L-006 · 2026-08-03 · 도구·자동화

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 둘 다 AI로 코드를 뽑으므로, 리뷰와 커밋·PR이 사람마다 다른 모양으로 쌓이지 않게 함 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` (8), (9)

**산출물** — 커밋 `d79f68b`, `0bd956b`

- `.github/copilot-instructions.md` — 한국어 리뷰 지시 + 이 저장소에서 실제로 사고가 나는
  다섯 가지(소유 경계 침범, 접점 초과, 절대경로 모듈, 렌더러 미결 상태의 렌더 코드, 콘텐츠 하드코딩)
- `.github/pull_request_template.md` — 개요 / 변경 사항 / 체크리스트
- `.claude/skills/pr-commit/SKILL.md` (82줄) — 커밋 전 **소유 경계 침범 점검** 포함

**"지적하지 않아도 되는 것" 목록이 이 지침의 핵심이다.** 빈 스켈레톤, 번들러 부재,
포매터 부재가 전부 의도한 결정이라, 이걸 빼면 리뷰가 통째로 노이즈가 된다.

두 산출물 모두 규칙을 복사하지 않고 `AGENTS.md` §5를 참조만 한다. `ai-log` 스킬에서
중복을 걷어낸 선례를 따랐다.

**사람이 한 것**

- 리뷰를 **한국어로** 받을 것을 요구
- PR/commit 규칙을 skill로 만들 것을 요구
- PR 템플릿을 별도로 둘 것을 지시 — **독자가 다르다.** 템플릿은 사람이 GitHub UI에서,
  skill은 에이전트가 쓴다

---

### L-007 · 2026-08-03 · 문서

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 브랜치·배포 결정을 규약에 확정하고, 실제와 반대로 적힌 담당자 표기를 바로잡음 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` (12), (13)

**산출물** — 커밋 `4048f95` `29613b1` `7cf7b0a` `c3543fd`

- **`team-plan.html`의 구현 담당 A/B 맞바꿈** (§03 카드·§04 일정). 그전까지는 `AGENTS.md`에
  경고를 달아 우회했는데, **팀원이 그 문서를 직접 열면 경고를 못 본다**
- develop 미도입 근거와 Pages 설정값을 `AGENTS.md`에 명시. `.nojekyll` 추가
- 문서를 실제로 고쳤으므로 `AGENTS.md`·`대회_정보.md`의 "라벨이 반대" 경고를 제거.
  경고가 남아 있으면 그것이 새 거짓이 된다

**사람이 한 것**

- `team-plan.html`을 **실제로 고치도록 지시** — 경고 우회로는 부족하다는 판단
- 반전 범위가 **구현 파일에만 해당**함을 짚음. 대외 산출물 담당은 원안이 맞다
- raw 원문의 저장소 공개를 **보류시킴.** 팀원과 상의 전이라는 이유

---

### L-008 · 2026-08-04 · 문서

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 보관 정책이 바뀐 뒤 따라오지 못한 문서를 실제와 일치시키고, 사라질 자산을 저장소로 들임 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-03.md` 23:33 항목, `2026-08-04.md` (a), (b), (f)

**산출물** — 커밋 `0cbf10a` `dde0dfb` `50b6cb4`
및 `docs/ai-log/specs/2026-08-04-ai-log-policy-plan.md`

- **raw 보관 정책을 노션 수동 보관으로 확정.** 문서에는 "공개 여부 미결이라 잠시 보류"로
  적혀 있어 곧 커밋할 것으로 읽혔다. `raw/README.md`의 "왜 커밋하는가" 절은 정반대 내용이었다
- **훅 승인 절차 안내** — 저장소 훅은 승인 전까지 실행되지 않는데 그 사실이 어디에도 없었다.
  이번에 직접 겪은 문제다
- 두 번째 deep-interview 명세를 `.omc/`에서 저장소로 보존 (L-004에서 만든 규칙의 이행)
- 가이드라인의 `### 두 개의 층` 제목이 3행 표를 달고 있던 것도 함께 정정

**사람이 한 것**

- 보관 사유를 **"어투가 구어체라서"가 아니라 심사자 관점에서 성립하는 근거**로
  다시 쓰도록 요구 → "제출물의 원자재이지 소스가 아니다 / Pages가 루트를 서빙한다 /
  올린 뒤에는 정정할 수 없다"로 정리됨
- 노션 업로드 자동화를 **하지 않기로 결정** — 파일 일곱 개에 스크립트·토큰을 붙일 값이
  나오지 않는다는 판단
- 계획 문서를 저장소에 보존하도록 지시

---

### L-009 · 2026-08-04 · 기획

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 렌더러 미결이 `world.js` 착수를 규약상 막고 있어, 결정을 문서에 반영 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-04.md` (c), (d), (e)

> 이 지시는 `ExitPlanMode` 거부 시 전달되어 `UserPromptSubmit`을 거치지 않았다.
> **훅이 잡지 못하는 입력이 있다**는 것이 여기서 처음 드러났고, 같은 세션이라 대화에서
> 원문을 복원해 raw에 수동으로 채웠다. 선택지 응답도 마찬가지로 잡히지 않는다.

**산출물** — 커밋 `ced96f8`, `3014da1`

- `AGENTS.md` §3에서 렌더러를 **"열려 있음" → "확정"**으로 이동.
  남는 열린 항목은 **`content/day1.json` 스키마 하나**가 됐다
- §7의 "렌더러 결정 전에 렌더 코드 쓰기"를 **"CDN에서 three.js 불러오기"**로 교체.
  금지 항목이 사라진 것이 아니라 바뀐 것이다
- §1 소유권 표에 `vendor/` 행 추가 (공동 · 수정하지 않음)
- `copilot-instructions.md` 리뷰 포인트 교체, `대회_정보.md` 확정 표 갱신
- `src/world.js` 착수 주석 — 프로토타입은 Canvas 2D 자체 투영이라 **이식 대상이 아니라는 점**도 명시

**CDN 대신 저장소 동봉을 택한 것이 이 엔트리의 판단이다.** CDN은 심사 당일 외부 서버가
죽거나 막히면 게임이 백지가 되는데 그건 통제할 수 없다. `team-plan.html` §02가
"Vite를 끼우면 마지막 날 백지"라며 잘라낸 것과 같은 종류의 리스크이고, 대가는 파일 하나뿐이다.

**사람이 한 것**

- **three.js 최종 결정** (Canvas 2D와의 비교는 `renderer-comparison.html`에서 이미 이뤄짐)
- 동봉과 CDN의 차이를 **되물어 근거를 확인한 뒤** 동봉 채택. 에이전트 권고를 그대로 수용
- 범위가 작으니 진행 중이던 작업과 함께 처리하도록 지시

---

### L-010 · 2026-08-04 · 도구·자동화

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 자정을 넘긴 턴에서 반영 커밋이 비는 것을 고쳐 원문과 커밋의 연결이 끊기지 않게 함 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 00:27, (g)

> 훅 자체는 뜨는데?

**산출물** — 커밋 `6cf5b38`

`UserPromptSubmit`이 원문을 쓴 날짜 파일과 `Stop`이 커밋을 채우러 가는 날짜 파일이
자정을 걸치면 달라진다. 그러면 원문은 어제 파일에 있는데 커밋은 오늘 파일을 뒤지므로
**아무 말 없이 `반영 커밋: _없음_`으로 남는다.** 실제로 8/3→8/4 전환에서 발생했다.

**사람이 한 것**

- **증상만 보고 원인 규정을 하지 않음** — "훅 자체는 뜨는데"로 범위를 좁혀 줌.
  훅이 안 도는 것과 훅이 돌고도 값이 비는 것은 원인이 전혀 다르다
- 대응 방식으로 **"지금 고침 — 새 브랜치 + PR"** 선택. 로그 정리와 섞지 않았다

---

### L-011 · 2026-08-04 · 문서

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 커밋·PR 본문이 사람마다 다른 모양으로 쌓이는 것을 막고, 해시를 갈아끼우는 조작을 금지 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 12:00, 12:15, 12:29, 13:03

> - pr-commit 스킬 수정: 변경 사항 사용자 확인 후 수행할 것.
> - 커밋, PR 본문 개선: 현재는 가독성이 떨어지고 verbose함. 커밋 본문도 간결한 (개요 + 리스트)화. 작업 전 시안 제시

**산출물** — 커밋 `fa58ab4` `71495c5` `e03ba5f`

- `AGENTS.md` §5에 **개요 한두 줄 + 리스트** 형식과 인용 시 파일명 병기 규칙
- `pr-commit` 스킬에 **실행 전 사용자 확인 게이트** 추가 — "커밋해줘"가 메시지 승인을 대신하지 않는다
- **rebase 금지 명문화.** `71495c5`가 그 필요를 증명한다 — 8/4에 PR #5를 머지 직전
  rebase해서 로그의 커밋 해시 5개가 죽어 있었고, 그것을 현재 해시로 교체한 커밋이다

**사람이 한 것**

- 기존 커밋·PR 본문이 **verbose하고 가독성이 떨어진다고 지적**하고 형식을 지정
- **`--force-with-lease` 예외를 제안** — 브랜치 정리 말고 충돌 해결에도 rebase를 쓸 것 같다는
  실무 근거였다. 확인 결과 **lease가 막는 것은 남의 push를 덮어쓰는 사고이지 해시 교체가
  아니라서** 예외가 성립하지 않았고, `git merge main`으로 대체됨
- 스킬이 사용자 확인 없이 커밋하던 것을 문제로 지목

---

### L-012 · 2026-08-04 · 기획

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview + Planner/Architect/Critic 합의 |
| 담당 | B |
| 목적 | `phone.js`·`quiz.js` 착수 전에 미정 기획을 걸러내고, 소유 경계를 침범하지 않는 구현 계획을 확정 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 16:35, 17:00, 17:21, 17:24, 17:27, 18:25, 20:21

> /deep-interview @src/quiz.js , @src/phone.js 개발. @docs/기획 초안.md 기획 초안 중 아직 미정 사항이 있음. 결정된(혹은 변경이 있어도 수정이 적은) 부분들만 우선 구현.

**산출물** — `docs/ai-log/specs/2026-08-04-phone-quiz-spec.md` (7라운드, 모호도 19%)
및 `docs/ai-log/specs/2026-08-04-phone-quiz-plan.md`

계획이 **Architect와 Critic을 거치며 크게 고쳐졌다.** 그 과정 자체가 검증 기록이다.

- Architect **REVISE** — 블로킹 5건. 그중 (B1) `AGENTS.md` §2의 A/B 라벨이 §1과 정반대인데
  계획이 그것을 정본으로 인용하고 있었다는 지적이 가장 컸다. 이 지적이 `e7ed1ee`(L-013)로 이어진다
- Architect가 제안한 **내부 rAF 종합안을 Critic이 기각** — 결함 지적은 실재하나 처방이
  결정을 뒤집을 뿐 아니라 규약을 깬다는 판정
- Critic이 **"대안 탐색이 불공정하다 — Option B의 단점 3개 중 2개가 사실이 아니다"**,
  **"수용 기준 24개 중 7개가 닫히지 않는다"**를 지적

**뼈대는 살아남았다** — 주입 방식, `TUNING` 블록 격리, dev 하네스, `setVisible(bool)` + CSS
transition. 최종 코드가 전부 이대로다.

**사람이 한 것**

- **미정 사항을 먼저 걸러내라고 범위를 지정.** "결정된 부분만 우선 구현"이 스코프의 기준이 됐다
- 시안 둘 중 **B안(`setVisible(bool)` + CSS transition) 결정**
- **타임아웃도 오답 처리로 확정**
- `거북목 컨셉 제거`가 **기능 변경이 아니라 컨셉 표현만 뺀 것**임을 설명 — 에이전트가
  고개 각도 연출 자체를 없애는 것으로 읽을 뻔했다
- 프로토타입의 고개 각도 보간 속도를 그대로 쓸 것을 지시

---

### L-013 · 2026-08-04 · 코드

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 문서로만 합의돼 있던 접점 4개를 실물 파일로 만들어 8/5 병합일의 어긋남을 없앰 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 21:36, 21:38, 21:47

> state.js 접점 생성과 역할 뒤집힘을 별도의 PR로 머지한 후에, A, B 각자 하던대로 하면 되나?

**산출물** — 커밋 `746d5eb` `e7ed1ee`

- `src/state.js` — 값 셋을 **객체 하나로** 내보낸다. `export let gauge = 0` 같은 원시값
  바인딩이면 import한 쪽의 대입이 ES 모듈 규칙상 조용히 무시된다
- `fireGate(gateIndex)`를 함께 둠. 등록과 발화는 **한 접점의 두 짝**이지 다섯 번째가 아니다
- 콜백 하나가 던져도 나머지는 돈다 — 한쪽 예외가 전체를 멈추면 "한 번은 되는데
  그다음부터 안 된다"로 나타나 원인을 못 찾는다
- `AGENTS.md` §2의 A/B 라벨이 §1 소유권 표와 반대인 것을 정정 (L-012의 Architect 지적)

**사람이 한 것**

- **별도 PR로 먼저 머지할 것을 제안** — 공동 파일이라 각자 브랜치에서 만들면 충돌한다는 판단
- `docs/team-plan.html`이 말하는 것이 **import 방식이 맞는지 되물어 확인**한 뒤 착수 지시

---

### L-014 · 2026-08-04 · 코드

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 폰 UI와 질문 모달을 실제로 만들고, A의 주행 코드 없이도 눈으로 볼 수 있는 검증 수단 확보 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 20:32, 20:53, 21:15, 21:16, 22:34, 22:41

> 질문 이벤트가 없음. 그리고, 실제 게임 화면처럼 만들어

**산출물** — `src/phone.js` `src/quiz.js` `content/day1.json` `dev/`
(그날은 스태시에 머물렀고 8/5에 `ead4bf7` `0146a8c` `a508b97`로 들어갔다)

- `phone.js` — 대화 스트림, `setVisible` 전환, 사람/알림 구분
- `quiz.js` — 모달, 카운트다운, 정오답, 감속 패널티
- `dev/harness.html` — 가짜 복도 위에서 폰·모달을 확인하는 검증 페이지.
  투영 수식만 프로토타입에서 가져왔고 **`src/`로 옮기지 않는다** (A의 `world.js`와 충돌한다)

> ⚠️ **Space 방향은 이날 지시로 프로토타입과 반대가 됐다.**
> 20:53 *"스페이스바 조작을 반대로 바꿔. 즉, 스페이스바를 누르면 휴대폰을 보고,
> 안 누르면 정면을 보고."* — 그래서 이 구현과 `dev/handoff-to-A.md`가 그 방향으로 쓰였다.
> **8/5에 다시 뒤집혀 프로토타입 방향으로 돌아간다** → L-018.

**사람이 한 것**

- **질문 이벤트가 통째로 빠진 것을 발견**하고 지적. 계획에는 있었으나 구현에서 누락됐다
- "실제 게임 화면처럼 만들어" — 하네스가 폼만 확인하는 페이지가 되는 것을 막음
- **Space 방향을 프로토타입과 반대로 지시** (위 참조)
- Windows에서 `python -m http.server`를 팀원에게 안내해도 되는지 물어 **`.js`가
  `text/plain`으로 나가는 함정**을 드러냄 → `AGENTS.md` §6에 경고로 남음
- 기획 최신화를 다른 세션에서 하기로 하고 **스태시 결정**

---

### L-015 · 2026-08-05 · 기획

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview |
| 담당 | 공동 |
| 목적 | 8/4에 나온 기획 1차 보완을 규약에 반영하고, 무엇을 만져도 되는지가 등급으로 보이게 함 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 00:05, 01:47

> - @docs/game_balance_review.html 가 절대적인 문서는 아님. 타임아웃 패널티는 오답과 같음
> - @AGENTS.md @docs/balance-todo.md 질문 개수를 4개로 고정한 적이 없음. 제거.
> - @docs/기획_1차_보완.md 패널티의 게이지 감소와 랜덤 부여는 (추가 판단 확정 전) 임시 보류한 것이지 완전 폐기(기각)가 아님. 스크랩도 `되살리지 않는다`라고 확정하진 말 것.

**산출물** — 커밋 `79de839` `bc79a9d` `fbd518b` `35b00b3`
및 `docs/ai-log/specs/2026-08-05-planning-revision-sync.md`

- **확정/잠정/열림 3등급 도입.** 이전 판은 "확정 / 열림" 둘뿐이라, 스스로
  *"수치는 구현 후 경험으로 조절"*이라 적어 둔 항목까지 되돌릴 수 없는 칸에 앉아 있었다
- `docs/ui-spec.md` 신설 — 실시간 안은 버렸지만 **거기서 나온 폰 UI는 살아남았다**
- `docs/balance-todo.md` 분리 — §3은 확정 사항의 자리이고 할 일 목록을 섞으면
  다음 사람이 "정해진 것"과 "해야 할 것"을 구분하지 못한다

**사람이 한 것**

- **`game_balance_review.html`이 정본이 아님을 명확히 함.** 에이전트가 그 문서의 권고를
  결론으로 취급하고 있었다. 7건 중 따르지 않기로 한 것이 4건이다
- **"질문 개수를 4개로 고정한 적이 없다"**고 지적 — 더미 콘텐츠 개수가 기획으로 굳어가고 있었다
- **보류와 폐기를 구분하도록 요구.** 취소선을 긋지 않은 것이 이 지시에서 나왔다
- 목업 이미지의 `1. 2. 3.`이 **AI 생성 산물이므로 무시**하라고 지시 → 숫자키 오해를 막음
- 더 이상 필요 없는 파일 파악을 지시 (실시간 배치 그림 2장 정리)

---

### L-016 · 2026-08-05 · 레벨·밸런싱

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 같은 날 오전에 "감속만 쓴다"로 좁혔던 패널티를 기획 원문의 2종 랜덤으로 되돌림 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 11:44, 11:48, 12:02, 12:03

> `패널티 랜덤 부여` 일단 살리고 경험으로 테스트

> 게이지 감소를 살리고 2가지 랜덤 기획 원문 그대로

**산출물** — 커밋 `31105f6` `1e129fe` `1a87300`

**이 결정이 접점 계약을 건드렸다.** `AGENTS.md` §2가 `gauge`를 정답 전용 채널로 두고 있었는데
오답·타임아웃에 감소 방향이 열렸다. 접점 개수가 는 것이 아니라 **채널 하나에 방향이 열린 것**이고,
기록자는 여전히 B 하나다.

손익분기 부등식의 `L`도 상수에서 **기댓값**이 됐다 — `L = r·L_감속 + (1−r)·L_게이지`.
`r`도 감소량도 정해진 적이 없어 8/6으로 넘겼다.

**사람이 한 것**

- **판단을 되돌린 주체.** 같은 날 오전 좁힌 것을 "기획 원문 그대로"로 복원하도록 지시.
  수치를 문서로 정하지 말고 **경험으로 보자**는 것이 근거였다
- **`src/state.js`와 `src/quiz.js`의 주석까지 고치도록 지시** —
  *"잠깐 내가 B야 quize 주석도 수정해"*. 규약과 코드가 두 벌로 어긋나면
  8/5 병합 때 상대가 읽는 것은 코드 쪽이라는 판단
- 게이지가 0일 때 감소가 무효가 되는 역상관을 **알고 받아들임** (8/6 관측 대상으로 남김)

---

### L-017 · 2026-08-05 · 도구·자동화

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) · Codex (gpt-5.5) |
| 담당 | 공동 |
| 목적 | 팀원이 Codex를 쓰는 경우에도 프롬프트 원문이 빠지지 않게 하고, 정본이 두 벌이 되는 것을 막음 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 14:20, 15:27, 15:29, 15:37, 15:38

> codex용 skill, hook 생성

**산출물** — 커밋 `868751b` `9096539` `b2c6e13`

- 훅·스킬 **정본을 `.agents/`에 한 벌만** 두고 `.claude/`·`.codex/`는 가리키기만 한다.
  `CLAUDE.md`가 `AGENTS.md`를 가리키는 것과 같은 구조다
- raw 항목 헤더에 **어느 도구로 쓴 것인지**가 함께 적힌다. 같은 파일에 둘이 쌓기 때문이고,
  그 표기가 지침 §3의 `도구` 필드 근거가 된다
- 스킬에서 도구 전용 지시를 걷어냄 — Codex는 `.claude/`를 읽지 않는다

**실제로 Codex에서 훅이 도는 것을 확인했다.** 15:37 항목의 헤더가
`· Codex (gpt-5.5)`로 찍혀 있는 것이 그 증거다.

**사람이 한 것**

- Codex 대응을 **먼저 요구.** 훅이 없는 도구로 전환하면 그 기간 기록이 통째로 빈다
- `raw/README.md`의 Codex 서술이 **예시인지 실제인지 되물어** 확인.
  문서가 동작하지 않는 것을 사실처럼 적는 것을 막았다
- **Codex에서 직접 입력해 검증** (15:37). 문서가 아니라 실행으로 확인한 것이다

---

### L-018 · 2026-08-05 · 코드

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview |
| 담당 | B |
| 목적 | 8/4 스태시를 되살리고, 그 사이 바뀐 기획에 코드를 맞춤 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 16:30, 17:26, 17:47, 18:00, 18:08

> - 모달 UI를 실제 휴대폰처럼 높이를 더 길게
> - 기획_1차_보완.md 재확인: 좌우 조작은 A/D이고, 스페이스바는 눌렀을 때 정면을 보는 것으로 확정했음. 이 외에 기획과 다른 부분 있는지 파악
> - 정답 분포를 고정하지 않고, 보기를 랜덤 배치하면 어떰?

> - harness.html 상 게이트에서 질문 이벤트가 트리거 되고 있음. 게이트와 질문 이벤트 시점은 관계가 없음. 수정할 것.
> - 기존 스태시에 있던 문서보다 현재 @docs/기획_1차_보완.md 와 @AGENTS.md 가 최신본임. 스태시와 충돌이 발생하면, 무조건 현재 문서로 판단할 것.

**산출물** — 커밋 `ead4bf7` `0146a8c` `a508b97`
및 `docs/ai-log/specs/2026-08-05-phone-quiz-sync.md` (5라운드, 모호도 14%)

스태시와 8/5 기획이 어긋난 곳이 **11개**였다. 카운트다운 6→10초, 감속 고정→2종 랜덤,
`gauge` 감소 방향, 숫자키 제거, 모달 형태, 알림 색 랜덤 등.

**질문 트리거가 갈림길에 물려 있던 것이 가장 큰 결함이었다.** 그러면 문항 수가 맵 구조에
묶여 스테이지마다 달라지고 기획의 **주기 6~8초**가 성립하지 않는다. `quiz.js`가 자기
타이머로 세도록 바꾸면서 `mountQuiz`가 `onGate`를 받지 않게 됐고, **접점 `onGate`를 지금
듣는 쪽이 없다** — 남은 소비처인 길 안내는 §3의 열림 항목이다.

> ⚠️ **Space 방향은 8/4 지시(L-014)의 반대로 되돌아갔다.** 8/4에는 사용자 지시로
> Space=폰이었고, 8/5에 프로토타입·`기획_1차_보완.md` §조작과 맞추어 **Space=정면**이 됐다.
> **커밋 `4dbdd55`·`a508b97`의 본문은 이 경위를 적지 않고 "8/4 판이 반대로 적었다"로만
> 서술했다.** 8/4 코드는 그날의 지시를 따른 것이므로 그 서술은 정확하지 않다.
> 커밋은 고치지 않는다 (§5 — squash·force-push 금지). 경위는 여기 남긴다.

검증은 브라우저 확장이 연결되지 않아 **DOM 셰임으로 모듈을 구동해 34개 항목을 기계적으로
확인**했다 — 주기 실측 6.03~8.00초, 갈림길 발화로는 모달이 뜨지 않음, 셔플 후 정답 자리가
0·1·2 전부 관측됨 등.

**사람이 한 것**

- **최신본 우선 원칙을 명시적으로 지시.** "스태시와 충돌하면 무조건 현재 문서로 판단할 것" —
  이 한 줄이 11건 대조의 기준이 됐다
- **게이트-질문 결합을 발견하고 지적.** 에이전트는 스태시 코드를 그대로 물려받아 검증하지 않았다
- 모달 높이를 실제 폰 비율로 요구
- **보기 랜덤 배치를 제안** → L-019의 방향을 바꿈
- 퀴즈가 안 뜨는 것을 보고 → 원인이 브라우저 캐시임이 드러나 `AGENTS.md` §6에 `-c-1`이 박힘

---

### L-019 · 2026-08-05 · 레벨·밸런싱

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 더미 12개로는 확인할 수 없던 실제 플레이 분량을 콘텐츠로 채움 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 16:30 이후 선택지 응답, 17:26

> 질문 문항 수는 최종적으로는 단조로움 탈피를 위해 보다 많은 질문을 구성하고 인플레이에서 랜덤으로 뽑는 것을 생각중이나, 지금은 20개 내외만 생성.

**산출물** — 커밋 `55f9eec` — `content/day1.json` 문자 58개 · 질문 20문항

개수가 계산에서 나왔다. 스트림 2.6초 간격 × 스테이지 상한 150초 ≈ 58,
주기 7초 기준 질문 ≈ 20. **모달 중 스트림이 멈추므로** 질문 20개 × 10초는 이 계산에
들어오지 않는다 — 안 멈췄다면 135개가 필요했다.

**초안에서 결함이 하나 나왔다.** 20문항 중 **18개의 정답이 보기 2번**이어서,
`balance-todo.md` §2가 판정 실험으로 잡아 두려던 *"전부 찍기만 해서 클리어되는가"*가
콘텐츠 단계에서 이미 성립했다. 처음에는 7/7/6으로 흩어 고쳤으나,
**사용자 제안으로 런타임 셔플로 대체**했다 — JSON에서 흩는 것은 한 판 안에서만 고르고
다시 하면 같은 자리라 외우면 그만이기 때문이다.

**사람이 한 것**

- **질문 20개는 "풀"이고 최종적으로는 더 늘려 인플레이 랜덤 추출로 간다**는 방향 제시
- **보기 랜덤 배치를 제안** — 콘텐츠에서 정답 위치를 관리하던 것을 코드로 옮겨,
  JSON은 사람이 검토하기 쉬운 자연스러운 순서로 되돌아갔다

---

### L-020 · 2026-08-05 · 문서

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 코드를 기획에 맞추는 과정에서 드러난, 문서 쪽의 어긋남을 함께 정정 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 17:26, 17:47 (L-018과 같은 세션)

**산출물** — 커밋 `4dbdd55` `63fde40`

정본인 `AGENTS.md`가 **자기 자신과 두 곳에서 모순**이었다.

- §3 **확정**의 *"오답과 타임아웃의 패널티는 같다. 둘 다 감속이다"* — 꼬리말이 랜덤 부활
  전 문구다. 같은 절의 **잠정** 항목도, §2도, `docs/기획_1차_보완.md`도 2종 랜덤을 말한다
- §1의 `quiz.js` 범위가 *"감속 패널티"*로 남아 있었고 **질문 도착 주기의 소유가 표에 없었다**.
  소유가 없으면 A가 `main.js`에 같은 타이머를 또 만든다
- §2에 **`onGate`가 갈림길 신호이지 질문 신호가 아니라는 것**을 못 박음
- `docs/ui-spec.md`의 Space 방향 정정 (경위는 L-018 참조)
- §6 실행 명령에 **`-c-1`** — `http-server` 기본값이 `max-age=3600`이라 고친 `.js`가
  한 시간 동안 반영되지 않는다. 8/5에 두 번 걸렸고, 두 번째는 캐시된 옛 코드가 새 호출
  규약과 안 맞아 예외를 던져 **퀴즈가 통째로 안 뜨는 것**으로 나타났다

**사람이 한 것**

- **최신본을 판단 기준으로 지정** — "기획_1차_보완.md와 AGENTS.md가 최신본이다"
- 정본 수정이 **공동 자산에 대한 단독 변경**임을 알고 진행 여부를 승인.
  A와의 확인이 필요하다는 것은 PR 본문에 남겼다

## §3. 폐기 기록

`반영: 폐기`인 항목을 여기에도 남긴다. **지우지 않는다.**
제출물 4번의 "AI 산출물의 검증" 절이 이 표에서 나온다.

| # | 날짜 | 무엇을 | 왜 폐기했나 |
|---|---|---|---|
| D-001 | 2026-08-03 | `docs/ai-log/prompts/` 층 (긴 세션을 주제별 파일로 분리 보관) | 훅이 모든 원문을 `raw/`에 자동 저장하게 되면서 존재 이유가 사라졌다. 훅이 못 잡는 세션도 `raw/`에 직접 붙여넣는 것으로 규정되어 있어 중복. 층을 셋(raw→prompts→엔트리)으로 두는 것은 이 규모에서 과투자 |
| D-002 | 2026-08-03 | 프롬프트 **직전** 커밋 해시를 기록하는 단일 훅 | 알고 싶은 것은 "이 지시가 반영된 커밋"인데 직전 해시는 그 반대였다. 2단계(UserPromptSubmit + Stop) 구조로 대체 |
| D-003 | 2026-08-03 | 스택형 브랜치 토폴로지 (①→②→③ 중첩) | 세 PR의 파일 집합이 겹치지 않아 스택이 기여하는 바가 없는데 병합 순서만 강제했다. 실제로 3-way 병합으로 충돌 0을 확인한 뒤 main 기준 독립 브랜치로 대체 |
| D-004 | 2026-08-03 | feature 단위 skill 세트 | 파일 5개를 3.5일에 각자 한두 번 쓰고 끝나는 규모에서 반복 워크플로가 성립하지 않는다. 같은 효과(경계 고정)를 `AGENTS.md` 한 장이 훨씬 싸게 낸다. `/ai-log`만 예외로 남김 |
| D-005 | 2026-08-03 | develop 브랜치 도입안 | **GitHub Pages는 저장소당 게시 소스가 하나**라 develop을 별도 URL로 띄울 수 없다. develop의 핵심 이점("미리 보고 main으로")이 원천 봉쇄된다. 게다가 8/7 동결 전까지 main이 깨져도 손해가 없고, 충돌은 이미 파일 소유권(§1)이 막고 있다. 대가만 확실하다 — 머지 2단계, 핫픽스도 develop 경유 |
| D-006 | 2026-08-03 | GitHub Actions 배포 워크플로 | `docs/`·`prototype/`을 웹에서 감출 수 있다는 이점이 있으나, 워크플로 자체가 새 실패 지점이 된다. `team-plan.html` §02가 "배포 파이프라인이 마지막 날 백지를 만든다"고 경고한 실패 모드다. branch 방식(`main`/root)으로 대체하고 루트 전체 노출은 수용 |
| D-007 | 2026-08-03 | three.js CDN + import map 로딩안 | 저장소가 가벼워지는 대신 **심사 당일 외부 서버 상태에 게임이 종속된다.** CDN이 죽거나 막히면 백지이고 그건 통제할 수 없다. 대회 요건이 "심사 종료 시점까지 접근 가능"인데 제3자에 의존하게 된다. `vendor/` 동봉으로 대체 — 대가는 파일 하나(~1MB) |

## §4. 외부 에셋·오픈소스 출처

에셋을 **쓰는 순간** 추가한다. 나중에 몰아서 정리하지 않는다.
**라이선스를 확인할 수 없는 에셋은 사용하지 않는다.**

| 파일 경로 | 종류 | 출처 URL | 라이선스 | 저작자 표기 의무 | 비고 |
|---|---|---|---|---|---|
| `docs/질문_이벤트_휴대폰_UI_예시.png` | 이미지 (UI 목업) | AI 생성 — Gemini / Nano Banana | 외부 저작물 아님 (생성물). 도구 약관 확인 필요 | 없음 | 배치·비율만 명세 근거. 안의 글자와 번호는 생성 산물이라 명세가 아니다 → `docs/ui-spec.md` |

*(**three.js 도입이 확정됐으므로 `vendor/` 파일이 들어오는 커밋에서 행을 추가한다** — 버전·MIT·출처 URL.)*

> 위 이미지는 게임에 들어가는 에셋이 아니라 **기획 단계의 참조 목업**이다. 그래도 여기 적는 것은
> §4가 "쓰는 순간" 적으라고 정하고 있고, 제출물 4번이 AI 산출물의 출처를 요구하기 때문이다.
> 같은 날 같은 도구로 뽑은 배치 예시 이미지가 한 장 더 있었으나 **저장소에 넣지 않았다** —
> 실시간 방식 안의 그림이라 모달로 가기로 한 뒤에는 오해만 만든다.

## §5. 게임 내 AI 기능 — 미탑재 판단

제출물 4번의 "게임 내 AI 기능" 절이 여기서 나온다.

**판단: 런타임 AI를 탑재하지 않는다.**

근거

- 실질 개발 기간이 3.5일(8/3~8/6)이고 버퍼가 없다 — `team-plan.html` §01
- 심사자가 별도 API 키·계정·결제 없이 플레이할 수 있어야 한다 — `대회_정보.md` 제출물 1번
- 유료 API 사용료는 참가자 부담이며, 심사 종료 시점까지 링크가 살아 있어야 한다
- 대신 **콘텐츠 생성(대화 로그·질문·정답·함정 단서)에 AI를 쓰고 정적 JSON으로 굽는다.**
  플레이 경험에는 AI 산출물이 그대로 들어가되 런타임 의존은 없다

*(판단이 바뀌면 지우지 말고 여기에 이어서 적는다.)*
