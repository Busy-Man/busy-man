# AI 활용 로그

제출물 4번(AI 활용 기술 문서)의 **유일한 재료.** 작성 규칙은 `ai_log_guidelines.md`를 따른다.
갱신은 `/ai-log`로 한다. 직접 고쳐도 되지만 §1 색인과 §2 본문을 항상 함께 고친다.

- 최종 갱신: 2026-08-08
- 누적 항목 수: 76

> 이 파일은 **누적**이다. 기존 항목을 지우거나 요약해서 줄이지 않는다.
> 폐기된 산출물도 지우지 않고 `반영`을 `폐기`로 바꾼다.

---

## §0. 사용 도구 목록

새 도구를 처음 쓴 날 행을 추가한다. 모델·버전까지 적는다.

| 도구        | 버전·형태                                    | 최초 사용  | 주 용도                                           |
| ----------- | -------------------------------------------- | ---------- | ------------------------------------------------- |
| Claude Code | Claude Opus 5                                | 2026-08-03 | 저장소 초기화, 규약 문서, 로깅 자동화, 폰·모달 구현 |
| Claude Code | Claude Sonnet 5                              | 2026-08-04 | 맵 밸런싱, world.js 구현(길·건물 게이트), 커밋/PR |
| Gemini      | Nano Banana (이미지 생성) — 모델 버전 미확인 | 2026-08-04 | 질문 폰 UI 목업 이미지                            |
| Codex       | gpt-5.6-terra / gpt-5.6-sol (CLI)           | 2026-08-05 | 기획 검토, 콘텐츠·퀴즈 구현, 테스트·커밋·PR       |
| Claude Code | Claude Opus 4.8                              | 2026-08-07 | 부스터·시간 게이지·시간초과 모달 등 A 파일 기능 구현 |
| GPT         | 이미지 생성·치수 제안 (버전 미상)            | 2026-08-07 | fence·가로수·바리게이트 3D 에셋 디자인 보조(모델링은 Blockbench로 직접) |
| OpenAI      | 내장 이미지 생성 — 모델 버전 미확인          | 2026-08-08 | 정식 시작 표지용 무문자 키아트 생성               |

## §1. 엔트리 색인

§2와 **같은 순서로** 행을 추가한다. 색인과 본문이 어긋나면 안 된다.

| #     | 날짜       | 영역        | 도구        | 담당 | 반영        | 한 줄 요약                                                                     |
| ----- | ---------- | ----------- | ----------- | ---- | ----------- | ------------------------------------------------------------------------------ |
| L-001 | 2026-08-03 | 기획        | Claude Code | 공동 | 부분 수정   | 소크라테스식 문답으로 저장소 초기화 요구사항을 명세로 확정                     |
| L-002 | 2026-08-03 | 코드        | Claude Code | 공동 | 부분 수정   | 파일 소유 경계 스켈레톤과 Pages 진입점 생성                                    |
| L-003 | 2026-08-03 | 문서        | Claude Code | 공동 | 부분 수정   | AGENTS.md 정본 체계 수립, 저장소 밖 문서 이관·최신화                           |
| L-004 | 2026-08-03 | 도구·자동화 | Claude Code | 공동 | 부분 수정   | 프롬프트 원문 포획 훅과 /ai-log 스킬 구축                                      |
| L-005 | 2026-08-03 | 기획        | Claude Code | 공동 | 부분 수정   | 리뷰 지침·브랜치 전략·PR 규칙 요구사항을 명세로 확정                           |
| L-006 | 2026-08-03 | 도구·자동화 | Claude Code | 공동 | 부분 수정   | Copilot 리뷰 지침, PR 템플릿, pr-commit 스킬 구축                              |
| L-007 | 2026-08-03 | 문서        | Claude Code | 공동 | 부분 수정   | 브랜치·배포 결정 확정, 실제와 반대로 적힌 담당자 표기 정정                     |
| L-008 | 2026-08-04 | 문서        | Claude Code | 공동 | 부분 수정   | raw 보관 정책 확정, 훅 승인 안내, 명세·계획서 저장소 편입                      |
| L-009 | 2026-08-04 | 기획        | Claude Code | 공동 | 그대로      | 렌더러를 three.js(저장소 동봉)로 확정하고 문서에 반영                          |
| L-010 | 2026-08-04 | 레벨·밸런싱 | Claude Code | A    | 폐기        | 갈림길 구조를 새로 설계하는 접근(deep-interview + v1~v6) — 방향 전환으로 폐기  |
| L-011 | 2026-08-04 | 레벨·밸런싱 | Claude Code | A    | 부분 수정   | 기존 맵 구조 유지, 구간 시간 배정으로 유효 경로 7개 확정                       |
| L-012 | 2026-08-04 | 코드        | Claude Code | A    | 골격만 차용 | world.js 최초 구현(prototype 포팅) + 맵 구조 적용 범위·회전 여부 결정          |
| L-013 | 2026-08-04 | 코드        | Claude Code | A    | 폐기        | 좌우 회전 가능한 열린 도시(자유 이동·건물 진입) 버전 — 최소 버전으로 전면 대체 |
| L-014 | 2026-08-04 | 코드        | Claude Code | A    | 부분 수정   | "길" 구조·이동감만 검증하는 최소 버전 구현                                     |
| L-015 | 2026-08-05 | 코드        | Claude Code | A    | 부분 수정   | prototype 게이트를 재사용한 건물(게이트+상자) 구현                             |
| L-016 | 2026-08-05 | 코드        | Claude Code | A    | 폐기        | 시간(초) 기반 자동 좌표 생성으로 맵 리팩터링 시도 — 벽 구멍·배치 어긋남 반복으로 되돌림 |
| L-017 | 2026-08-05 | 코드        | Claude Code | A    | 그대로      | 맵 구조 유지, MAP_SCALE 균등 확대로 전체 이동시간 ~70초·건물 체류 ≥5초 조정        |
| L-018 | 2026-08-05 | 코드        | Claude Code | A    | 그대로      | B7('ㅗ') 천장 세 갈래 전체 커버, T·C 색상 구분(이벤트 없이) 구현                  |
| L-019 | 2026-08-05 | 코드        | Claude Code | A    | 골격만 차용 | prototype 행인을 경로 위에 배치하는 초기 버전 — 이후 맵 전체 배치로 재작성됨      |
| L-020 | 2026-08-05 | 코드        | Claude Code | A    | 부분 수정   | 건물 게이트 간격 조정, B7 중앙 회전공간 제외, 행인을 맵 전체 도로망에 재배치, 도착지 연출·다시 시작 |
| L-021 | 2026-08-05 | 코드        | Claude Code | A    | 그대로      | 플레이 타이머·HUD·결과 화면·다시 시작 버튼(main.js) 구현                          |
| L-022 | 2026-08-05 | 코드        | Claude Code | A    | 그대로      | 공사장 바리게이트·신호등 횡단보도 이벤트(안전 차선·갇힘 로직 포함) 구현            |
| L-023 | 2026-08-05 | 문서        | Claude Code | A    | 그대로      | 이전 병합에서 §4 표에 남은 충돌 마커를 정리(내용은 두 행 모두 유지)               |
| L-024 | 2026-08-06 | 코드        | Claude Code | A    | 그대로      | 행인 생성 금지 구간을 건물 전체 대신 게이트의 경로 진행 거리(s) 기준으로 변경         |
| L-025 | 2026-08-06 | 코드        | Claude Code | A    | 그대로      | 신호등(T) 구역 행인 배치·차선 판정 시점을 8회 피드백으로 자연스럽게 다듬음            |
| L-026 | 2026-08-06 | 레벨·밸런싱 | Claude Code | A    | 그대로      | 맵 시간 배분 문서를 실측(현재 좌표) 기준으로 다시 쓰고 채택 밴드를 60~81초로 좁힘      |
| L-027 | 2026-08-06 | 코드        | Claude Code | A    | 그대로      | ROUTES를 60~81초 밴드 9개로 교체                                                     |
| L-028 | 2026-08-06 | 코드        | Claude Code | A    | 그대로      | 실제 갈림길(차수 3 이상)에서도 직진 안내를 띄우도록 확장                              |
| L-029 | 2026-08-06 | 기획        | Claude Code | A    | 부분 수정   | "휴대폰 이벤트 알림 큐" 구현 전 파일 구조 검토 — pushMap 재사용·갈림길 물리 제약 발견 |
| L-030 | 2026-08-06 | 코드        | Claude Code | A    | 부분 수정   | 건물 게이트 전부(진입·중간·진출)를 판정 대상으로 넣고 틀리면 되돌리기 구현            |
| L-031 | 2026-08-06 | 코드        | Claude Code | A    | 그대로      | 회전·게이트·경고 안내를 큐 하나로 통합해 재안내 누락 문제를 구조적으로 해소            |
| L-032 | 2026-08-06 | 코드        | Claude Code | A    | 그대로      | main.js에 phone/quiz 마운트 + world 안내를 phone.pushMap으로 배선                     |
| L-033 | 2026-08-06 | 코드        | Claude Code | A    | 그대로      | 폰 신호등·공사장 안내에 뜨던 undefined 버그(lane 필드 누락) 수정                       |
| L-034 | 2026-08-04 | 도구·자동화 | Claude Code | 공동 | 부분 수정   | 자정을 넘긴 턴의 반영 커밋이 비던 훅 버그 수정                                        |
| L-035 | 2026-08-04 | 문서        | Claude Code | 공동 | 부분 수정   | 커밋·PR 본문 형식 고정, rebase 금지와 그 사유 명문화                                  |
| L-036 | 2026-08-04 | 기획        | Claude Code | B    | 부분 수정   | 폰·모달 구현 계획을 Planner/Architect/Critic 합의로 확정                             |
| L-037 | 2026-08-04 | 코드        | Claude Code | B    | 부분 수정   | 접점 4개를 `state.js`에 실물로 구현                                                  |
| L-038 | 2026-08-04 | 코드        | Claude Code | B    | 부분 수정   | `phone.js`·`quiz.js` 1차 구현과 dev 검증 하네스                                      |
| L-039 | 2026-08-05 | 기획        | Claude Code | 공동 | 부분 수정   | 기획 1차 보완을 기준으로 확정 목록 재조정, 3등급 도입                                |
| L-040 | 2026-08-05 | 레벨·밸런싱 | Claude Code | B    | 부분 수정   | 오답 패널티 2종 랜덤 부활 — 같은 날 좁혔던 것을 되돌림                               |
| L-041 | 2026-08-05 | 도구·자동화 | Claude Code · Codex | 공동 | 부분 수정 | 훅·스킬을 Codex에도 붙이고 정본을 `.agents/`로 이관                            |
| L-042 | 2026-08-05 | 코드        | Claude Code | B    | 부분 수정   | 스태시 복원과 8/5 기획 동기화, 모달을 폰 형태로 재구현                               |
| L-043 | 2026-08-05 | 레벨·밸런싱 | Claude Code | B    | 부분 수정   | `day1.json` 실콘텐츠 — 문자 58개, 질문 20문항                                        |
| L-044 | 2026-08-05 | 문서        | Claude Code | B    | 그대로      | 규약·UI 명세가 기획·구현과 어긋난 네 곳을 정정                                       |
| L-045 | 2026-08-06 | 코드        | Claude Code | 공동 | 그대로      | world.js 네비게이션 토스트를 걷어내고 신호등 잘못된 차선 경고만 남김                  |
| L-046 | 2026-08-06 | 코드        | Claude Code | 공동 | 부분 수정   | main.js를 정식 phone.js/quiz.js/day1.json로 재배선, phone.js pushMap을 이벤트 기반으로 교체 |
| L-047 | 2026-08-06 | 코드        | Claude Code | 공동 | 부분 수정   | quiz.js 판정 후 이동 재개 타이밍 수정(사람 작성)을 재포맷팅 노이즈 없이 커밋에 반영    |
| L-048 | 2026-08-07 | 코드        | Claude Code | A    | 부분 수정   | 도착 화면에서 폰·퀴즈 모달을 멈추고 재시작 시 퀴즈를 재마운트로 되살림                 |
| L-049 | 2026-08-07 | 코드        | Claude Code | A    | 부분 수정   | 정답 충전·W 소비 부스터(무적+가속) 구현, 게이지 채널을 A가 소비만 하도록 배선          |
| L-050 | 2026-08-07 | 코드        | Claude Code | A    | 부분 수정   | 게이트 오진입 토스트·루트별 제한시간·시간 게이지·시간초과 모달·게이지 좌상단 배치       |
| L-051 | 2026-08-07 | 코드        | Claude Code | A    | 그대로      | 퀴즈 오답 감속(state.speedMul)이 world.js 전진 속도에 반영 안 되던 연결 누락 수정        |
| L-052 | 2026-08-07 | 코드        | Claude Code | A    | 부분 수정   | 신호등 구역에만 랜덤 배치 행인 스폰을 막음(spawnBlocks 확장) — 범위를 세션 중간에 정정  |
| L-053 | 2026-08-07 | 코드        | Claude Code | A    | 그대로      | R키 다시 시작 확인 모달 추가 + 재시작 시 퀴즈 인스턴스가 중복되던 버그 수정              |
| L-054 | 2026-08-07 | 코드        | Claude Code | A    | 부분 수정   | 길 안내 순서를 문서 9개 경로와 맞추고, 그 과정에서 드러난 안내 큐 영구 정지 버그 수정      |
| L-055 | 2026-08-07 | 아트        | GPT         | A    | 부분 수정   | fence·가로수·바리게이트 3D 에셋 디자인 보조(이미지 생성·치수 제안) + Blockbench 직접 제작   |
| L-056 | 2026-08-07 | 코드        | Claude Code | A    | 부분 수정   | 도로 벽·잔디 바닥·가로수를 fence.glb 계열·tree.glb 3D 에셋으로 교체, 잔디 뒷면 컬링 버그 수정 |
| L-057 | 2026-08-07 | 코드        | Claude Code | A    | 그대로      | 공사장 바리게이트를 barricade.glb로 교체, 공사장 벽 색 구분 제거, GLTFLoader 중복이름 버그 수정 |
| L-058 | 2026-08-06 | 기획        | Codex       | B    | 그대로      | 근거 대화가 3초 이상 보인 뒤에만 해당 퀴즈를 열도록 명세 확정                         |
| L-059 | 2026-08-06 | 코드        | Codex       | B    | 그대로      | 근거 문자 표시 시각을 출제 자격에 연결해 대화보다 퀴즈가 먼저 뜨는 문제 해결          |
| L-060 | 2026-08-07 | 기획        | Codex       | B    | 골격만 차용 | 업무 문자형 퀴즈 문체·발신자·숫자키 입력의 범위와 안전한 입력 경계 검토               |
| L-061 | 2026-08-07 | 코드        | Codex       | B    | 부분 수정   | 업무 문자형 문체·발신자와 숫자키 전용 답안 선택·눌림 효과 구현                         |
| L-062 | 2026-08-07 | 코드        | Codex       | B    | 그대로      | 메시지 종류별 배경색을 랜덤 대신 고정색으로 통일                                      |
| L-063 | 2026-08-07 | 코드        | Codex       | B    | 그대로      | 퀴즈 모달의 질문과 하단 답장 후보 사이 여백 축소                                      |
| L-064 | 2026-08-06 | 기획        | Claude Code | B    | 골격만 차용 | 반복 암기를 막기 위한 판 단위 대화·퀴즈 수치 랜덤화 명세·계획 수립                    |
| L-065 | 2026-08-07 | 코드        | Codex       | B    | 부분 수정   | 대화 묶음·수치·보기까지 판마다 새로 구성해 반복 플레이의 암기 이점 축소                |
| L-066 | 2026-08-07 | 코드        | Codex       | B    | 부분 수정   | 두 정보를 모두 기억해야 풀리는 복합 문항 10개와 매판 20% 고정 출제 구현                |
| L-067 | 2026-08-07 | 코드        | Claude Code | 공동 | 부분 수정   | 지인 테스트용 단색 임시 시작 화면(목표·조작법 안내) 추가, 시작 전 게임 부트스트랩 게이팅 |
| L-068 | 2026-08-07 | 코드        | Claude Code | 공동 | 그대로      | main 병합으로 부스터·시간초과·R 재시작 반영, 재시작 시 부스터 게이지 미초기화 버그 수정  |
| L-069 | 2026-08-08 | 코드        | Claude Code | A    | 그대로      | 맵분위기.png 배치 아이디어대로 잔디 배경을 도시 에셋(건물·가로등·화단·나무)으로 꾸미고 신호등 벽 제거 |
| L-070 | 2026-08-08 | 코드        | Claude Code | A    | 그대로      | L-069의 가로등이 과하게 커 보인다는 피드백을 받아 목표 높이를 절반으로 축소               |
| L-071 | 2026-08-08 | 코드        | Claude Code | A    | 그대로      | B7 이후 안내가 통째로 멈추던 문제 — L-054가 놓친 같은 버그 계열을 재현·근본 수정          |
| L-072 | 2026-08-08 | 코드        | Claude Code | A    | 그대로      | 도착 지점 "도착" 기둥·배너를 회사 건물(myCompany.glb)로 교체, 결승선 뒤 도로 0.5초 연장    |
| L-073 | 2026-08-08 | 코드        | Claude Code | A    | 부분 수정   | 좌상단 시간·부스터 게이지에 아이콘 적용, 여러 차례 디자인 조정 끝에 최초 레이아웃+막대 높이 확대로 정착 |
| L-074 | 2026-08-08 | 코드        | Claude Code | A    | 그대로      | 신호등 갇힘 구간을 펜스 경계까지로 좁히고 trafficLight.glb 설치, 바닥을 사다리 무늬로 교체 |
| L-075 | 2026-08-08 | 아트        | OpenAI      | A    | 부분 수정   | 출근길 주행과 업무 문자 응답을 한 장면에 담은 무문자 정식 표지 키아트 제작             |
| L-076 | 2026-08-08 | 코드        | Codex       | A    | 부분 수정   | 임시 시작 화면을 판교 테크 오피스풍 정식 표지로 교체                                  |

**영역 분포** (균형 점검용 — `코드`·`아트`·`사운드`·`레벨·밸런싱`이 절반 이상이어야 한다)

| 영역        | 건수 |
| ----------- | ---- |
| 기획        | 9    |
| 코드        | 50   |
| 아트        | 2    |
| 사운드      | 0    |
| 레벨·밸런싱 | 5    |
| 문서        | 6    |
| 도구·자동화 | 4    |

> PR #13 병합 갱신(2026-08-06): main에 먼저 들어가 있던 B의 L-010~L-020을
> L-034~L-044로 이어붙여 33건 → 44건이 됐다. `phone.js`·`quiz.js`·`state.js`
> 작업이 이번에 처음 로그에 실렸다 — 8/6 이전 note가 지적하던 "B 쪽 작업 미반영"은
> 이 병합으로 해소됐다. `코드`+`레벨·밸런싱`이 28/44(64%)로 여전히 절반을 넘는다.
> **`아트`와 `사운드`는 여전히 0이다.** 지금 스코프에 에셋 작업이 없어서인데,
> 8/6 밸런싱과 8/7 이후 에셋 작업에서 채워져야 한다.

> PR #19 병합 갱신(2026-08-07): main에 먼저 들어가 있던 B의 L-048~L-055(콘텐츠
> 랜덤화 · 업무 문자형 퀴즈)와 PR #19의 A L-048~L-054(부스터 · 시간 게이지 · 길
> 안내 정리)가 같은 번호대를 각자 썼다. A의 번호를 그대로 두고 B의 8건을
> L-055~L-062로 이어붙였다 — PR #13 때와 같은 방식이다.

> main 병합 갱신(2026-08-07, PR #21 반영): main에 먼저 들어가 있던 L-063~L-065
> (복합 문항 10개 · 지인 테스트용 시작 화면 · main 병합/재시작 버그 수정)가 이
> 브랜치의 L-055~L-065(3D 에셋 · 업무 문자형 퀴즈)와 같은 번호대를 각자 썼다.
> 이 브랜치의 번호를 그대로 두고 main의 3건을 L-066~L-068로 이어붙였다 —
> PR #13·#19 때와 같은 방식이다.

## §2. 엔트리

아래 형식으로 `L-001`부터 이어 붙인다.

> 엔트리가 가리키는 `ai-log/raw/YYYY-MM-DD.md`는 **저장소에 없다. 노션에 보관한다.**
> 각자 로컬에 쌓이고, 문서 작성 시 내려받는다. 사유는 `ai-log/raw/README.md` 참조.

<!--
### L-0NN · YYYY-MM-DD · 영역

| | |
|---|---|
| 도구 | |
| 담당 | |
| 목적 | |
| 반영 | 그대로 / 부분 수정 / 골격만 차용 / 폐기 |

**프롬프트**

> 원문 그대로. 길면 ai-log/raw/의 날짜·시각으로 가리킨다.

**산출물**

**사람이 한 것** — 비워두지 않는다.
-->

### L-001 · 2026-08-03 · 기획

|      |                                                                                  |
| ---- | -------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5) — deep-interview                                     |
| 담당 | 공동                                                                             |
| 목적 | 초기 상태 저장소를 무엇으로 채울지, 어디까지가 뼈대이고 어디부터가 구현인지 확정 |
| 반영 | 부분 수정                                                                        |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (2)

에이전트가 한 번에 한 질문씩 던지고 답할 때마다 모호성 점수를 다시 계산하는 방식.
목표·제약·완료판정·맥락 네 축으로 점수를 매겨 임계값(20%) 아래로 내려갈 때까지 반복했다.

**산출물** — `docs/ai-log/specs/2026-08-03-repo-init.md` (182줄)

4라운드 만에 모호성 100% → 14%. 6개 최상위 컴포넌트, 수용 기준 17개,
드러난 가정 6건과 그 해소 과정이 기록되어 있다.

특히 뒤집힌 가정 두 개가 이후 설계를 갈랐다.

- "훅이 로그를 자동으로 채운다" → 가이드라인이 요구하는 `사람이 한 것`·`반영 판정`은
  사후 판단이라 훅이 알 수 없다. 훅의 고유 가치는 **복원 불가능한 원문의 포획**뿐이다
- "Pages는 배포만 되면 된다" → 진짜 리스크는 하위 경로 ES 모듈 해석이다

**사람이 한 것**

- 스켈레톤이 구현 코드를 잠식할 것을 우려해 **"디렉토리 + 빈 파일만"으로 범위를 잘라냄.**
  에이전트가 추천한 "접점만 실물, 나머지는 배선"보다 한 단계 더 보수적인 선택
- 컴포넌트 토폴로지에 **Pages 배포 관통**을 6번째로 추가
- 팀원 도구 환경(Claude Code 주력 + Codex 대비) 정보 제공 → AGENTS.md 정본 체계의 근거
- 훅이 커밋 해시도 남기도록 지시

---

### L-002 · 2026-08-03 · 코드

|      |                                                                                |
| ---- | ------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Opus 5)                                                    |
| 담당 | 공동                                                                           |
| 목적 | 두 사람이 각자 AI로 코드를 뽑아도 서로 덮어쓰지 않도록 파일 경계를 실물로 고정 |
| 반영 | 부분 수정                                                                      |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (4)

**산출물** — 커밋 `0a27d14` `44afc53` `2f5c1c0` `229bca7` `9ce58cf`

- `.gitignore` — `.omc/` 제외. `docs/ai-log/raw/`의 날짜 파일은 노션 보관 결정에 따라 제외
- `.gitattributes` — LF 통일. 8/5 병합일에 줄바꿈 충돌이 진짜 충돌과 섞이는 것 방지
- `src/{state,main,world,phone,quiz}.js` — 담당자와 착수 전 확인사항만. **실코드 3줄**
- `index.html` — `./src/main.js`를 module로 로드해 Pages 하위 경로 검증

`docs/team-plan.html`이 문서로만 합의했던 파일 분담을 실제 파일로 옮긴 것이다.
그 문서 자체가 "문서로 합의하면 지켜지지 않지만, 파일이 이미 있으면 둘 다 그걸
import 한다"고 적고 있다.

**사람이 한 것**

- **담당 A/B가 실제와 반대임을 지적**하고 `docs/역할 분담.txt`를 근거로 제시.
  에이전트는 team-plan.html만 보고 반대로 배정했었다
- 반전 범위가 **구현 파일에만 해당**함을 다시 정정 — 배포·제출·영상·문서 담당은 원안이 맞다.
  이 정정으로 "8/10 제출 담당이 그날 부재"라는 모순이 해소됨
- 커밋 단위와 메시지 분량 기준 제시

---

### L-003 · 2026-08-03 · 문서

|      |                                                                                      |
| ---- | ------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Opus 5)                                                          |
| 담당 | 공동                                                                                 |
| 목적 | 저장소 밖에 있어 팀원이 볼 수 없던 규약·대회 요건을 안으로 들이고 정본을 하나로 정리 |
| 반영 | 부분 수정                                                                            |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (4)

**산출물** — 커밋 `17d9593` `1b77681`

- `AGENTS.md` — 정본. 파일 소유권, 접점 4개, 확정/미결 구분, 커밋 규약, 실행·배포 함정
- `CLAUDE.md` — `@AGENTS.md` 임포트만
- `docs/대회_정보.md` — 상위 디렉토리에서 이관 + 최신화

에이전트 판단으로 상위 `CLAUDE.md`를 그대로 옮기지 않고 **사실(대회 요건)과
행동 규약(AGENTS.md)으로 분리**했다. 매 세션 주입되는 파일에 긴 사실 문서를 넣으면
컨텍스트만 소모하기 때문. 최신화 과정에서 "컨셉별 폴더 분리" 구조 서술과
"아직 결정되지 않은 것" 6항목이 이미 낡았음이 드러나 확정 표로 교체했다.

**사람이 한 것**

- 이관 여부·위치를 애초에 **판단 대상으로 지정**해서 물음 (지시가 아니라 검토 요청)
- **`CLAUDE.md`를 요약본이 아니라 `@import`로 바꾸도록 지시** — 에이전트는 핵심 3줄을
  요약해 뒀는데, 그 요약본도 결국 두 번째 진실이 된다는 판단
- 상위 원본 2개 삭제 결정 (이관 누락 여부를 대조로 확인한 뒤 실행)

---

### L-004 · 2026-08-03 · 도구·자동화

|      |                                                                             |
| ---- | --------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                 |
| 담당 | 공동                                                                        |
| 목적 | 제출물 4번의 재료인 프롬프트 원문이 사후 복원 불가능하므로 그 포획을 자동화 |
| 반영 | 부분 수정                                                                   |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (6)

**산출물** — 커밋 `307c381`, `374b4e3`

`307c381` — 로깅 인프라

- `.claude/hooks/log-prompt.mjs` — 2단계 훅.
  `UserPromptSubmit`이 원문을 적고 `Stop`이 그 사이 늘어난 커밋을 채운다
- `.claude/settings.json` — 훅 등록 (저장소 공유)
- `.claude/skills/ai-log/SKILL.md` — raw와 git 기록을 엔트리로 승격하는 절차
- `docs/ai_log_guidelines.md` — 상위에서 이관 + 원자재/엔트리 2층 구조 반영
- `docs/ai_usage_log.md` — 이 파일의 골격

`374b4e3` — 명세 보존 규칙. 첫 기록을 쓰다가 필요성이 드러나 뒤늦게 추가됐다

- `docs/ai-log/specs/` 신설 → **원자재 / 명세 / 엔트리 3층**으로 확장
- `ai_log_guidelines.md` §2에 트리거 추가, §4를 "원문과 중간 산출물 취급"으로 넓힘.
  절 번호를 재배열하지 않아 기존 참조가 그대로 유효하다
- `SKILL.md` 3단계에 `.omc/`의 명세를 저장소로 옮기는 절차 추가

> 이 로그 파일만 고치는 커밋은 여기 나열하지 않는다 — 갱신할 때마다 늘어나 끝이 없다.
> **인프라나 지침을 함께 고친 커밋만 적는다.**

자동화 범위를 원문 포획으로 한정한 것이 핵심 판단이다. 가이드라인이 요구하는
`사람이 한 것`·`반영` 필드는 사후 판단이라 훅이 알 수 없고, 반대로 원문은
훅만 알 수 있으며 그 순간이 지나면 복원되지 않는다.

검증: 커밋 2개 발생 / 커밋 없음 / 앞 항목 미훼손 / 깨진 JSON / 파일 부재 —
다섯 경우 모두 의도대로 동작하고 세션을 막지 않음(exit 0).

**사람이 한 것**

- **커밋 해시를 "직전"이 아니라 "반영된 커밋"으로 바꾸도록 지시.** 이 한 마디가
  단일 훅을 2단계 구조로 바꿨다. 프롬프트 시점에는 그 커밋이 아직 없기 때문
- **`SKILL.md`가 가이드라인 내용을 중복 서술함을 지적** → 규칙을 전부 참조로 돌리고
  절차만 남김
- **`prompts/` 층 폐지 결정** (§3 참조)
- 훅이 없는 도구(Codex)로 전환할 가능성을 상기시켜, 훅 없이도 성립하는 프로세스를 요구
- 명세 보존을 **일회성 조치가 아니라 지침의 규칙으로** 만들 것을 요구
  ("앞으로도 deep-interview 쓸 텐데, 이거도 지침에 포함해야 하는 거 아님?").
  에이전트는 명세 하나를 옮기는 것으로 끝내려 했다

---

### L-005 · 2026-08-03 · 기획

|      |                                                     |
| ---- | --------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5) — deep-interview        |
| 담당 | 공동                                                |
| 목적 | 리뷰 지침·브랜치 전략·PR 규칙을 코드 착수 전에 확정 |
| 반영 | 부분 수정                                           |

**프롬프트** — `ai-log/raw/2026-08-03.md` (8), (9)

**산출물** — `docs/ai-log/specs/2026-08-03-review-and-conventions.md` (186줄)

3라운드, 모호성 100% → 17.1%. 5개 컴포넌트.

**확인된 외부 사실 두 개가 결정을 갈랐다.**

- **GitHub Pages는 저장소당 게시 소스가 하나다.** develop을 별도 URL로 띄울 수 없으므로
  "develop에서 미리 보고 main으로" 흐름이 성립하지 않는다 → **develop 미도입**
- `.github/copilot-instructions.md`는 코드 리뷰가 읽으며 기본 활성이지만,
  **리뷰 언어 지정은 문서화되어 있지 않다** → 보장으로 취급하지 않고 실증 항목으로 남김

**사람이 한 것**

- 토폴로지를 3개 → 5개로 쪼개도록 지시. 브랜치 전략을 **"develop 여부"와 "Pages 배포 방식"**
  둘로 나누고 PR 템플릿을 별도 컴포넌트로 추가
- develop 미도입 결정. 동결 태그는 8/7로 미룸
- Pages를 branch 방식으로 선택 — **루트 전체가 웹에 노출된다는 것을 알고도 수용**.
  Actions는 실패 지점을 늘려 3.5일 일정에 맞지 않는다는 판단
- skill 범위를 "커밋 분할 → 메시지 → PR 본문 전 과정"으로 확정

---

### L-006 · 2026-08-03 · 도구·자동화

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                        |
| 담당 | 공동                                                                               |
| 목적 | 둘 다 AI로 코드를 뽑으므로, 리뷰와 커밋·PR이 사람마다 다른 모양으로 쌓이지 않게 함 |
| 반영 | 부분 수정                                                                          |

**프롬프트** — `ai-log/raw/2026-08-03.md` (8), (9)

**산출물** — 커밋 `d79f68b`, `0bd956b`

- `.github/copilot-instructions.md` — 한국어 리뷰 지시 + 이 저장소에서 실제로 사고가 나는
  다섯 가지(소유 경계 침범, 접점 초과, 절대경로 모듈, 렌더러 미결 상태의 렌더 코드, 콘텐츠 하드코딩)
- `.github/pull_request_template.md` — 개요 / 변경 사항 / 체크리스트
- `.claude/skills/pr-commit/SKILL.md` (82줄) — 커밋 전 **소유 경계 침범 점검** 포함

**"지적하지 않아도 되는 것" 목록이 이 지침의 핵심이다.** 빈 스켈레톤, 번들러 부재,
포매터 부재가 전부 의도한 결정이라, 이걸 빼면 리뷰가 통째로 노이즈가 된다.

두 산출물 모두 규칙을 복사하지 않고 `AGENTS.md` §5를 참조만 한다. `ai-log` 스킬에서
중복을 걷어낸 선례를 따랐다.

**사람이 한 것**

- 리뷰를 **한국어로** 받을 것을 요구
- PR/commit 규칙을 skill로 만들 것을 요구
- PR 템플릿을 별도로 둘 것을 지시 — **독자가 다르다.** 템플릿은 사람이 GitHub UI에서,
  skill은 에이전트가 쓴다

---

### L-007 · 2026-08-03 · 문서

|      |                                                                               |
| ---- | ----------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                   |
| 담당 | 공동                                                                          |
| 목적 | 브랜치·배포 결정을 규약에 확정하고, 실제와 반대로 적힌 담당자 표기를 바로잡음 |
| 반영 | 부분 수정                                                                     |

**프롬프트** — `ai-log/raw/2026-08-03.md` (12), (13)

**산출물** — 커밋 `4048f95` `29613b1` `7cf7b0a` `c3543fd`

- **`team-plan.html`의 구현 담당 A/B 맞바꿈** (§03 카드·§04 일정). 그전까지는 `AGENTS.md`에
  경고를 달아 우회했는데, **팀원이 그 문서를 직접 열면 경고를 못 본다**
- develop 미도입 근거와 Pages 설정값을 `AGENTS.md`에 명시. `.nojekyll` 추가
- 문서를 실제로 고쳤으므로 `AGENTS.md`·`대회_정보.md`의 "라벨이 반대" 경고를 제거.
  경고가 남아 있으면 그것이 새 거짓이 된다

**사람이 한 것**

- `team-plan.html`을 **실제로 고치도록 지시** — 경고 우회로는 부족하다는 판단
- 반전 범위가 **구현 파일에만 해당**함을 짚음. 대외 산출물 담당은 원안이 맞다
- raw 원문의 저장소 공개를 **보류시킴.** 팀원과 상의 전이라는 이유

---

### L-008 · 2026-08-04 · 문서

|      |                                                                                         |
| ---- | --------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                             |
| 담당 | 공동                                                                                    |
| 목적 | 보관 정책이 바뀐 뒤 따라오지 못한 문서를 실제와 일치시키고, 사라질 자산을 저장소로 들임 |
| 반영 | 부분 수정                                                                               |

**프롬프트** — `ai-log/raw/2026-08-03.md` 23:33 항목, `2026-08-04.md` (a), (b), (f)

**산출물** — 커밋 `0cbf10a` `dde0dfb` `50b6cb4`
및 `docs/ai-log/specs/2026-08-04-ai-log-policy-plan.md`

- **raw 보관 정책을 노션 수동 보관으로 확정.** 문서에는 "공개 여부 미결이라 잠시 보류"로
  적혀 있어 곧 커밋할 것으로 읽혔다. `raw/README.md`의 "왜 커밋하는가" 절은 정반대 내용이었다
- **훅 승인 절차 안내** — 저장소 훅은 승인 전까지 실행되지 않는데 그 사실이 어디에도 없었다.
  이번에 직접 겪은 문제다
- 두 번째 deep-interview 명세를 `.omc/`에서 저장소로 보존 (L-004에서 만든 규칙의 이행)
- 가이드라인의 `### 두 개의 층` 제목이 3행 표를 달고 있던 것도 함께 정정

**사람이 한 것**

- 보관 사유를 **"어투가 구어체라서"가 아니라 심사자 관점에서 성립하는 근거**로
  다시 쓰도록 요구 → "제출물의 원자재이지 소스가 아니다 / Pages가 루트를 서빙한다 /
  올린 뒤에는 정정할 수 없다"로 정리됨
- 노션 업로드 자동화를 **하지 않기로 결정** — 파일 일곱 개에 스크립트·토큰을 붙일 값이
  나오지 않는다는 판단
- 계획 문서를 저장소에 보존하도록 지시

---

### L-009 · 2026-08-04 · 기획

|      |                                                                      |
| ---- | -------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                          |
| 담당 | 공동                                                                 |
| 목적 | 렌더러 미결이 `world.js` 착수를 규약상 막고 있어, 결정을 문서에 반영 |
| 반영 | 그대로                                                               |

**프롬프트** — `ai-log/raw/2026-08-04.md` (c), (d), (e)

> 이 지시는 `ExitPlanMode` 거부 시 전달되어 `UserPromptSubmit`을 거치지 않았다.
> **훅이 잡지 못하는 입력이 있다**는 것이 여기서 처음 드러났고, 같은 세션이라 대화에서
> 원문을 복원해 raw에 수동으로 채웠다. 선택지 응답도 마찬가지로 잡히지 않는다.

**산출물** — 커밋 `ced96f8`, `3014da1`

- `AGENTS.md` §3에서 렌더러를 **"열려 있음" → "확정"**으로 이동.
  남는 열린 항목은 **`content/day1.json` 스키마 하나**가 됐다
- §7의 "렌더러 결정 전에 렌더 코드 쓰기"를 **"CDN에서 three.js 불러오기"**로 교체.
  금지 항목이 사라진 것이 아니라 바뀐 것이다
- §1 소유권 표에 `vendor/` 행 추가 (공동 · 수정하지 않음)
- `copilot-instructions.md` 리뷰 포인트 교체, `대회_정보.md` 확정 표 갱신
- `src/world.js` 착수 주석 — 프로토타입은 Canvas 2D 자체 투영이라 **이식 대상이 아니라는 점**도 명시

**CDN 대신 저장소 동봉을 택한 것이 이 엔트리의 판단이다.** CDN은 심사 당일 외부 서버가
죽거나 막히면 게임이 백지가 되는데 그건 통제할 수 없다. `team-plan.html` §02가
"Vite를 끼우면 마지막 날 백지"라며 잘라낸 것과 같은 종류의 리스크이고, 대가는 파일 하나뿐이다.

**사람이 한 것**

- **three.js 최종 결정** (Canvas 2D와의 비교는 `renderer-comparison.html`에서 이미 이뤄짐)
- 동봉과 CDN의 차이를 **되물어 근거를 확인한 뒤** 동봉 채택. 에이전트 권고를 그대로 수용
- 범위가 작으니 진행 중이던 작업과 함께 처리하도록 지시

---

### L-010 · 2026-08-04 · 레벨·밸런싱

|      |                                                                                      |
| ---- | ------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5) — deep-interview                                       |
| 담당 | A                                                                                    |
| 목적 | 매 게임 랜덤 배정되는 루트가 시간상 균형 잡히도록, 맵의 갈림길 구조 자체를 새로 설계 |
| 반영 | 폐기                                                                                 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 14:17~16:34 (7개 요청, deep-interview 1건 포함)

**산출물** — `docs/ai-log/specs/2026-08-04-map-route-balancing.md` (deep-interview 6라운드 +
v1~v6 시행착오 전문)

deep-interview로 시작해(모호도 15.75%로 임계값 통과) 손그림 그래프에서 균형 잡힌 조합을
골라내는 1차 설계를 했지만 38개 중 3~4개뿐이라 부족했다. 그래서 아예 갈림길 구조를
새로 설계하는 2차 접근(대칭 3레인, 3ⁿ 조합)으로 전환해 v1~v6까지 반복했다.

**사람이 한 것**

- 매 라운드 산출물을 실사용 관점에서 반려 — "완전히 같은 종류로 나오면 갈림길 만드는
  의미가 없다"(v1, 완전 대칭안 기각), "신호등이 너무 많다"(v3→v4), "회전했을 때 나오는
  길에 시간이 왜 없냐"(v4→v5), "회전 후 원래 길로 돌아오는 비용과, 직진도 건물까지
  걸어가는 접근 구간이 빠진 것 아니냐"(v5→v6) — 총 5차례 구체적 반례로 재설계를 유도
- 이후(18:19) 이 접근 전체를 접고 "기존 맵 구조를 그대로 쓴다"는 방향으로 전환 → L-011

---

### L-011 · 2026-08-04 · 레벨·밸런싱

|      |                                                                                                       |
| ---- | ----------------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                         |
| 담당 | A                                                                                                     |
| 목적 | 기존 맵(`docs/aaa.jpg`) 구조는 그대로 두고, 구간 시간만 배정해 어느 경로든 100초 내외가 되도록 밸런싱 |
| 반영 | 부분 수정                                                                                             |

**프롬프트** — `ai-log/raw/2026-08-04.md` 18:19, 18:35, 18:46, 19:16

> 현재 맵 구조는 그대로 유지하고, 시간을 조정해 어떤 경로를 가더라도 감속이 없다는
> 가정하에 약 100초 내외에 도착하도록 밸런싱한다. (…) S(이동 구간): 4~6초, T(신호등):
> 4초 고정, C(공사장): 3초 고정, B(건물): 10~15초

**산출물** — `docs/map/맵 시간 배분.md` (유효 경로 7개 + 구간별 확정 시간 + 제외된 경로와
그 사유)

**사람이 한 것**

- S/T/C/B 시간 배정 기준을 직접 제시해 계산 방향을 확정
- "모든 경우의 90~100초 사이에 도착하는 경로만 사용, 그 사이 값이 가장 많이 나오는
  시간 분배를 채택"이라는 최적화 기준을 직접 제시(정수계획법으로 풀도록 유도)
- "이미지 기호를 이용해서 정확한 경로를 정리해달라"고 요구해 채택/제외 양쪽 다 근거를
  남기게 함

---

### L-012 · 2026-08-04 · 코드

|      |                                                                                                     |
| ---- | --------------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                       |
| 담당 | A                                                                                                   |
| 목적 | `world.js` 최초 구현(prototype 포팅) 및 L-011의 맵 구조를 world.js에 어떻게 적용할지 범위·순서 결정 |
| 반영 | 골격만 차용 (코드 구조는 이후 전면 재작성됐지만 prototype 튜닝값은 계속 재사용됨)                   |

**프롬프트** — `ai-log/raw/2026-08-04.md` 13:27, 13:47, 13:57, 19:34, 19:45, 19:49, 20:09

**산출물** — 초기 `src/world.js`(복도 렌더·자동 전진·좌우 이동·보행자 충돌 감속),
`src/main.js` 연결

**사람이 한 것**

- "맵 구조만 넣는 건데 day1 스키마를 합의한 뒤에 해야 되냐"고 되물어, 맵 구조 적용은
  B(퀴즈 스키마) 담당과 무관하게 먼저 진행할 수 있다는 범위 판단을 이끌어냄
- 좌/우회전 카메라 전환을 되살리기로 결정 — `AGENTS.md` §3의 "확정 — 되돌리지 않는다"
  항목을 뒤집는 판단이라 에이전트가 먼저 확인을 구했고, 사람이 직접 승인

---

### L-013 · 2026-08-04 · 코드

|      |                                                                                           |
| ---- | ----------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                             |
| 담당 | A                                                                                         |
| 목적 | 좌우 회전이 가능한 열린 도시(자유 이동, 건물 내부 진입) 버전으로 `world.js`를 전면 재작성 |
| 반영 | 폐기                                                                                      |

**프롬프트** — `ai-log/raw/2026-08-04.md` 20:36, 21:38, 21:47

**산출물** — `N[]` 노드 좌표·`ROADS` 도로 그래프, 건물 충돌·시야 로직(이후 완전 재작성)

**사람이 한 것**

- "그냥 직진 코스만 나오는데"라는 버그 리포트로, 갈림길 판정이 "그 순간 누르고 있던
  키"에만 의존해 대부분 기본값(직진)으로 빠지던 결함을 발견하게 함
- "위치가 자꾸 바뀌고 건물 들어갈 때 시야가 이상하다" 리포트로, 건물 충돌 판정이
  z축(또는 x축) 하나만 겹쳐도 오탐하던 버그(먼 위치로 순간이동)를 발견하게 함
- 이후(22:51) 이 방향 자체를 접고 "길 구조·이동감만 검증하는 최소 버전"으로 전환 지시
  → L-014

---

### L-014 · 2026-08-04~2026-08-05 · 코드

|      |                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                                                                                                          |
| 담당 | A                                                                                                                                                      |
| 목적 | 게임성이 아니라 "길" 구조·이동감만 검증하는 최소 버전 구현 — 보행자·건물·이벤트 제거, 유효 경로 7개 중 무작위 1개만 실제 이동 가능, 갈림길 안내 토스트 |
| 반영 | 부분 수정                                                                                                                                              |

**프롬프트** — `ai-log/raw/2026-08-04.md` 22:51(조건 7개 명시), 23:02, 23:17, 23:32, 23:38,
23:46

> 1. 플레이어 외의 모든 보행자를 제거한다. 2. 건물(B)·신호등(T)·공사장(C)은 모두
>    제거하고 일반 도로(S)로 대체한다. (…) 6. 갈림길에서는 임시 알림(UI 또는 Toast)으로
>    "← 왼쪽", "↑ 직진", "→ 오른쪽"과 같이 안내한다. 7. 길을 벗어나지 않고 START부터
>    GOAL까지 자연스럽게 이동하면 성공이다.

**산출물** — 현재 `src/world.js`의 도로망·이동·안내 토스트 대부분. 커밋 `ed6e981`

**사람이 한 것**

- 7개 조건을 직접 작성해 범위를 못박음(보행자 제거, B/T/C→일반 도로, 경로 1개만 실사용,
  갈림길 토스트 안내)
- "선택 안 된 길도 UI에 나와야 한다"고 지시해, 실제 경로와 겉모습이 같은 기만적 UI를
  요구
- `docs/image.png`에 화살표로 직접 그려서 도로 이음매(모서리) 버그를 지적
- "벽이 실제로는 안 막혀 있어야, 유저 속이는 용도"라는 발언으로 설계 의도(안내 없이는
  진짜 경로를 구분 못 하게)를 명확히 함
- "이음매 버그가 다시 생겼다"는 리포트로 `mergeStraightRoads`의 회귀(그냥 지나가는
  지점까지 교차로로 오판)를 발견하게 함

---

### L-015 · 2026-08-05 · 코드

|      |                                                                                                              |
| ---- | ------------------------------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                                                                |
| 담당 | A                                                                                                            |
| 목적 | prototype의 게이트(3통로 아치)를 재사용해 건물을 게이트+상자 형태로 되살리고, 어느 문을 통과해야 하는지 안내 |
| 반영 | 부분 수정                                                                                                    |

**프롬프트** — `ai-log/raw/2026-08-05.md` 00:03, 00:06, 00:31, 00:32

> 이제 여기에 건물을 추가해보자. `prototype/busy-man-prototype.html`에 게이트 있지?
> 일단 똑같이 설치해. 그리고 그 위에 그냥 건물처럼 보이도록 상자를 하나 올리는거야.
> 게이트랑 상자 색깔은 똑같게 해줘. (…) 다른 게이트 문은 유저 속이기용이니까 실제로
> 사용할 게이트 문이랑 UI 똑같이 해줘

**산출물** — `addGate`/`addBuildingBox` 등 건물 게이트 구현, `nearestArcLength`의
falsy-zero 버그 수정(`best === 0`을 "값 없음"으로 오판해 문 안내 토스트가 전혀 안 뜨던
원인). 커밋 `ed6e981`(같은 커밋에 포함), 이후 4개 커밋으로 분리해 커밋·push

**사람이 한 것**

- `docs/건물.png`·`docs/map/맵 시간 배분.md`를 근거 자료로 직접 지정
- "게이트/상자 색깔 똑같이", "시작·끝 둘 다 게이트" 등 구체적 스펙을 제시
- "문 안내 토스트가 안 뜬다"는 관찰로 `nearestArcLength` 버그를 발견하게 함
- "상자 사방 막기", "배치 안 맞는 건물 검토", "번호 크게" 등 후속 수정을 직접 지시
- 커밋 분할 기준과 메시지 초안을 직접 검토·승인 (4개 커밋으로 분리)

---

### L-016 · 2026-08-05 · 코드

|      |                                                                                              |
| ---- | -------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                |
| 담당 | A                                                                                            |
| 목적 | `docs/맵 시간 배분.md`의 구간별 초가 실제 플레이 시간과 일치하도록, 노드 좌표를 시간(초) 기준으로 자동 생성하는 구조로 바꾸기 |
| 반영 | 폐기                                                                                          |

**프롬프트** — `ai-log/raw/2026-08-05.md` 15:25(요구사항 6개 명시), 16:03/16:04, 16:17, 16:35

> "시간(초)"을 기준으로 월드 거리를 생성하는 구조로 변경합니다. 즉, distance = PLAYER_SPEED
> × duration 을 기본 원칙으로 합니다. (…) Node 좌표를 하드코딩한 뒤 시간을 맞추는 방식이
> 아니라, 경로를 따라 누적 거리를 계산하면서 Node 위치를 생성하는 구조로 변경해 주세요.

**산출물** — `durationToDistance`/`walkRoute`/`computeLayout`(장식용 노드를 그래프 위상만
보고 BFS로 배치) 등. 커밋 전에 되돌려져 저장소에 남지 않음(반영 커밋: _없음_)

건물 두 이웃이 그래프의 서로 다른 갈래에서 각자 좌표를 받으며 "길에 안 맞는 건물"·"보이는데
뚫고 지나가지는 벽" 버그가 반복됐다. 원인을 좁혀 여러 차례 고쳤지만(좌표 중복 제거,
건물 두 이웃을 한 번에 정착시키는 로직 등) 근본적으로 7개 유효 경로가 그래프상 순환을
이루고 있어 하나의 정적 2D 배치로 전부를 동시에 정확히 맞추는 것이 불가능하다는 결론에
이르렀다.

**사람이 한 것**

- "b3 -> B7을 가는 길에 벽이 없는 부분", "b6 배치가 이상해"라는 관찰로 버그를 재현하게 함
- "아직 이상해. b3도 이상해졌어"라는 재현으로, 앞선 수정이 다른 곳에서 같은 종류의 문제를
  다시 일으켰음을 지적
- "내가 말한 부분만 보지 말고 맵 전체적으로 보고 수정해야될 것 같아"라고 요구해 국소
  패치가 아니라 전체 스캔(장식 도로가 실제 경로와 겹치는 경우 전수 조사)을 이끌어냄
- 최종적으로 "문제가 계속 해결되지 않아 이전 상태로 되돌렸습니다"라고 선언하며 접근
  자체를 폐기하고 방향을 전환(→ L-017)

---

### L-017 · 2026-08-05 · 코드

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                      |
| 담당 | A                                                                                  |
| 목적 | 맵 구조(노드·갈림길·연결)는 절대 바꾸지 않고, 구간 길이만 조정해 전체 이동 시간을 약 70초로 맞추기 |
| 반영 | 그대로                                                                            |

**프롬프트** — `ai-log/raw/2026-08-05.md` 17:05, 17:07

> 맵 구조는 절대 변경하지 말고, 길이(거리)만 조정해 주세요. (…) 3. 건물(B)은 내부를
> 통과하는 공간이므로 최소 5초 이상 머무를 수 있는 길이로 만들어 주세요. (…) 5. 플레이어
> 속도는 그대로 유지하고, 속도를 변경하지 말고 거리만 변경해 주세요.

**산출물** — `MAP_SCALE`(원본 노드 좌표에 2.1배 균등 확대), `BUILDING_DWELL_SEC`(5.5초 균일
체류), `durationToDistance` 유지. 커밋 `6dfb9f0`

L-016의 자동 좌표 생성이 순환 문제로 막힌 뒤, 손으로 그린(그리고 이미 정상 렌더링되는)
노드 좌표 배치는 그대로 두고 **배율만** 곱하는 훨씬 단순한 방식을 선택했다. 구조·모양은
그대로 두고 거리만 늘리므로 이전 방식의 버그 계열이 원천적으로 재발하지 않는다. 자주
나오는 5개 경로가 68~72초에 들어오고, 홉이 가장 적은 1개 경로는 55.5초로 구조상 짧다는
점을 실측으로 확인해 보고했다.

**사람이 한 것**

- "맵 구조는 절대 변경하지 말고 길이만"이라는 제약을 명시해 접근 자체를 좁힘
- 건물 체류 최소 5초, 전체 약 70초라는 구체적 수치 목표를 직접 제시
- "이후 신호등(T), 공사장(C) 이벤트를 추가할 예정이므로 구간별 목표 시간을 기준으로
  계산되는 구조를 유지"하라고 요구해 `durationToDistance` 재사용을 못박음

---

### L-018 · 2026-08-05 · 코드

|      |                                                                             |
| ---- | --------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                               |
| 담당 | A                                                                           |
| 목적 | B7('ㅗ' 건물) 천장이 지나가는 두 갈래만 덮이던 것을 세 갈래 전체로 확장하고, T·C를 이벤트 없이 색으로만 구분 |
| 반영 | 그대로                                                                       |

**프롬프트** — `ai-log/raw/2026-08-05.md` 17:25

> B7은 현재 맵 구조상 'ㅗ' 형태의 건물입니다. (…) 천장이 건물 전체를 덮도록 'ㅗ' 형태
> 그대로 이어지게 수정해 주세요. (…) 신호등(T)과 공사장(C)은 아직 이벤트를 구현하지
> 않을 예정입니다. (…) T와 C 타입은 그대로 유지해 주세요. (S로 변경하지 말 것)

**산출물** — `buildB7`(T4·J3·C3 세 갈래를 각각 독립 통로로 세움), T/C 전용 바닥·벽 재질,
`ZONE_SEC`(구간 최소 2초) 기반 `expandZones`. 커밋 `6dfb9f0`(같은 커밋에 포함)

**사람이 한 것**

- "지나가는 경로만 덮는 게 아니라 건물의 전체 형태를 유지하는 천장이 필요"하다고
  요구해, 플레이어가 실제로 걷는 두 갈래만 렌더링하던 기존 범위를 넓힘
- "이동 방식은 일반 도로(S)와 동일", "타입은 그대로 유지(S로 변경하지 말 것)"이라고
  못박아, 지금 단계가 이벤트 구현이 아니라 맵·길 형태 검증임을 명확히 함
- T/C 구간 최소 길이(2초 이상)를 직접 지정

---

### L-019 · 2026-08-05 · 코드

|      |                                                                  |
| ---- | ---------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                    |
| 담당 | A                                                                |
| 목적 | `prototype/busy-man-prototype.html`의 행인을 맵에 배치            |
| 반영 | 골격만 차용 (L-020에서 배치 방식이 경로 전용 → 맵 전체로 재작성됨) |

**프롬프트** — `ai-log/raw/2026-08-05.md` 17:50

> 이제 여기에 prototype/busy-man-prototype.html 처럼 행인을 배치해줘

**산출물** — `createPedestrians`/`stepPedestrians` 초기 버전(실제로 걷는 경로 위에만 배치),
prototype의 속도·간격·충돌 판정(원이 아니라 진행방향·좌우 상자) 상수를 그대로 이식.
커밋 `6dfb9f0`(같은 커밋에 포함, 이후 L-020에서 배치 로직이 재작성됨)

**사람이 한 것**

- prototype 파일을 직접 참조로 지정해, 속도·간격·충돌 판정 방식을 새로 설계하지 않고
  이미 검증된 값을 그대로 가져오게 함

---

### L-020 · 2026-08-05 · 코드

|      |                                                                                                             |
| ---- | ------------------------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                                  |
| 담당 | A                                                                                                              |
| 목적 | 건물 게이트를 평균 3초 간격으로 늘리고 B7 중앙 회전공간은 비우기, 행인을 맵 전체 도로망에 고르게 재배치, 도착지 연출과 다시 시작 |
| 반영 | 부분 수정                                                                                                       |

**프롬프트** — `ai-log/raw/2026-08-05.md` 21:21 (건물·행인·도착지 절)

> 1. 건물 내부에 게이트를 추가로 배치해 주세요. 게이트는 평균적으로 3초 이동마다 1개
> 정도의 간격으로 배치해 주세요. (…) 2. B7은 'ㅗ' 형태의 건물입니다. 좌/우회전하는 중앙의
> 'ㅁ' 공간에는 게이트를 배치하지 말아 주세요. (…) 플레이어가 실제로 지나가는 경로뿐만
> 아니라, 지나가지 않는 다른 도로에도 행인을 배치해 주세요. (…) 행인 수는 현재가 적당하므로
> 유지해 주세요. (…) GOAL에 도착하면 '다시 시작' 버튼을 표시해 주세요.

**산출물** — `addGatesAlongChain`(체류시간/GATE_INTERVAL_SEC로 게이트 개수 산출), B7 중앙
`centerClear` 반경, `createPedestrians`를 맵 전체 논리 도로 순회로 재작성, `buildGoalMarker`,
`world.reset()`. 커밋 `6dfb9f0`

**사람이 한 것**

- "행인 수는 현재가 적당하므로 유지"라고 명시해 밀도 변경을 차단 — 배치 **위치**만
  바꾸는 것으로 범위를 한정
- B7 중앙 회전공간에는 게이트를 두지 말라는 요구로, 좌/우회전이 일어나는 자리에
  장애물성 오브젝트를 두면 안 된다는 제약을 명확히 함
- `PED_LANE_HALF`(3.2) 등 행인 배치 상수 일부를 직접 손으로 조정함(확인됨) — 에이전트가
  마지막으로 적어 둔 값과 달라져 있었음
- 게이트 개수를 "3초마다 1개"라는 구체적 밀도로 지정해 `GATE_INTERVAL_SEC` 도입을 이끎

---

### L-021 · 2026-08-05 · 코드

|      |                                                                    |
| ---- | -------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                        |
| 담당 | A                                                                    |
| 목적 | 클리어까지 걸린 시간을 실시간 표시하고, 클리어 시 최종 시간과 다시 시작 버튼을 보여주기 |
| 반영 | 그대로                                                                |

**프롬프트** — `ai-log/raw/2026-08-05.md` 21:21 (플레이 시간 표시 절)

> 게임 시작과 동시에 타이머를 시작합니다. (…) GOAL에 도착하면 타이머를 정지합니다. (…)
> 문제(퀴즈)가 열려 게임이 일시정지되는 동안에는 타이머도 함께 멈추도록 구현해 주세요.
> (…) 향후 기록 저장이나 최고 기록 기능을 추가할 수 있도록, 클리어 시간을 별도의
> 상태(state)로 관리해 주세요.

**산출물** — `src/main.js`에 `elapsed`/`clearTime` 상태, `isPaused()` 게이트, HUD·결과 카드·
다시 시작 버튼. 커밋 `c48253e`

**사람이 한 것**

- 퀴즈가 아직 없는데도 "일시정지 시 타이머도 멈추도록" 요구해, `isPaused()`를 한 줄만
  바꾸면 되는 게이트 함수로 미리 분리하게 함
- "클리어 시간을 별도의 상태로 관리"하라고 요구해, 진행 중인 `elapsed`와 확정된
  `clearTime`을 분리하게 함(최고 기록 확장을 염두에 둔 결정)
- 시간 표시 형식(`MM:SS` 또는 `SS.s`)을 직접 지정

---

### L-022 · 2026-08-05 · 코드

|      |                                                                     |
| ---- | --------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                         |
| 담당 | A                                                                     |
| 목적 | 공사장(C)에 우회 가능한 바리게이트를, 신호등(T)에 안전 차선·갇힘이 있는 횡단보도 군중을 추가 |
| 반영 | 그대로                                                                 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 22:58

> 바리게이트는 플레이어가 공사장에 도착하기 약 2초 전 바닥에서 올라오는 애니메이션으로
> 등장합니다. (…) 플레이어가 행인과 충돌하기 약 0.5초 전 네비게이션이 안내하는 차선을
> 제외한 나머지 차선으로 행인들이 이동합니다. (…) 플레이어가 다른 차선으로 진입하면
> 행인들 사이에 갇힙니다. (…) 약 6초 동안 (…) 좌우 이동 및 좌/우회전 입력은
> 비활성화됩니다. (…) 공사장의 바리게이트 위치와 신호등의 안전 차선은 해당 이벤트
> 구역에 진입하기 전에 한 번만 랜덤으로 결정하며, 통과를 완료할 때까지 변경되지 않습니다.

**산출물** — 차선 판정 공통 인프라(`LANE_X` 등), `createEvents`(구역 진입 전 한 번만
바리게이트 차선/안전 차선 결정), `stepEventControl`(진입 차선으로 통과/갇힘을 그 자리에서
확정), `stepEventVisual`(바리게이트 상승·차단, 군중 이동·안전 차선 열기), 신호등 바닥
횡단보도 재질. 커밋 `0454011`

**사람이 한 것**

- 타이밍 수치(바리게이트 2초 전, 신호등 0.5초 전, 정상 통과 4초, 갇힘 6초)를 전부
  직접 제시
- "해당 이벤트 구역에 진입하기 전에 한 번만 랜덤으로 결정하며, 통과를 완료할 때까지
  변경되지 않습니다"라고 못박아, 매 프레임 재판정하는 구현을 배제
- 갇혔을 때의 정확한 제약(좌우 이동·좌/우회전 비활성화, 행인과 함께 느리게 이동)을
  구체적으로 지정

---

### L-023 · 2026-08-05 · 문서

|      |                                                                          |
| ---- | -------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5) — `/ai-log` 스킬 수행 중 발견                 |
| 담당 | A                                                                          |
| 목적 | `docs/ai_usage_log.md` §4 출처 표에 이전 병합(9faa2b3)에서 남은 충돌 마커를 정리 |
| 반영 | 그대로                                                                     |

**프롬프트** — 별도 프롬프트 없음. "ai-log하고 커밋 진행해줘" 요청에 따라 `/ai-log`
스킬의 1단계(가이드라인·로그 읽기)를 수행하던 중 에이전트가 직접 발견함

**산출물** — `<<<<<<<`/`=======`/`>>>>>>>` 마커 제거, 두 브랜치가 각자 추가한 행
(`vendor/three.module.js`, Gemini 이미지)을 모두 유지. 커밋 `8b29583`

**사람이 한 것**

- 두 행 모두 유지하는 처리 방식과, 이 정리를 로그에 남길지 여부를 확인·승인

### L-024 · 2026-08-06 · 코드

|      |                                                                                              |
| ---- | -------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                |
| 담당 | A                                                                                            |
| 목적 | 행인 생성 금지 구간을 건물 전체 차단에서 게이트의 경로 진행 거리(s) 기준으로 바꿔, 건물 내부에도 행인이 자연스럽게 채워지게 함 |
| 반영 | 그대로                                                                                        |

> 이날 훅 미승인으로 `UserPromptSubmit`이 동작하지 않아 원문이 자동 포획되지 않았다.
> 아래 프롬프트는 같은 세션의 대화 기록에서 수동 복원한 것이며, 본문은 원문 그대로이되
> 시각은 커밋 시각을 기준으로 앞뒤 순서만 맞춘 추정치다(`ai-log/raw/2026-08-06.md` 머리말 참조).

**프롬프트** — `ai-log/raw/2026-08-06.md` 11:00

> 현재 행인은 건물을 기준으로 생성을 제한하고 있는데, 건물 기준 생성 제한은 모두
> 제거해 주세요. 대신 **게이트를 기준으로만** 행인이 생성되지 않도록 변경해 주세요.
> 월드 좌표(x, z)가 아니라 현재 프로젝트에서 사용하는 **경로 진행 거리(s)** 를 기준으로
> 구현해 주세요. (…) 이후 신호등(T), 공사장(C) 등 다른 이벤트에도 동일한 방식의
> "진행 거리(s) 기반 생성 금지 구간"을 쉽게 추가할 수 있도록 확장성을 고려해 주세요.

**산출물** — `pedRoads`(게이트 등록과 행인 생성이 공유하는 도로 객체), `addSpawnBlock`/
`isSpawnBlocked`(게이트 s ± `durationToDistance(1)` 비교), 건물 전체 차단 로직 제거.
커밋 `3f36d52`

**사람이 한 것**

- 건물 통째 차단 대신 게이트만 기준으로 삼도록 명시적으로 지시하고, 월드 좌표가 아니라
  경로 진행 거리(s) 기준으로 구현하라고 판단 기준 자체를 지정
- 금지 범위를 "약 1초 이동 거리"라는 구체적 수치로 제시
- 신호등·공사장 등 후속 이벤트도 같은 함수로 확장 가능해야 한다는 설계 제약을 미리 요구

---

### L-025 · 2026-08-06 · 코드

|      |                                                                                              |
| ---- | -------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                |
| 담당 | A                                                                                            |
| 목적 | 신호등(T) 구역의 행인 배치와 차선 판정 시점이 부자연스럽다는 체감 피드백을 여러 차례에 걸쳐 반영해 다듬음 |
| 반영 | 그대로                                                                                        |

> L-024와 같은 사유로 원문은 대화 기록에서 수동 복원됨. `ai-log/raw/2026-08-06.md` 참조.

**프롬프트** — `ai-log/raw/2026-08-06.md` 11:35, 11:55, 12:10, 12:25, 12:40, 12:55, 13:05, 13:15
(8개 요청, 한 커밋으로 반영)

> 1. 현재 신호등 구역의 행인은 세로 일렬로 배치되어 있는데, 이를 **신호등 시작 지점에
> 가로 2줄**로 배치해 주세요. 2. 플레이어가 신호등에 가까워지면, 행인들은 **네비게이션이
> 안내하는 길을 비워두고 나머지 두 차선으로 퍼지는 형태**로 이동합니다. (…) 3. 플레이어가
> **잘못된 차선**으로 진입한 경우에는 행인들 사이에 갇혀 행인들과 함께 이동합니다. (…)
> 이 동안에는 좌우 이동 및 방향 전환이 불가능합니다. (11:35)

**산출물** — 군중을 시각 구역 경계(`zoneStartS`)에 가로 2줄로 배치, 안전 차선을 비울 때
나머지 두 차선에 분산 이동, `TL_JUDGE_DELAY_SEC`(0.75초)만큼 판정 시점을 진입선보다
뒤로 이동, 판정 후(성공·실패 모두) 군중이 플레이어와 같은 속도로 동행, 잘못된 차선
진입 시 시야를 즉시 정면으로 고정 + sticky 토스트, 한 자리에 여러 명을 겹쳐 밀도 증가.
`src/world.js` 144줄 변경. 커밋 `cfdd207`

**사람이 한 것**

- 세로 배치 → 네비 안내 차선만 비우는 가로 2줄 배치로 바꾸도록 최초 스펙을 직접 제시 (11:35)
- 잘못된 차선 진입 시 "행인과 함께 이동"하는 시각 효과와, 밀도를 눈에 띄게 늘리라는
  구체적 요구 (11:55)
- 마지막 줄 위치·침범 허용 범위 등 배치 세부 규칙과, 잘못된 길 진입 시 시야를 정면으로
  되돌리라는 요구 (12:10)
- "행인이 신호등 구역 안쪽에 생성된다"는 관찰로 배치 기준을 `startS` 기준으로
  재조정하게 하고, 줄 간격 수치까지 직접 지정 (12:25)
- "군중이 노란 벽 안에 있어야 하는데 일반 도로를 너무 많이 차지한다"는 관찰로, 갇힘
  판정 구간(`TL_HALF_SEC`)과 시각 구역(`ZONE_SEC`)이 서로 다른 폭으로 어긋나 있던
  문제를 드러나게 함 (12:40)
- "판정이 진입 직후라 너무 빠르게 느껴진다"는 체감 리포트로 판정 시점을 0.5~1초
  뒤로 늦추도록 요구하되, 군중이 갈라지는 애니메이션 시점은 그대로 두라고 범위를
  한정 (12:55)
- 옳은 차선 진입 시 옆 차선 행인도 같이 전진시키라는 추가 요구 (13:05)
- 이 세션과 별개로 직접 조정한 수치(바리게이트 노출 타이밍 등)도 같은 커밋에
  포함하되 커밋 메시지에는 상세히 언급하지 않아도 된다고 지시 (13:15) — 그중
  `BARR_LEAD_SEC`(2초→1초)와 `TL_OPEN_LEAD_SEC`(0.5초→1초)는 이 세션 도중
  AI 프롬프트 없이 직접 코드를 고쳐 별도 커밋 `2e050f0`(`chore(world)`)으로
  먼저 남긴 것이었다

---

### L-026 · 2026-08-06 · 레벨·밸런싱

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                      |
| 담당 | A                                                                                  |
| 목적 | 맵 시간 배분 문서를 실측(현재 world.js 좌표) 기준으로 다시 쓰고, 채택 밴드를 60~81초로 좁힘 |
| 반영 | 그대로                                                                              |

**프롬프트** — `ai-log/raw/2026-08-06.md` 14:04, 14:29

> 지금 맵 구조 분석해서 `docs/map/맵 시간 배분.md` 의 14개의 루트가 각각 가지는 총
> 플레이 시간 알려줘.(감속이 한 번도 일어나지 않았다고 가정하고) (14:04)
>
> 지금 시간초 기준으로 `docs/map/맵 시간 배분.md` 다시 작성해줘. 60~81초 사이의
> 경로만 사용하는 걸로 바꿀거야. 사용할 경로들은 `docs/map/맵 구조.jpg`에 나와있는
> 라벨 그대로 정리해주고, 라벨별 시간도 정리해줘. 이것도 감속이 없다는 가정하에 (14:29)

**산출물** — `docs/map/맵 시간 배분.md` 전면 재작성. 커밋 `a475705`

L-011(8/4)이 만든 문서는 이후 좌표 방식이 통째로 바뀌면서(L-016 폐기, L-017
MAP_SCALE) 더 이상 실제 게임과 안 맞았다. "시간을 먼저 정하고 좌표를 맞추는" 첫
판 방식 대신, 14개 조합의 실제 이동 시간을 world.js 좌표(N_RAW × MAP_SCALE ÷
SPEED)로 역산하는 방식으로 전환했다 — 최단 경로가 8/5 실측 보고(55.5초)와 정확히
일치해 검증됨.

**사람이 한 것**

- 문서가 낡았다는 걸 알아채고 "지금 맵 구조 분석해서" 실측을 먼저 요구 — 배정이
  아니라 역산으로 순서를 뒤집게 함
- 채택 기준을 90~100초에서 60~81초로 직접 좁힘
- 라벨 표기를 `docs/map/맵 구조.jpg`(원본 손그림) 그대로 쓰라고 지정해 문서와
  그림이 다시 맞물리게 함

---

### L-027 · 2026-08-06 · 코드

|      |                                                    |
| ---- | -------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                      |
| 담당 | A                                                  |
| 목적 | `world.js`의 `ROUTES`를 L-026에서 정한 60~81초 밴드 9개로 교체 |
| 반영 | 그대로                                              |

**프롬프트** — `ai-log/raw/2026-08-06.md` 15:36

> ROUTES 다시 써줘

**산출물** — 기존 7개 중 6개 재사용, "중간 직진→END3"(55.5초 급행) 제외, B4 우회
2종·하단(B1)→END3 3개 신규 추가. 커밋 `4011d94`

**사람이 한 것**

- L-026에서 이미 정한 9개 목록을 코드에 그대로 반영하도록 짧게 승인·확정

---

### L-028 · 2026-08-06 · 코드

|      |                                                                          |
| ---- | ------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                            |
| 담당 | A                                                                        |
| 목적 | 방향이 안 꺾이는 진짜 갈림길(차수 3 이상)에서도 "직진" 안내를 띄우기 |
| 반영 | 그대로                                                                    |

**프롬프트** — `ai-log/raw/2026-08-06.md` 16:12, 16:21, 16:25

> 현재는 출발할 때를 제외하곤 갈림길에서 '직진' 알림을 보내주는 경우가 없는데
> 보내줘야 해. 직진도 알림이 가야 유저가 좌우 갈림길이 있는 구간에서 헷갈려
> 할거야. (16:12)

**산출물** — `ROADS`에서 노드별 차수(`NODE_DEGREE`)를 계산해, 방향이 안 꺾여도
차수 3 이상인 노드면 "↑ 직진" 안내를 추가하는 `computeTurnHints` 확장. 커밋
`df2864e`

한 번에 안 끝난 작업이다. 16:12에 구현을 마쳤는데, 16:21에 "작업하는 동안 손대서
저장이 안 된다"는 리포트가 들어와 확인해보니 사용자 에디터가 예전 버전(직진
안내가 없는)을 덮어써 변경분이 사라져 있었다. 16:25 "코드 다시 채워줘" 지시로
같은 변경을 재적용 — 그 사이 사용자가 파일 상단 TODO 체크리스트에 표시해 둔 것은
보존하고 내 변경만 다시 넣어 커밋함.

**사람이 한 것**

- 직진 안내가 빠져 있다는 것을 직접 지적하고 이유(좌우 갈림길에서 헷갈림)까지 제시
- 에디터 충돌로 코드가 사라진 것을 발견해 리포트 (16:21)
- 재작업을 지시하되, 그 사이 스스로 체크리스트에 표시해 둔 것(루트셋 확장
  검토·루트별 시간 계산 완료)은 유지되게 함

---

### L-029 · 2026-08-06 · 기획

|      |                                                                             |
| ---- | --------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                               |
| 담당 | A                                                                           |
| 목적 | "휴대폰 이벤트 알림 큐" 기능을 지금 파일 구조에 얹으려면 뭘 먼저 풀어야 하는지 검토(코드 작성 전) |
| 반영 | 부분 수정                                                                    |

**프롬프트** — `ai-log/raw/2026-08-06.md` 16:41(스펙 전문), 17:05, 17:12, 17:14, 17:17

> 현재 파일 구조에서 아래 내용을 구현하려면 고려해야하는 부분이 있는지
> 검토해줘 (…) 플레이어가 잘못된 길을 선택하면 3초 전 위치로 되돌아갑니다. (…)
> 이벤트를 한 번 소비해 버리는 방식이 아니라 큐의 맨 앞 이벤트를 기준으로
> 관리하는 구조로 구현해 주세요. (…) 공사장, 신호등, 건물 게이트는 랜덤 요소가
> 있습니다. 이 랜덤은 게임 시작 시 단 한 번만 수행해 주세요. (16:41 — 전문은
> raw 참조)
>
> 갈림길 직진도 잊지 말기! 그리고 `src/phone-a.js` `src/quiz-a.js` 참고해. 더
> 필요한 거 있으면 말하고. 코드는 아직 짜지 말고 저 정보가 있으면 어떻게
> 되는지 알려줘 (17:05)

**산출물** — 코드 없음(검토 결과만 대화로 전달). 핵심 발견 두 가지:

- `phone-a.js`에 이미 `pushMap(laneIndex)`가 A와의 합의를 기다리며 미리 세워져
  있었다 — `state.js`를 안 늘려도 되는 기존 통로
- 지금 "잘못된 길"은 물리적으로 선택 불가능하다(선택된 경로 하나만 실제로 걸을
  수 있고, 나머지는 장식) — 갈림길에 되돌리기를 적용하려면 장식 갈림길을 실제로
  걸을 수 있게 만들어야 함

**사람이 한 것**

- AI가 처음 제시한 판단 두 가지를 직접 정정: 신호등·공사장은 "뒤로 보내기"
  대상에서 뺌("내가 잘 못 생각해서"), "3초 전"의 의미를 위치 스냅이 아니라 거리
  환산(`durationToDistance(3)`)으로 명확히 정정
- 장식 갈림길을 실제로 걷게 만드는 큰 스코프 쪽을 선택(반대쪽은 "입력 기준으로만
  판정"하는 더 쉬운 대안이었는데, "선택이 이름 그대로 진짜 선택이 되게" 하려고
  일부러 어려운 쪽을 고름)
- `phone-a.js`/`quiz-a.js`/`content/day1-a.json`을 직접 참고 자료로 지정해,
  `pushMap` 인터페이스를 새로 설계하지 않고 이미 있는 것을 재사용하게 함
- 게이트 안내 문구("환승 통로 — {lane}")를 신호등/공사장과도 같이 써도 되는지
  17:17에 확인해줘 설계를 완성시킴

---

### L-030 · 2026-08-06 · 코드

|      |                                                                          |
| ---- | -------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                              |
| 담당 | A                                                                          |
| 목적 | 건물 게이트(진입·중간·진출 전부)를 실제 판정 대상으로 만들고, 틀리면 되돌리기 |
| 반영 | 부분 수정                                                                  |

**프롬프트** — `ai-log/raw/2026-08-06.md` 17:18, 17:35, 17:59

> 4번도 잘못들어가면 뒤로 보내기 해야돼. 이거 기억해두고 1번부터 하자 (17:18)
>
> 3초 너무 길어서 내가 2초로 바꿨어. 그리고 지금 건물 들어가는 게이트만
> 동작하는데 건물 안에 있는 게이트, 나가는 게이트 전부다 판정 대상이야. 그래서
> 네비도 띄워줘야돼 (17:35)

**산출물** — `computeChainGatePoints`/`computeB7GatePoints`로 렌더링·판정이 같은
좌표를 보게 통일, 건물당 게이트 전부(3개, B7은 2개)를 독립 이벤트로 판정, 틀린
문 통과 시 되돌리기. `src/world.js` 178줄 변경. 커밋 `5087e6a`

**사람이 한 것**

- L-029에서 미룬 4번(갈림길 되돌리기)을 잊지 말라고 못박고, 착수 순서를
  1번(게이트)부터로 확정
- **`WRONG_CHOICE_REWIND` 값(3초→2초)을 AI 프롬프트 없이 직접 코드에서 수정**
- "진입 게이트 하나만 판정되는데 전부 판정돼야 한다"고 범위를 넓히고, 안내도
  게이트마다 다시 뜨게 요구
- 검증 중 드러난 "실패 후 재안내가 가끔 안 뜨는" 제약을 알고도 "큐 쓰면
  괜찮아질 것"이라며 수용, 우선 커밋을 지시
- **직접 코드를 고쳐 건물 하나당 문 하나였던 것을 게이트마다 독립 랜덤으로
  바꿈** — AI가 짠 "건물당 문 하나 유지" 설계를 뒤집은 것(원문 없음, git diff로
  확인)

---

### L-031 · 2026-08-06 · 코드

|      |                                                                                   |
| ---- | ----------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                       |
| 담당 | A                                                                                   |
| 목적 | 회전·게이트·경고 안내를 큐 하나로 합쳐, 뒤 항목이 앞이 안 끝났는데 먼저 안내되는 문제를 구조적으로 없앰 |
| 반영 | 그대로                                                                               |

**프롬프트** — `ai-log/raw/2026-08-06.md` 18:02

> 응, 2번 진행해줘

**산출물** — `hints`(`nextHint` 인덱스 방식)와 `gateEvents`(매 프레임 전수 순회
방식) 둘을 `queue` 하나로 통합. `update()`는 이제 큐 맨 앞(가장 가까운 미해결
항목) 하나만 본다. `src/world.js` 59줄 변경. 커밋 `70632a3`

L-030에서 남겨둔 "실패 후 재안내가 가끔 안 뜨는" 제약이 이 구조 변경으로 자연히
해소됨 — 뒤 항목은 앞이 `resolved`되기 전엔 절대 안내되지 않으므로.

**사람이 한 것**

- "2번 진행해줘"라는 짧은 승인으로 L-029에서 이미 합의된 설계(큐 구조화 2단계)를
  그대로 진행시킴

---

### L-032 · 2026-08-06 · 코드

|      |                                                                        |
| ---- | ------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                            |
| 담당 | A                                                                        |
| 목적 | `main.js`에 `phone-a.js`/`quiz-a.js`를 마운트하고, world의 안내가 `phone.pushMap`으로 흘러가게 배선 |
| 반영 | 그대로                                                                    |

**프롬프트** — `ai-log/raw/2026-08-06.md` 18:13, 18:44

> 응 (18:13 — 3번 진행 승인)
>
> 커밋은 a빼고 해주고, 커밋 후에 다시 돌렬줘 (18:44)

**산출물** — `main.js`가 `phone-a.js`/`quiz-a.js`/`content/day1-a.json`을 직접
import·fetch, `createWorld(container, {onNavHint})`로 안내 이벤트를 phone에
전달, Space 키(누르는 동안 정면 보기) 라우팅 추가, `isPaused()`를
`state.quizOpen`에 연결. `src/main.js` 42줄·`src/world.js` 13줄 변경. 커밋
`613e373`

정식 파일명(`phone.js`/`quiz.js`/`day1.json`)이 아직 빈 스텁이라 `-a` 접미사
파일을 대신 참조하는 절충은 AskUserQuestion으로 먼저 확인받고 진행함(대화만,
커밋 없음).

**사람이 한 것**

- import 대상(`phone-a.js` vs 정식 스텁 대기)과 fetch 대상(`day1-a.json` vs 빈
  `day1.json`)을 직접 선택
- **커밋 범위에서 `-a` 파일(아직 git에 안 잡힌 B 작업물)을 직접 제외**하도록
  지시 — 같이 커밋하면 B가 나중에 정식 이름으로 옮길 때 혼선이 생길 수 있다는
  판단으로 보임
- 커밋 후 재검증(다시 돌려서 확인)을 요구해, 커밋된 상태 자체가 실제로
  동작하는지 확인하게 함

---

### L-033 · 2026-08-06 · 코드

|      |                                                                     |
| ---- | --------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                         |
| 담당 | A                                                                     |
| 목적 | 폰에 뜨는 신호등·공사장 안내가 "undefined"로 표시되는 버그 원인 파악 및 수정 |
| 반영 | 그대로                                                                 |

**프롬프트** — `ai-log/raw/2026-08-06.md` 20:38

> 폰에 공사장, 신호등 네비가 undefined로 뜨는데 찾아서 고쳐줘

**산출물** — `phone-a.js`의 `pushMap(event)`이 `event.kind`별로
`event.lane`/`event.door`를 읽어 문장을 조립하는데(B 쪽에서 turn/gate 이후 이
방식으로 바뀜), 공사장·신호등 `navHints`에는 `lane` 필드가 없어 `undefined`가
그대로 찍히던 것을 발견. C 이벤트엔 막힌 차선, T 이벤트엔 안전 차선을 `lane`으로
채워 넣음. `src/main.js`·`src/world.js` 총 70줄 변경. 커밋 `589633a`

이 버그는 L-032 커밋 이후, 세션 사이에 사람이 `pushMap`을 laneIndex 단일값
방식에서 `event` 객체 + `kind` 스위치 방식으로 직접 바꾸면서 생겼다
(`main.js`/`world.js`의 `onNavHint(front)` 변경, `phone-a.js`의 `pushMap(event)`
스위치문 추가) — AI 프롬프트 기록은 없다(원문 미확보, git diff로만 확인).

**사람이 한 것**

- **`pushMap`을 laneIndex 단일값에서 `event` 객체 + `kind` 스위치 방식으로 직접
  리팩터링** — `main.js`/`world.js`/`phone-a.js` 세 파일에 걸쳐 AI 프롬프트 없이
  직접 코드를 고침
- 버그 증상("undefined로 뜬다")만 보고하고 원인 진단과 수정은 위임
- 죽은 코드(`front.kind !== "warn"` 조건)와 낡은 주석 정리는 AI가 알아서 하도록
  둠

---

### L-034 · 2026-08-04 · 도구·자동화

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 자정을 넘긴 턴에서 반영 커밋이 비는 것을 고쳐 원문과 커밋의 연결이 끊기지 않게 함 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 00:27, (g)

> 훅 자체는 뜨는데?

**산출물** — 커밋 `6cf5b38`

`UserPromptSubmit`이 원문을 쓴 날짜 파일과 `Stop`이 커밋을 채우러 가는 날짜 파일이
자정을 걸치면 달라진다. 그러면 원문은 어제 파일에 있는데 커밋은 오늘 파일을 뒤지므로
**아무 말 없이 `반영 커밋: _없음_`으로 남는다.** 실제로 8/3→8/4 전환에서 발생했다.

**사람이 한 것**

- **증상만 보고 원인 규정을 하지 않음** — "훅 자체는 뜨는데"로 범위를 좁혀 줌.
  훅이 안 도는 것과 훅이 돌고도 값이 비는 것은 원인이 전혀 다르다
- 대응 방식으로 **"지금 고침 — 새 브랜치 + PR"** 선택. 로그 정리와 섞지 않았다

---

### L-035 · 2026-08-04 · 문서

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | 공동 |
| 목적 | 커밋·PR 본문이 사람마다 다른 모양으로 쌓이는 것을 막고, 해시를 갈아끼우는 조작을 금지 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 12:00, 12:15, 12:29, 13:03

> - pr-commit 스킬 수정: 변경 사항 사용자 확인 후 수행할 것.
> - 커밋, PR 본문 개선: 현재는 가독성이 떨어지고 verbose함. 커밋 본문도 간결한 (개요 + 리스트)화. 작업 전 시안 제시

**산출물** — 커밋 `fa58ab4` `71495c5` `e03ba5f`

- `AGENTS.md` §5에 **개요 한두 줄 + 리스트** 형식과 인용 시 파일명 병기 규칙
- `pr-commit` 스킬에 **실행 전 사용자 확인 게이트** 추가 — "커밋해줘"가 메시지 승인을 대신하지 않는다
- **rebase 금지 명문화.** `71495c5`가 그 필요를 증명한다 — 8/4에 PR #5를 머지 직전
  rebase해서 로그의 커밋 해시 5개가 죽어 있었고, 그것을 현재 해시로 교체한 커밋이다

**사람이 한 것**

- 기존 커밋·PR 본문이 **verbose하고 가독성이 떨어진다고 지적**하고 형식을 지정
- **`--force-with-lease` 예외를 제안** — 브랜치 정리 말고 충돌 해결에도 rebase를 쓸 것 같다는
  실무 근거였다. 확인 결과 **lease가 막는 것은 남의 push를 덮어쓰는 사고이지 해시 교체가
  아니라서** 예외가 성립하지 않았고, `git merge main`으로 대체됨
- 스킬이 사용자 확인 없이 커밋하던 것을 문제로 지목

---

### L-036 · 2026-08-04 · 기획

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview + Planner/Architect/Critic 합의 |
| 담당 | B |
| 목적 | `phone.js`·`quiz.js` 착수 전에 미정 기획을 걸러내고, 소유 경계를 침범하지 않는 구현 계획을 확정 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 16:35, 17:00, 17:21, 17:24, 17:27, 18:25, 20:21

> /deep-interview @src/quiz.js , @src/phone.js 개발. @docs/기획 초안.md 기획 초안 중 아직 미정 사항이 있음. 결정된(혹은 변경이 있어도 수정이 적은) 부분들만 우선 구현.

**산출물** — `docs/ai-log/specs/2026-08-04-phone-quiz-spec.md` (7라운드, 모호도 19%)
및 `docs/ai-log/specs/2026-08-04-phone-quiz-plan.md`

계획이 **Architect와 Critic을 거치며 크게 고쳐졌다.** 그 과정 자체가 검증 기록이다.

- Architect **REVISE** — 블로킹 5건. 그중 (B1) `AGENTS.md` §2의 A/B 라벨이 §1과 정반대인데
  계획이 그것을 정본으로 인용하고 있었다는 지적이 가장 컸다. 이 지적이 `e7ed1ee`(L-037)로 이어진다
- Architect가 제안한 **내부 rAF 종합안을 Critic이 기각** — 결함 지적은 실재하나 처방이
  결정을 뒤집을 뿐 아니라 규약을 깬다는 판정
- Critic이 **"대안 탐색이 불공정하다 — Option B의 단점 3개 중 2개가 사실이 아니다"**,
  **"수용 기준 24개 중 7개가 닫히지 않는다"**를 지적

**뼈대는 살아남았다** — 주입 방식, `TUNING` 블록 격리, dev 하네스, `setVisible(bool)` + CSS
transition. 최종 코드가 전부 이대로다.

**사람이 한 것**

- **미정 사항을 먼저 걸러내라고 범위를 지정.** "결정된 부분만 우선 구현"이 스코프의 기준이 됐다
- 시안 둘 중 **B안(`setVisible(bool)` + CSS transition) 결정**
- **타임아웃도 오답 처리로 확정**
- `거북목 컨셉 제거`가 **기능 변경이 아니라 컨셉 표현만 뺀 것**임을 설명 — 에이전트가
  고개 각도 연출 자체를 없애는 것으로 읽을 뻔했다
- 프로토타입의 고개 각도 보간 속도를 그대로 쓸 것을 지시

---

### L-037 · 2026-08-04 · 코드

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 문서로만 합의돼 있던 접점 4개를 실물 파일로 만들어 8/5 병합일의 어긋남을 없앰 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 21:36, 21:38, 21:47

> state.js 접점 생성과 역할 뒤집힘을 별도의 PR로 머지한 후에, A, B 각자 하던대로 하면 되나?

**산출물** — 커밋 `746d5eb` `e7ed1ee`

- `src/state.js` — 값 셋을 **객체 하나로** 내보낸다. `export let gauge = 0` 같은 원시값
  바인딩이면 import한 쪽의 대입이 ES 모듈 규칙상 조용히 무시된다
- `fireGate(gateIndex)`를 함께 둠. 등록과 발화는 **한 접점의 두 짝**이지 다섯 번째가 아니다
- 콜백 하나가 던져도 나머지는 돈다 — 한쪽 예외가 전체를 멈추면 "한 번은 되는데
  그다음부터 안 된다"로 나타나 원인을 못 찾는다
- `AGENTS.md` §2의 A/B 라벨이 §1 소유권 표와 반대인 것을 정정 (L-036의 Architect 지적)

**사람이 한 것**

- **별도 PR로 먼저 머지할 것을 제안** — 공동 파일이라 각자 브랜치에서 만들면 충돌한다는 판단
- `docs/team-plan.html`이 말하는 것이 **import 방식이 맞는지 되물어 확인**한 뒤 착수 지시

---

### L-038 · 2026-08-04 · 코드

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 폰 UI와 질문 모달을 실제로 만들고, A의 주행 코드 없이도 눈으로 볼 수 있는 검증 수단 확보 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 20:32, 20:53, 21:15, 21:16, 22:34, 22:41

> 질문 이벤트가 없음. 그리고, 실제 게임 화면처럼 만들어

**산출물** — `src/phone.js` `src/quiz.js` `content/day1.json` `dev/`
(그날은 스태시에 머물렀고 8/5에 `ead4bf7` `0146a8c` `a508b97`로 들어갔다)

- `phone.js` — 대화 스트림, `setVisible` 전환, 사람/알림 구분
- `quiz.js` — 모달, 카운트다운, 정오답, 감속 패널티
- `dev/harness.html` — 가짜 복도 위에서 폰·모달을 확인하는 검증 페이지.
  투영 수식만 프로토타입에서 가져왔고 **`src/`로 옮기지 않는다** (A의 `world.js`와 충돌한다)

> ⚠️ **Space 방향은 이날 지시로 프로토타입과 반대가 됐다.**
> 20:53 *"스페이스바 조작을 반대로 바꿔. 즉, 스페이스바를 누르면 휴대폰을 보고,
> 안 누르면 정면을 보고."* — 그래서 이 구현과 `dev/handoff-to-A.md`가 그 방향으로 쓰였다.
> **8/5에 다시 뒤집혀 프로토타입 방향으로 돌아간다** → L-042.

**사람이 한 것**

- **질문 이벤트가 통째로 빠진 것을 발견**하고 지적. 계획에는 있었으나 구현에서 누락됐다
- "실제 게임 화면처럼 만들어" — 하네스가 폼만 확인하는 페이지가 되는 것을 막음
- **Space 방향을 프로토타입과 반대로 지시** (위 참조)
- Windows에서 `python -m http.server`를 팀원에게 안내해도 되는지 물어 **`.js`가
  `text/plain`으로 나가는 함정**을 드러냄 → `AGENTS.md` §6에 경고로 남음
- 기획 최신화를 다른 세션에서 하기로 하고 **스태시 결정**

---

### L-039 · 2026-08-05 · 기획

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview |
| 담당 | 공동 |
| 목적 | 8/4에 나온 기획 1차 보완을 규약에 반영하고, 무엇을 만져도 되는지가 등급으로 보이게 함 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 00:05, 01:47

> - @docs/game_balance_review.html 가 절대적인 문서는 아님. 타임아웃 패널티는 오답과 같음
> - @AGENTS.md @docs/balance-todo.md 질문 개수를 4개로 고정한 적이 없음. 제거.
> - @docs/기획_1차_보완.md 패널티의 게이지 감소와 랜덤 부여는 (추가 판단 확정 전) 임시 보류한 것이지 완전 폐기(기각)가 아님. 스크랩도 `되살리지 않는다`라고 확정하진 말 것.

**산출물** — 커밋 `79de839` `bc79a9d` `fbd518b` `35b00b3`
및 `docs/ai-log/specs/2026-08-05-planning-revision-sync.md`

- **확정/잠정/열림 3등급 도입.** 이전 판은 "확정 / 열림" 둘뿐이라, 스스로
  *"수치는 구현 후 경험으로 조절"*이라 적어 둔 항목까지 되돌릴 수 없는 칸에 앉아 있었다
- `docs/ui-spec.md` 신설 — 실시간 안은 버렸지만 **거기서 나온 폰 UI는 살아남았다**
- `docs/balance-todo.md` 분리 — §3은 확정 사항의 자리이고 할 일 목록을 섞으면
  다음 사람이 "정해진 것"과 "해야 할 것"을 구분하지 못한다

**사람이 한 것**

- **`game_balance_review.html`이 정본이 아님을 명확히 함.** 에이전트가 그 문서의 권고를
  결론으로 취급하고 있었다. 7건 중 따르지 않기로 한 것이 4건이다
- **"질문 개수를 4개로 고정한 적이 없다"**고 지적 — 더미 콘텐츠 개수가 기획으로 굳어가고 있었다
- **보류와 폐기를 구분하도록 요구.** 취소선을 긋지 않은 것이 이 지시에서 나왔다
- 목업 이미지의 `1. 2. 3.`이 **AI 생성 산물이므로 무시**하라고 지시 → 숫자키 오해를 막음
- 더 이상 필요 없는 파일 파악을 지시 (실시간 배치 그림 2장 정리)

---

### L-040 · 2026-08-05 · 레벨·밸런싱

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 같은 날 오전에 "감속만 쓴다"로 좁혔던 패널티를 기획 원문의 2종 랜덤으로 되돌림 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 11:44, 11:48, 12:02, 12:03

> `패널티 랜덤 부여` 일단 살리고 경험으로 테스트

> 게이지 감소를 살리고 2가지 랜덤 기획 원문 그대로

**산출물** — 커밋 `31105f6` `1e129fe` `1a87300`

**이 결정이 접점 계약을 건드렸다.** `AGENTS.md` §2가 `gauge`를 정답 전용 채널로 두고 있었는데
오답·타임아웃에 감소 방향이 열렸다. 접점 개수가 는 것이 아니라 **채널 하나에 방향이 열린 것**이고,
기록자는 여전히 B 하나다.

손익분기 부등식의 `L`도 상수에서 **기댓값**이 됐다 — `L = r·L_감속 + (1−r)·L_게이지`.
`r`도 감소량도 정해진 적이 없어 8/6으로 넘겼다.

**사람이 한 것**

- **판단을 되돌린 주체.** 같은 날 오전 좁힌 것을 "기획 원문 그대로"로 복원하도록 지시.
  수치를 문서로 정하지 말고 **경험으로 보자**는 것이 근거였다
- **`src/state.js`와 `src/quiz.js`의 주석까지 고치도록 지시** —
  *"잠깐 내가 B야 quize 주석도 수정해"*. 규약과 코드가 두 벌로 어긋나면
  8/5 병합 때 상대가 읽는 것은 코드 쪽이라는 판단
- 게이지가 0일 때 감소가 무효가 되는 역상관을 **알고 받아들임** (8/6 관측 대상으로 남김)

---

### L-041 · 2026-08-05 · 도구·자동화

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) · Codex (gpt-5.5) |
| 담당 | 공동 |
| 목적 | 팀원이 Codex를 쓰는 경우에도 프롬프트 원문이 빠지지 않게 하고, 정본이 두 벌이 되는 것을 막음 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 14:20, 15:27, 15:29, 15:37, 15:38

> codex용 skill, hook 생성

**산출물** — 커밋 `868751b` `9096539` `b2c6e13`

- 훅·스킬 **정본을 `.agents/`에 한 벌만** 두고 `.claude/`·`.codex/`는 가리키기만 한다.
  `CLAUDE.md`가 `AGENTS.md`를 가리키는 것과 같은 구조다
- raw 항목 헤더에 **어느 도구로 쓴 것인지**가 함께 적힌다. 같은 파일에 둘이 쌓기 때문이고,
  그 표기가 지침 §3의 `도구` 필드 근거가 된다
- 스킬에서 도구 전용 지시를 걷어냄 — Codex는 `.claude/`를 읽지 않는다

**실제로 Codex에서 훅이 도는 것을 확인했다.** 15:37 항목의 헤더가
`· Codex (gpt-5.5)`로 찍혀 있는 것이 그 증거다.

**사람이 한 것**

- Codex 대응을 **먼저 요구.** 훅이 없는 도구로 전환하면 그 기간 기록이 통째로 빈다
- `raw/README.md`의 Codex 서술이 **예시인지 실제인지 되물어** 확인.
  문서가 동작하지 않는 것을 사실처럼 적는 것을 막았다
- **Codex에서 직접 입력해 검증** (15:37). 문서가 아니라 실행으로 확인한 것이다

---

### L-042 · 2026-08-05 · 코드

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) — deep-interview |
| 담당 | B |
| 목적 | 8/4 스태시를 되살리고, 그 사이 바뀐 기획에 코드를 맞춤 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 16:30, 17:26, 17:47, 18:00, 18:08

> - 모달 UI를 실제 휴대폰처럼 높이를 더 길게
> - 기획_1차_보완.md 재확인: 좌우 조작은 A/D이고, 스페이스바는 눌렀을 때 정면을 보는 것으로 확정했음. 이 외에 기획과 다른 부분 있는지 파악
> - 정답 분포를 고정하지 않고, 보기를 랜덤 배치하면 어떰?

> - harness.html 상 게이트에서 질문 이벤트가 트리거 되고 있음. 게이트와 질문 이벤트 시점은 관계가 없음. 수정할 것.
> - 기존 스태시에 있던 문서보다 현재 @docs/기획_1차_보완.md 와 @AGENTS.md 가 최신본임. 스태시와 충돌이 발생하면, 무조건 현재 문서로 판단할 것.

**산출물** — 커밋 `ead4bf7` `0146a8c` `a508b97`
및 `docs/ai-log/specs/2026-08-05-phone-quiz-sync.md` (5라운드, 모호도 14%)

스태시와 8/5 기획이 어긋난 곳이 **11개**였다. 카운트다운 6→10초, 감속 고정→2종 랜덤,
`gauge` 감소 방향, 숫자키 제거, 모달 형태, 알림 색 랜덤 등.

**질문 트리거가 갈림길에 물려 있던 것이 가장 큰 결함이었다.** 그러면 문항 수가 맵 구조에
묶여 스테이지마다 달라지고 기획의 **주기 6~8초**가 성립하지 않는다. `quiz.js`가 자기
타이머로 세도록 바꾸면서 `mountQuiz`가 `onGate`를 받지 않게 됐고, **접점 `onGate`를 지금
듣는 쪽이 없다** — 남은 소비처인 길 안내는 §3의 열림 항목이다.

> ⚠️ **Space 방향은 8/4 지시(L-038)의 반대로 되돌아갔다.** 8/4에는 사용자 지시로
> Space=폰이었고, 8/5에 프로토타입·`기획_1차_보완.md` §조작과 맞추어 **Space=정면**이 됐다.
> **커밋 `4dbdd55`·`a508b97`의 본문은 이 경위를 적지 않고 "8/4 판이 반대로 적었다"로만
> 서술했다.** 8/4 코드는 그날의 지시를 따른 것이므로 그 서술은 정확하지 않다.
> 커밋은 고치지 않는다 (§5 — squash·force-push 금지). 경위는 여기 남긴다.

검증은 브라우저 확장이 연결되지 않아 **DOM 셰임으로 모듈을 구동해 34개 항목을 기계적으로
확인**했다 — 주기 실측 6.03~8.00초, 갈림길 발화로는 모달이 뜨지 않음, 셔플 후 정답 자리가
0·1·2 전부 관측됨 등.

**사람이 한 것**

- **최신본 우선 원칙을 명시적으로 지시.** "스태시와 충돌하면 무조건 현재 문서로 판단할 것" —
  이 한 줄이 11건 대조의 기준이 됐다
- **게이트-질문 결합을 발견하고 지적.** 에이전트는 스태시 코드를 그대로 물려받아 검증하지 않았다
- 모달 높이를 실제 폰 비율로 요구
- **보기 랜덤 배치를 제안** → L-043의 방향을 바꿈
- 퀴즈가 안 뜨는 것을 보고 → 원인이 브라우저 캐시임이 드러나 `AGENTS.md` §6에 `-c-1`이 박힘

---

### L-043 · 2026-08-05 · 레벨·밸런싱

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 더미 12개로는 확인할 수 없던 실제 플레이 분량을 콘텐츠로 채움 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 16:30 이후 선택지 응답, 17:26

> 질문 문항 수는 최종적으로는 단조로움 탈피를 위해 보다 많은 질문을 구성하고 인플레이에서 랜덤으로 뽑는 것을 생각중이나, 지금은 20개 내외만 생성.

**산출물** — 커밋 `55f9eec` — `content/day1.json` 문자 58개 · 질문 20문항

개수가 계산에서 나왔다. 스트림 2.6초 간격 × 스테이지 상한 150초 ≈ 58,
주기 7초 기준 질문 ≈ 20. **모달 중 스트림이 멈추므로** 질문 20개 × 10초는 이 계산에
들어오지 않는다 — 안 멈췄다면 135개가 필요했다.

**초안에서 결함이 하나 나왔다.** 20문항 중 **18개의 정답이 보기 2번**이어서,
`balance-todo.md` §2가 판정 실험으로 잡아 두려던 *"전부 찍기만 해서 클리어되는가"*가
콘텐츠 단계에서 이미 성립했다. 처음에는 7/7/6으로 흩어 고쳤으나,
**사용자 제안으로 런타임 셔플로 대체**했다 — JSON에서 흩는 것은 한 판 안에서만 고르고
다시 하면 같은 자리라 외우면 그만이기 때문이다.

**사람이 한 것**

- **질문 20개는 "풀"이고 최종적으로는 더 늘려 인플레이 랜덤 추출로 간다**는 방향 제시
- **보기 랜덤 배치를 제안** — 콘텐츠에서 정답 위치를 관리하던 것을 코드로 옮겨,
  JSON은 사람이 검토하기 쉬운 자연스러운 순서로 되돌아갔다

---

### L-044 · 2026-08-05 · 문서

| | |
|---|---|
| 도구 | Claude Code (Claude Opus 5) |
| 담당 | B |
| 목적 | 코드를 기획에 맞추는 과정에서 드러난, 문서 쪽의 어긋남을 함께 정정 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-05.md` 17:26, 17:47 (L-042과 같은 세션)

**산출물** — 커밋 `4dbdd55` `63fde40`

정본인 `AGENTS.md`가 **자기 자신과 두 곳에서 모순**이었다.

- §3 **확정**의 *"오답과 타임아웃의 패널티는 같다. 둘 다 감속이다"* — 꼬리말이 랜덤 부활
  전 문구다. 같은 절의 **잠정** 항목도, §2도, `docs/기획_1차_보완.md`도 2종 랜덤을 말한다
- §1의 `quiz.js` 범위가 *"감속 패널티"*로 남아 있었고 **질문 도착 주기의 소유가 표에 없었다**.
  소유가 없으면 A가 `main.js`에 같은 타이머를 또 만든다
- §2에 **`onGate`가 갈림길 신호이지 질문 신호가 아니라는 것**을 못 박음
- `docs/ui-spec.md`의 Space 방향 정정 (경위는 L-042 참조)
- §6 실행 명령에 **`-c-1`** — `http-server` 기본값이 `max-age=3600`이라 고친 `.js`가
  한 시간 동안 반영되지 않는다. 8/5에 두 번 걸렸고, 두 번째는 캐시된 옛 코드가 새 호출
  규약과 안 맞아 예외를 던져 **퀴즈가 통째로 안 뜨는 것**으로 나타났다

**사람이 한 것**

- **최신본을 판단 기준으로 지정** — "기획_1차_보완.md와 AGENTS.md가 최신본이다"
- 정본 수정이 **공동 자산에 대한 단독 변경**임을 알고 진행 여부를 승인.
  A와의 확인이 필요하다는 것은 PR 본문에 남겼다

---

### L-045 · 2026-08-06 · 코드

|      |                                                                           |
| ---- | ------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                             |
| 담당 | 공동                                                                      |
| 목적 | 신호등 잘못된 차선 경고 토스트만 남기고, 죽어 있던 방향 안내 토스트 코드를 걷어냄 |
| 반영 | 그대로                                                                    |

**프롬프트** — `ai-log/raw/2026-08-06.md` 22:17

> @src/world.js에서 네비게이션용 토스트느 다 빼고, 신호등에서 잘못된 차선으로 진입했을 때 나오는 토스트 코드만 유지해줘

**산출물** — 커밋 `eac105c`

`const toast = buildToast(container)` 선언이 주석 처리된 채 남아 있어, 남겨야 할 잘못된
차선 경고(`showToast(toast, ...)`)조차 실제로는 정의되지 않은 변수를 참조하는 상태였다 —
요청받은 정리를 하다가 발견한 기존 버그다. 그 선언을 살리고, 방향 안내 토스트·
`toastTimer`/`TOAST_DURATION` 자동 숨김 로직을 제거했다. `reset()`의 "↑ 직진" 토스트도
이전 판 sticky 경고를 지우는 코드로 바꿨다.

**사람이 한 것**

- 범위를 "신호등 잘못된 차선 경고만 남긴다"로 직접 지정
- diff 확인 후 커밋 승인

---

### L-046 · 2026-08-06 · 코드

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                      |
| 담당 | 공동                                                                               |
| 목적 | main.js가 8/5 핸드오프 임시 스냅숏(-a 파일)을 여전히 참조하고 있어, B가 이미 완성해 병합해 둔 정식 phone.js/quiz.js/day1.json으로 배선을 마무리 |
| 반영 | 부분 수정                                                                          |

**프롬프트** — `ai-log/raw/2026-08-06.md` 22:44

> @dev\ 합쳐줘

짧은 한 마디였고, 실제 범위(main.js의 -a 참조를 정식 파일로 교체)는 Explore 에이전트 2개를
병렬로 띄워 `dev/handoff-to-A.md`, git 히스토리, 스키마 검사기, 각 파일 쌍의 diff를 조사해
확인했다. 계획은 `docs/ai-log/specs/2026-08-06-dev-handoff-wiring-plan.md`에 보존했다.

**산출물** — 커밋 `56667fe`

- `main.js`를 `phone.js`/`quiz.js`/`content/day1.json`으로 배선 교체
- `phone.js`의 `pushMap(laneIndex)`(옛 시그니처)를 `world.js`가 실제로 보내는 이벤트 객체
  기반 `pushMap(event)`로 교체 — `phone-a.js`가 먼저 구현해 둔 로직을 그대로 재사용했고,
  `quiz.js`가 의존하는 `activeTime`/`renderedAt`/`publishTimeline`은 손대지 않았다
- 더 이상 참조되지 않는 미추적 스냅숏 3개(`content/day1-a.json`, `src/phone-a.js`,
  `src/quiz-a.js`) 삭제
- 브라우저에 직접 띄워 `pushMap`의 네 이벤트 종류(turn/gate/construction/traffic) 전부와
  `quiz.js`의 `bm:phone-timeline` 수신을 확인

**사람이 한 것**

- 조사 결과 `phone.js`(B 담당 파일)의 `pushMap` 시그니처까지 고쳐야 한다는 것이 드러나자,
  손댈지 여부를 직접 결정("phone.js의 pushMap까지 같이 고치기")
- 미추적 스냅숏 3개의 삭제 여부도 직접 결정("삭제한다")
- 계획(Plan 모드) 승인

---

### L-047 · 2026-08-06 · 코드

|      |                                                                              |
| ---- | ---------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5) — 핵심 수정 자체는 사람이 직접 작성            |
| 담당 | 공동                                                                        |
| 목적 | 사용자가 quiz.js에 직접 넣은 수정(판정 후 이동 재개를 모달이 사라진 뒤로 미룸)을 diff 노이즈 없이 커밋에 반영 |
| 반영 | 부분 수정                                                                    |

**프롬프트** — `ai-log/raw/2026-08-06.md` 23:19

> @src/quiz.js 수정한 것도 같이 커밋 넣어줘

`git diff`로 보니 실질 변경은 `close()`에서 `state.quizOpen = false`를 모달이 완전히
사라진 뒤(중첩 `setTimeout` 안)로 미루는 한 줄뿐이었고, 나머지는 파일 전체의 따옴표·
들여쓰기가 재포맷팅된 것이었다(에디터 자동포맷 추정). `AGENTS.md`가 포매터 금지·기존
스타일 유지를 명시하고 있어, 그대로 커밋하면 diff가 스타일 노이즈로 뒤덮여 실제 변경이
묻힌다는 점을 지적하고 처리 방식을 물었다.

**산출물** — 커밋 `5c42a5a`

`git checkout`으로 원래 스타일을 복원한 뒤, 확인된 한 줄(`state.quizOpen` 이동)만 다시
적용 — diff가 5줄 추가·2줄 삭제로 좁혀졌다.

**사람이 한 것**

- `state.quizOpen` 타이밍 변경 자체를 에디터에서 직접 작성
- 재포맷팅 노이즈를 남길지 실질 변경만 남길지 묻는 질문에 "실질 변경만 남기고 스타일은
  원래대로 되돌린다"를 선택

---

### L-048 · 2026-08-07 · 코드

|      |                                                                                        |
| ---- | -------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                          |
| 담당 | A                                                                                     |
| 목적 | 도착(게임 종료) 화면에 폰·퀴즈 모달이 얹히지 않게 하고, 재시작 시 퀴즈를 되살림       |
| 반영 | 부분 수정                                                                              |

**프롬프트** — `ai-log/raw/2026-08-07.md` 09:42, 09:53, 09:54, 10:09, 10:17

**산출물** — 커밋 `7daedb1`, `44c4e48`

도착 시 `phone.setVisible(false)` + `quiz.destroy()`로 결과 화면 동안 폰·퀴즈를 멈추고,
`restart()`에서는 `quiz.reset()` 대신 `mountQuiz()` 재호출로 죽은 raf 루프를 되살린다.
`quiz.destroy()`가 rAF를 끊어 두므로 `reset()`만으로는 다시 뜨지 않는 문제를 확인해 재마운트로 처리.

**사람이 한 것**

- 폰이 다시 뜨지 않게 하는 부분(keyup 조건)은 **본인이 직접 작성** — 퀴즈 쪽만 AI에 맡김
- 퀴즈 모달을 B 파일이 아니라 A 담당(main.js)에서 제어할 수 있는지 물어 범위를 A로 고정
- `mountQuiz()` 재호출과 B에 export 요청 중 **재마운트를 선택**(게임 오버 후 완전 재시작이면 idx 초기화가 맞다는 판단)

---

### L-049 · 2026-08-07 · 코드

|      |                                                                                        |
| ---- | -------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                          |
| 담당 | A                                                                                     |
| 목적 | 정답으로 충전하고 W로 소비하는 부스터(무적 + 가속) 시스템 구현                        |
| 반영 | 부분 수정                                                                              |

**프롬프트** — `ai-log/raw/2026-08-07.md` 10:41(스펙 전문), 11:16(게이지 색·바 폭)

**산출물** — 커밋 `7423ad2`(world 물리·무적·게이지 소모), `df19ffb`(main 입력·게이지 바),
`b962fb9`(AGENTS.md §3·기획 문서 정정)

W 입력·게이지>0·신호등 미갇힘일 때만 부스트, 부스트 중엔 충돌 감속 대신 `BOOST_SPEED`·
사람과 부딪혀도 무적. `state.gauge`(B가 채우는 채널)를 A가 소비만 하도록 배선.

**사람이 한 것**

- 부스터 스펙 전문을 작성(게이지 +25%/−15%, W 유지, 무적+가속, 0%면 즉시 종료, 신호등 오진입 시 사용 불가)
- 이 스펙이 `AGENTS.md` §3 확정 항목(가속키 Shift·무적)과 충돌함을 지적하자 **W키 변경 합의·무적 유지·단위는 B에 맞춤**을 결정
- `BOOST_SPEED`(9→12)·소모 속도(→30)를 에디터에서 직접 튜닝하고, 문서를 코드값(12)에 맞추도록 결정
- 게이지 색을 빨강으로, 바 가로 폭을 늘리도록 지시

---

### L-050 · 2026-08-07 · 코드

|      |                                                                                        |
| ---- | -------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5 · Claude Opus 4.8) — 게이트·게이지는 Sonnet, 시간초과·배치는 Opus |
| 담당 | A                                                                                     |
| 목적 | 게이트 오진입 토스트, 숫자 없는 시간 게이지, 루트별 성공 제한시간, 시간초과 재시작 모달, 게이지 좌상단 배치 |
| 반영 | 부분 수정                                                                              |

**프롬프트** — `ai-log/raw/2026-08-07.md` 11:35, 11:52, 12:12, 12:15

**산출물** — 커밋 `6aa2927`(world: 잘못된 게이트 토스트·루트별 제한시간),
`045399e`(main: 시간 게이지·시간초과 모달·게이지 좌상단 배치·부딪힘을 결과 모달로)

잘못된 문 진입 시 신호등 패널티와 **같은 토스트 UI**로 "잘못된 게이트 진입"을 되돌리기 직전에
표시(sticky 대신 시간으로 자동 소멸). 성공 제한 시간은 `totalLength/SPEED + 10초`로, `docs/map/
맵 시간 배분.md`의 "예상 소요 시간 + 10초"를 표에서 옮기지 않고 경로 길이에서 직접 재도록 했다.

**사람이 한 것**

- 요구사항 전문을 작성(게이트 오진입 토스트, 숫자 없는 로딩바 시간 게이지, 루트별 제한시간, 게이지 동작·일시정지)
- 성공 제한 시간 기준을 "각 루트 예상 소요 + 10초"로 지정하고 `맵 시간 배분.md`를 근거로 지목
- 제한 시간 초과 시 **재시작 모달**을 띄우도록 요구
- 게이지를 부딪힘 왼쪽·부스터와 같은 크기·타임→부스터 순·**한 줄(일직선)**로 배치하도록 3차례 교정
- 상시 표시하던 **부딪힘 횟수를 결과 모달에서만** 보이도록 변경 지시

---

### L-051 · 2026-08-07 · 코드

|      |                                                                                        |
| ---- | -------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                          |
| 담당 | A                                                                                     |
| 목적 | 퀴즈 오답 패널티(state.speedMul)가 world.js의 전진 속도 계산에 반영되지 않던 연결 누락을 고침 |
| 반영 | 그대로                                                                                 |

**프롬프트** — `ai-log\raw\2026-08-07.md` 13:23, 13:47

**산출물** — 커밋 `b2bcf85`

`quiz.js`가 오답·타임아웃 패널티로 `state.speedMul`을 낮춰도 `world.js`의 전진 속도
계산(`SPEED * collisionMul`)이 이 값을 아예 읽지 않고 있었다. `state.js` 주석이 이미
못 박아 둔 공식(`SPEED * state.speedMul * collisionMul`)대로 곱하도록 한 줄을 연결했다.
부스트 중에는 기존대로 무적 + `BOOST_SPEED` 고정이라 영향을 받지 않는다.

**사람이 한 것**

- "퀴즈 감속을 world와 연결해달라"는 요청 자체가, world.js가 애초에 그 값을 읽지 않던
  연결 누락 버그였음을 알아채게 한 계기 — 진단·구현·검증은 AI가 하고 결과를 바로
  승인("커밋해주세요")

---

### L-052 · 2026-08-07 · 코드

|      |                                                                                        |
| ---- | -------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                          |
| 담당 | A                                                                                     |
| 목적 | 신호등 구역에 이미 서 있는 패널티용 군중과 겹치지 않게, 그 구역에만 지도 전체 랜덤 배치 행인의 스폰을 막음 |
| 반영 | 부분 수정                                                                              |

**프롬프트** — `ai-log\raw\2026-08-07.md` 14:10, 14:10(계속), 14:25

**산출물** — 커밋 `abbddae`

게이트 주변에 쓰던 `spawnBlocks` 생성 금지 구간을 신호등(T) 구역까지 확장해 등록했다
(주석이 이미 예고해 둔 확장 방식 그대로). `createPedestrians`가 이 등록을 읽어야 하므로
`createEvents` 호출 순서를 그보다 앞으로 옮겼다. 공사장·일반 도로의 랜덤 행인은
그대로 둔다.

**사람이 한 것**

- 최초 요청("신호등 구역 패널티 행인 말고 랜덤 행인은 없애고 싶다")을 AI가 "지도
  전체 랜덤 행인 삭제"로 과잉 해석해 `createPedestrians` 자체를 들어내는 구현을
  진행하자, 세션 중간에 **"신호등 구역에서만 빼고 다른 구역 랜덤 행인은 그대로 둬야
  한다"고 정정** — 이미 만든 (잘못된) 변경을 되돌리고 올바른 범위(spawnBlocks 확장)로
  다시 구현하게 이끎. 이 정정 원문은 세션 중간에 도착해 훅이 못 잡아
  `ai-log/raw/2026-08-07.md`에 수동으로 복원했다(L-009와 같은 경우)

---

### L-053 · 2026-08-07 · 코드

|      |                                                                                        |
| ---- | -------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                          |
| 담당 | A                                                                                     |
| 목적 | R 키로 게임 다시 시작 확인 모달을 추가하고, 그 과정에서 발견된 재시작 시 퀴즈 인스턴스 중복 버그를 함께 고침 |
| 반영 | 그대로                                                                                 |

**프롬프트** — `ai-log\raw\2026-08-07.md` 14:28, 14:38, 14:41, 15:09

**산출물** — 커밋 `540ebb8`

`src/main.js`에 "게임을 다시 시작하시겠습니까?" 확인 모달(취소/확인)과 `confirmOpen`
가드를 추가해, 모달이 떠 있는 동안 퀴즈 일시정지와 같은 방식으로 시간·이동을 멈췄다.
같은 turn에서 `restart()`가 `quiz.destroy()` 없이 바로 `mountQuiz()`를 재호출하던
기존 버그(도착·시간초과 경로만 전제한 코드라, R로 게임 도중 재시작하면 이전 퀴즈
인스턴스의 rAF·리스너가 안 죽고 새 인스턴스와 겹쳐 도는 문제)도 함께 고쳤다.

**사람이 한 것**

- 모달 문구·버튼 구성(취소/확인)을 직접 지정
- 퀴즈 모달이 뜬 동안에도 R을 받을 수 있는지 물었다가, `quiz.js`가 B 소유 파일이라
  그 rAF 타이머를 A 쪽에서 멈출 방법이 없다는 설명을 듣고 **"지금처럼 둔다"**로
  현재 구현(퀴즈 중엔 R 무시) 유지를 결정
- 게이지 초기화도 같은 이유(퀴즈 게이지의 유일한 기록자는 B)로 **"B한테 따로
  말할게"**라며 보류하고 팀 간 협의로 넘김 — 두 논의 모두 코드 변경 없이 파일 소유권
  경계를 재확인한 결정이지만, 그 과정에서 AI가 스스로 찾아낸 `restart()` 버그는
  그대로 반영하도록 승인

---

### L-054 · 2026-08-07 · 코드

|      |                                                                                        |
| ---- | -------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                          |
| 담당 | A                                                                                     |
| 목적 | 길 안내(회전·게이트·신호등·공사장) 순서를 문서(맵 시간 배분.md) 9개 경로와 맞추고, 그 과정에서 드러난 안내 큐 영구 정지 버그를 고침 |
| 반영 | 부분 수정                                                                              |

**프롬프트** — `ai-log\raw\2026-08-07.md` 15:12(9개 경로 전문 포함), 15:33, 15:49

**산출물** — 커밋 `c575992`

`computeTurnHints`에 건물 노드 예외(B7 제외, 도수 3 미만 `B\d+`는 물리적으로 꺾여도
안내 안 함)를 추가하고, `createEvents`의 신호등/공사장 사전 안내 `s`에 `nodeS(i-1)`
하한 클램프를 넣었다가, 그 클램프가 앞 노드가 실제 갈림길일 때 두 안내의 `s`를
정확히 같게 만들어 `update()`의 전진 판정(`prevS < front.s && s >= front.s`)이
영원히 성립하지 않는 새 버그(안내 큐가 그 지점에서 영구 정지)를 냈음을 자체
시뮬레이션으로 재현·확인하고 `+1` 여유를 둬 최종 수정했다. Node로 `update()`의
순수 계산부를 그대로 복제해 9개 경로 전부를 문서와 대조 검증했다.

**사람이 한 것**

- 실제 플레이에서 관찰한 두 구체적 증상을 지목 — "B8은 건물 안에 갈림길 없는데
  관련 네비 나옴", "게이트 안내는 입구 말곤 안 나옴"
- 정답 기준으로 `docs/map/맵 시간 배분.md`의 9개 경로 기호 순서 전문을 직접
  붙여넣어 검증 기준을 제공(요약이 아니라 원문 그대로)
- 1차 수정 후에도 **"B7, B8에서 안내 문자가 안 나와"라고 재현 결과를 재보고** —
  AI가 자신의 1차 수정이 새 버그를 냈음을 스스로 발견·수정하게 만든 재귀적
  디버깅을 이끎

---

### L-055 · 2026-08-07 · 아트

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | GPT (이미지 생성·치수 제안, 세부 도구·버전 미상)                                  |
| 담당 | A                                                                                  |
| 목적 | fence(펜스·연석)·가로수·공사장 바리게이트 3D 모델의 형태·치수 감을 잡기 위해 GPT로 이미지 생성과 치수 제안을 받음 |
| 반영 | 부분 수정                                                                          |

**프롬프트** — (원문 미확보 — 요약만)

이 저장소(Claude Code/Codex) 밖의 별도 세션에서 진행돼 훅이 원문을 잡지 못했다.
fence·가로수·바리게이트 각각의 형태를 GPT로 이미지 생성해 보고, 치수(크기) 제안을
받은 것으로 확인됨. `/ai-log` 진행 중 사용자에게 직접 확인해 얻은 정보다.

**산출물** — `assets/map/fence.glb`, `assets/map/fence2.glb`, `assets/map/fence3.glb`,
`assets/map/tree.glb`, `assets/map/barricade.glb` (Blockbench 5.1.6 export)

**사람이 한 것**

- GPT의 이미지·치수 제안은 형태를 잡는 참고 자료로만 쓰고, 실제 모델링은
  Blockbench로 직접 제작
- 게임에 넣을 최종 모델·스케일(예: fence2.glb를 최종 채택, 높이 0.9로 조정)은
  이후 코드 단계(L-056)에서 직접 여러 차례 재조정

---

### L-056 · 2026-08-07 · 코드

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                      |
| 담당 | A                                                                                  |
| 목적 | 절차적으로 그리던 도로 벽(quad)·잔디 바닥·가로수 없음을 fence.glb 계열·tree.glb 3D 에셋으로 교체 |
| 반영 | 부분 수정                                                                          |

**프롬프트** — `ai-log/raw/2026-08-07.md` 17:00, 17:31, 17:45, 17:51, 18:35

> @assets/map/fence.glb 이 모델을 기존 벽을 대체하는 시각적 모델로 적용하세요. 조건은
> 다음과 같습니다. 기존 벽의 충돌은 그대로 유지합니다. fence.glb는 충돌용이 아니라
> 화면에 보이는 모델만 담당합니다. (…) 여러 개의 벽이 생성되어도 모델이 정상적으로
> 재사용되도록 구현해주세요. (매번 새로 로드하지 말고, 한 번 로드한 뒤 clone해서
> 사용) (…) Three.js 권장 방식으로 구현해주세요.

이어서 17:31 "fence2.glb로 바꿔줘", 17:45 "fence3.glb 크기 너무 키우지 말고 (…) 턱이
있다정도록", 17:51 "게임 루트 말고 지금 파란색으로 뜨는 바닥 부분을 잔디 깔아둔
것처럼", 18:35 "잔디 위에 tree.glb를 색 입혀서 여러 곳에 심어줘"로 이어졌다.

**산출물** — `src/world.js`(`loadFenceUnit`/`buildFenceSegment`/`loadTreeUnit`/
`scatterTrees` 등), `vendor/GLTFLoader.js`, `vendor/BufferGeometryUtils.js`
(three.js r160 examples를 받아 CDN 대신 동봉, import를 상대경로로 수정). 커밋 `f25ad66`

**사람이 한 것**

- fence.glb → fence2.glb → fence3.glb로 세 차례 모델을 바꾸며 "크기 너무 키우지 말고
  턱이 있다 정도로"처럼 구체적 크기 감각을 지시
- "게임 루트 말고 지금 파란색으로 뜨는 바닥"이라는 관찰로, 잔디 바닥 사각형이 뒷면
  컬링에 걸려 아예 안 그려지고 배경색만 비치던 버그를 발견하게 함(AI가 원인 규명·수정)
- fence3.glb(턱) 적용 이후 실제 채택 모델·높이(fence2.glb, 0.9)를 **코드에서 직접
  재조정** — 이 전환을 지시한 프롬프트는 raw에 없다(같은 세션 내 직접 수정으로 확인됨,
  8/8 `/ai-log`에서 재확인)
- "색 입혀서 여러 곳에" 심으라고 지시해, GLTFLoader가 같은 재질을 여러 메시가
  공유하는 문제(부위별 색 구분이 안 되는)를 피하도록 요구

---

### L-057 · 2026-08-07 · 코드

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                      |
| 담당 | A                                                                                  |
| 목적 | 공사장 바리게이트를 절차적 판(quad)에서 barricade.glb 모델로 교체하고, 공사장 벽의 색 구분(주황 줄무늬)을 없애 일반 도로 벽과 통일 |
| 반영 | 그대로                                                                             |

**프롬프트** — `ai-log/raw/2026-08-07.md` 19:41, 19:43

> @assets/map/barricade.glb 공사장 바리게이트를 이걸로 바꿔줘. 색은 큐브이름이랑
> 똑같이 넣어주면 돼. 주황은은 최대한 쨍했으면 좋겠어.
>
> 그리고 공사장 벽도 @assets/map/fence2.glb로 바꿔주고, 구분 색도 없애줘

**산출물** — `src/world.js`(`loadBarricadeUnit`/`buildBarricadeGroup`, `mats.C`에서
`wall` 제거). 커밋 `f25ad66`

**사람이 한 것**

- 색 규칙을 "큐브 이름 그대로"로 명확히 지정하고, 주황은 "최대한 쨍하게"라는
  구체적 톤을 요구
- 공사장 벽도 일반 도로와 같은 fence2.glb로 통일하고 색 구분을 완전히 없애라고
  지시 — 그 결과 AI가 별도 검증 페이지(임시 스크래치 HTML, 확인 후 삭제)를 만들어
  GLTFLoader가 같은 이름의 노드를 "이름_1"처럼 고유화하는 바람에 색이 한 조각에만
  입혀지던 버그를 스스로 발견·수정하고 눈으로 검증하게 만듦

---

### L-058 · 2026-08-06 · 기획

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-terra) — deep-interview |
| 담당 | B |
| 목적 | 대화의 근거를 보기도 전에 퀴즈가 열리는 불공정한 경우를 막을 기준과 범위를 확정 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-06.md` 15:09~15:31

> 대화 표시와 문제 사이에 최소 3초의 간격을 보장할 것.

**산출물** — `docs/ai-log/specs/2026-08-06-conversation-quiz-separation.md`

각 퀴즈가 `sourceMessageIndex`로 지정한 근거 메시지가 실제 표시된 뒤, 활성 게임 시간으로 3초가 지나야 열리도록 명세화했다. 기존 6~8초 주기가 먼저 끝나면 해당 퀴즈를 건너뛰지 않고 자격 충족 시점까지 미루며, 모달 중 시간 정지도 유지한다. 메시지·퀴즈를 하나의 큐로 묶는 대안은 2.6초 메시지 주기와 3초 큐 소비가 누적 지연을 만들므로 제외했다.

**사람이 한 것**

- 불쾌한 선출제 사례를 문제로 특정하고 최소 대기 시간을 **3초**로 결정
- 전역 침묵 시간이 아니라 각 근거 메시지의 실제 표시 시각을 기준으로 하도록 결정
- 큐 기반 재설계와 `sourceMessageIndex` 게이팅을 비교하게 하고, 후자를 채택

---

### L-059 · 2026-08-06 · 코드

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-terra) |
| 담당 | B |
| 목적 | L-058 명세대로 근거 대화가 먼저 표시되고 충분히 읽힌 뒤에만 퀴즈를 열기 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-06.md` 15:32~16:58

**산출물** — 커밋 `82cd71b`

- `phone.js`가 실제 메시지 표시 시각을 `bm:phone-timeline`으로 알리고 `quiz.js`가 이를 출제 자격에 사용
- `day1.json`에 각 퀴즈의 근거 메시지 인덱스를 맞추고, `quiz-eligibility.mjs`와 단위 테스트로 3초 경계를 검증
- 주기가 먼저 끝난 퀴즈는 근거 메시지의 활성 시간 3초가 찰 때까지 보류

**사람이 한 것**

- 구현 중 "퀴즈가 안 뜬다"는 관찰을 제공해 게이팅 동작을 재점검
- 커밋과 기존 PR #12에의 반영 방식을 직접 결정

---

### L-060 · 2026-08-07 · 기획

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-terra) — deep-interview, ralplan |
| 담당 | B |
| 목적 | 퀴즈를 시험 문제가 아닌 업무 문자 답장으로 보이게 하는 최소 변경 범위와 숫자키 입력 경계를 검토 |
| 반영 | 골격만 차용 |

**프롬프트** — `ai-log/raw/2026-08-07.md` 01:14~01:32

**산출물** — `docs/ai-log/specs/2026-08-07-quiz-business-text-spec.md`, `docs/ai-log/specs/2026-08-07-quiz-business-text-plan.md`

문체와 발신자만 먼저 바꾸고, 콘텐츠 의미·정답·난이도·주기·패널티는 유지하는 범위를 정했다. 숫자키는 모달이 열린 동안만 동작하고 같은 답안 제출 경로를 타야 한다고 정의했다. 다만 최종 구현 전 사용자가 마우스 선택을 완전히 없애 숫자키 전용으로 좁혔으므로, 원 계획은 입력 경계와 데이터 설계의 골격만 사용됐다.

**사람이 한 것**

- 업무 문자 느낌이면 상황이 섞여도 된다고 품질 기준을 정함
- 첫 패스는 문체·발신자만 바꾸고 의미 변경 여부는 화면 확인 뒤 결정하도록 범위를 제한
- 숫자키를 모달 밖에서 무시하도록 정하고 가상 발신자 집합 사용을 승인

---

### L-061 · 2026-08-07 · 코드

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-terra) |
| 담당 | B |
| 목적 | 업무 문자형 퀴즈 문체·발신자와 전방/폰 입력 전환에 맞는 숫자키 전용 답안 선택 구현 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-07.md` 02:50

**산출물** — 커밋 `48347e4`

- 20개 퀴즈에 발신자와 업무 문자형 질문·답장 문체 추가; 기존 정답 인덱스와 의미는 유지
- 열린 모달에서만 `1`~`3`이 답안으로 제출되고, 마우스 선택은 제거
- 숫자와 답장 본문을 시각적으로 분리하고 선택된 답안에 눌림 효과 추가

**사람이 한 것**

- 마우스와 병행하지 않고 **숫자키 전용**으로 바꾸도록 범위를 수정
- 선택 직후 어떤 답안을 냈는지 보이도록 피드백 효과를 요청
- 문체의 업무 문자화와 답장 후보의 하단 배치를 직접 피드백

---

### L-062 · 2026-08-07 · 코드

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-terra) |
| 담당 | B |
| 목적 | 대화 흐름에서 메시지 종류를 일관되게 구분하도록 랜덤 배경색을 없애기 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-07.md` 02:50

> 추가로, 지금은 대화 메시지의 배경색이 랜덤인데, 랜덤 제거. 고정으로 변경.

**산출물** — 커밋 `d5875fe`

알림의 랜덤 색상 분기를 제거하고 사람·알림·길 안내 메시지의 색을 고정했다.

**사람이 한 것** — 랜덤 배경을 고정색으로 바꾸는 UI 판단을 직접 지시.

---

### L-063 · 2026-08-07 · 코드

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-terra) |
| 담당 | B |
| 목적 | 퀴즈 모달의 중앙 여백을 줄이되 하단 답장 후보 배치는 유지 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-07.md` 02:58~03:06

**산출물** — 커밋 `eb22426`

모달 최대 높이를 낮춰 질문과 하단 답장 후보 사이의 빈 공간을 축소했다.

**사람이 한 것**

- 중앙 여백이 크다는 시각 검토 결과를 제공
- 답장 후보는 하단에 그대로 붙여 두도록 명시

---

### L-064 · 2026-08-06 · 기획

|      | |
| ---- | - |
| 도구 | Claude Code (Claude Sonnet 5) — deep-interview, ralplan |
| 담당 | B |
| 목적 | 반복 플레이에서 고정된 수치·고유명사와 정답을 외우는 이점을 막을 판 단위 랜덤화 설계 수립 |
| 반영 | 골격만 차용 |

**프롬프트** — `ai-log/raw/2026-08-06.md` 17:56 이후, `2026-08-07.md` 03:23

**산출물** — `docs/ai-log/specs/2026-08-06-content-value-randomization-spec.md`, `docs/ai-log/specs/2026-08-06-content-value-randomization-plan.md`

19개 수치·고유명사형 퀴즈는 판 시작 때 값 풀에서 독립적으로 정하고, 서사형 1개는 고정으로 남기는 설계를 만들었다. 메시지는 플레이스홀더 템플릿으로 보존하고 보기·정답을 런타임에 구성하며, 근거 메시지와 퀴즈의 결속을 검사하는 방안까지 포함했다. 최종 구현은 이 원칙을 유지하되 별도 값 해석 모듈 대신 세션 단위 콘텐츠 재구성으로 확장했으므로 골격만 차용으로 기록한다.

**사람이 한 것**

- 랜덤화 대상을 예시 몇 개가 아닌 모든 수치·고유명사 근거로 넓히고, 판마다 한 번 배정하도록 결정
- 19개 동적 문항과 서사형 1개 고정이라는 경계를 확정
- 오답도 유형별 함정 특성을 유지하고, 반복 암기를 막기 위해 대화량·표시 순서도 함께 검토하도록 지시

---

### L-065 · 2026-08-07 · 코드

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-sol) |
| 담당 | B |
| 목적 | 판마다 대화·수치·보기 조합을 다시 만들어 반복 플레이의 정답 암기를 약화 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-07.md` 03:23~04:17

**산출물** — 커밋 `e24fb3f`

- 대화 묶음을 무복원 추출해 한 판 분량의 메시지·퀴즈를 구성하는 `src/content-session.mjs` 추가
- 안정 ID로 실제 표시된 근거 메시지와 출제 자격을 연결하고, 값·보기·오답을 판마다 새로 구성
- `day1.json`을 대화 묶음과 값 풀 중심 스키마로 확장하고 검사기·단위 테스트·하네스를 함께 갱신

**사람이 한 것**

- Claude가 만든 랜덤화 계획을 검토 대상으로 지정하고 구현을 지시
- 수치뿐 아니라 대화도 늘려 일부를 랜덤 표시하도록 목표를 확장
- 고개 각도 시험 변경과 첫 하네스 제외 결정을 번복해, 최종 커밋에는 하네스 변경을 포함하고 고개 각도 변경은 제외하도록 결정

---

### L-066 · 2026-08-07 · 코드

|      | |
| ---- | - |
| 도구 | Codex (gpt-5.6-sol) |
| 담당 | B |
| 목적 | 단일 정보만 묻는 퀴즈의 난도를 높이고, 두 정보를 모두 기억해야 정답을 확정할 수 있는 문항을 일정 비율로 출제 |
| 반영 | 부분 수정 |

**프롬프트** — `docs/ai-log/raw/2026-08-07.md` 17:57~21:07

복합 문항을 소수 추가하라는 지시에서 시작해, 전체 50문항 중 복합 문항 10개를 별도 풀로 두고
매판 20문항의 20%인 4개를 고정 출제하는 방향으로 구체화했다. 처음 제안된 3지선다 오답은
한 정보만 기억하거나 보기의 반복 패턴을 보면 정답이 드러난다는 사용자 검토로 두 차례
기각됐다. 정답 `A1+B1`과 세 오답 `A1+B2`, `A2+B1`, `A2+B2`를 모두 제공하는 복합 문항
전용 4지선다로 바꿔, 한 정보만 기억하면 후보가 두 개 남도록 확정했다.

**산출물** — 커밋 `9df51a9`, PR #20

- `content/day1.json`에 날짜+목적, 시간+장소처럼 두 정보를 함께 묻는 복합 문항 10개 추가
- `content-session.mjs`가 매판 복합 4개와 단순 16개를 무복원 추출해 기존 20문항·58메시지 유지
- `quiz.js`가 단순 문항은 숫자키 `1`~`3`, 복합 문항은 `1`~`4`를 처리하도록 확장
- 50개 난수 시드의 복합 문항 비율과 각 문항의 완전한 2×2 보기 조합을 회귀 테스트로 검증
- 헤드리스 Chrome 1280×720 렌더링에서 보기 4개가 폰 안에 들어가는지 확인

**사람이 한 것**

- 단순 문항보다 두 변수를 함께 기억하는 복합 문항이 필요하다는 발상을 제시
- 복합 문항을 별도 10개 풀로 두고 매판 출제 비율을 정확히 20%로 정함
- `13일 출장 / 13일 휴가 / 15일 출장`은 반복값의 교집합으로 답이 드러난다고 지적
- 모든 보기의 값을 다르게 두는 대안도 한 정보만 알면 정답이 확정된다고 다시 지적
- 두 정보가 각각 두 번 나오는 복합 문항 전용 4지선다를 선택하고 구현 계획을 승인
- 실제 커밋 메시지와 PR 본문을 검토하고 커밋·PR 생성을 각각 승인

---

### L-067 · 2026-08-07 · 코드

|      | |
| ---- | - |
| 도구 | Claude Code (claude-sonnet-5) |
| 담당 | 공동 |
| 목적 | 지인 대상 테스트에서 목표·조작법 설명 없이 시작해도 알 수 있도록, 단색 임시 시작 화면 추가 |
| 반영 | 부분 수정 |

**프롬프트** — `ai-log/raw/2026-08-07.md` 20:57~21:01

> 지인 대상 테스트를 위해 메인화면 생성. 임시이기 때문에 꾸미지 말고 단색으로 구성. 단, 목표와 조작법은 쉽게 설명.

> 제목은 Busy Boy. 그리고 아직 머지되지 않아서 지금은 없지만, 좌상단에 게이지가 2개 있는데, 그중 왼쪽 거는 타이머(비율만 표시)고, 오른쪽 거는 부스터 게이지야. 게이지 사용은 W(유지). 부스터는 가속 + 행인으로부터 무적.

> 코드는 건들지 말고 메인 화면 설명만 넣어. 지금 없는 기능은 머지가 아직 안 된 것 뿐이니까.

> - 게임이름: Busy Man으로 수정
> - 다시시작 설명: 괄호 부분 제거

**산출물** — 커밋 `15bab51`

- `makeStartScreen()` 추가 — 단색 배경, 제목·목표·조작법 안내, "시작하기" 버튼
- 게임 부트스트랩(콘텐츠 fetch, 폰·퀴즈 마운트, world 생성, 루프)을 `startGame()`으로
  감싸 버튼을 누르기 전에는 실행되지 않게 게이팅 — `quiz.js`가 마운트 즉시 자체
  타이머로 질문 주기를 세기 시작하는 것을 확인하고 반영한 판단
- 부스터(W)·게이지·다시 시작(R)은 아직 병합 전 기능이라 문구만 넣고 실제 키
  바인딩은 만들지 않음

**사람이 한 것**

- 제목을 Busy Boy → Busy Man으로 정정
- R 재시작을 실제로 동작하게 구현한 것을 되돌리라고 지시 — "코드는 건들지 말고
  문구만" 넣으라고 범위를 명시적으로 좁혀, 아직 병합되지 않은 기능을 앞서
  구현하지 않도록 막음
- 안내 문구의 괄호 설명("도착 후")을 제거해 문구를 간결화
- 좌상단 두 게이지의 의미(타이머 비율/부스터)와 W 조작을 직접 설명해 문구
  정확도를 확보 — 이 시점에는 아직 병합 전이라 AI가 직접 확인할 수 없던 정보

---

### L-068 · 2026-08-07 · 코드

|      | |
| ---- | - |
| 도구 | Claude Code (claude-sonnet-5) |
| 담당 | 공동 |
| 목적 | main에 그 사이 반영된 기능(부스터·시간초과·R 재시작 모달)을 병합하고, 지인 테스트 중 발견한 재시작 관련 버그 2건을 조사·수정 |
| 반영 | 그대로 |

**프롬프트** — `ai-log/raw/2026-08-07.md` 21:25

> main pull로 최신화 후 커밋. 추가로, 일부 버그 수정
>
> - 현재는 게임 진행 중 다시 시작하면, 더 이상 퀴즈 모달창이 뜨지 않음.
> - 게임 다시 시작 시, 부스터 게이지 초기화

**산출물** — 커밋 `bc83307`(병합), `f8a3fed`(버그 수정), PR #21,
`docs/ai-log/specs/2026-08-07-merge-and-restart-bugfix-plan.md`

- 서브에이전트 2개를 병렬로 띄워 origin/main의 부스터·재시작 흐름과, 퀴즈 재시작
  버그의 근본 원인을 각각 조사
- 퀴즈 모달 버그는 이미 `origin/main` 커밋 `44c4e48`(`quiz.destroy()` 후
  `quiz.reset()`이 죽은 rAF 루프를 재가동하지 못하던 것을, 재시작 시 인스턴스를
  통째로 새로 만드는 방식으로 고친 것)로 해결되어 있음을 확인 — 병합만으로
  해소되어 별도 코드 수정 없음
- 부스터 게이지는 `world.reset()`·`quiz.destroy/mount` 어디에서도 초기화되지
  않음을 확인하고, `main.js`의 `restart()`에 `state.gauge = 0` 추가
- `src/main.js` 병합 충돌을 origin 구현 위에 시작 화면 래핑만 재적용하는 방식으로
  해결(다른 충돌 파일 없음)
- PR #21 생성(본문 AI 작성, 사용자 승인)

**사람이 한 것**

- 버그 2건을 정확히 지목해 조사 범위를 좁혀 줌
- `state.gauge = 0` 쓰기가 `AGENTS.md` §2의 "A는 W 소비 외에 여기 쓰지 않는다"
  관례 밖의 새 쓰기라는 점을 AI가 조사해 보고하자, 재시작 초기화는 그 정답/오답
  판정과는 별개 동작이라고 보고 진행을 승인 — 이후 B와 조율이 필요하면 알리기로
  하고 그 사실을 커밋 메시지에 남기도록 함
- 병합 커밋과 버그 수정 커밋을 분리하는 계획을 승인(ExitPlanMode) — 실행 중 게이지
  수정이 병합 커밋에 섞여 들어간 것은 AI가 스스로 발견해 amend로 분리함
- "커밋"·"pr" 각 한 마디로 실행 승인

---

### L-069 · 2026-08-08 · 코드

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (claude-sonnet-5)                                                      |
| 담당 | A                                                                                  |
| 목적 | docs/map/맵분위기.png의 배치 아이디어를 따라 빈 잔디 배경을 도시 에셋(건물·가로등·화단·나무)으로 꾸미고, 신호등 구역 벽을 제거 |
| 반영 | 그대로                                                                             |

**프롬프트** — `ai-log/raw/2026-08-08.md` 00:47

> @docs/map/맵분위기.png 를 참고하여 현재 게임의 배경인 잔디밭 구역을 @assets/map\ 의 에셋를 활용하여 꾸미세요.
> - 이미 설치해둔 나무의 위치를 변경하거 제거해도 됩니다.
> - 도로 UI를 사용할 경우, 게임의 이벤트인 신호등(T)와 이어지는 것처럼 표현해도 됩니다.(현재 신호등 구역에 있는 벽은 제거해 주세요)
> - 단, @assets/map/fence.glb @assets/map/fence2.glb @assets/map/fence3.glb @assets/map/barricade.glb 은 배경을 꾸밀 때 절대 사용하지 마세요.

**산출물** — 커밋 `7dc1a16`

- `src/world.js` — 도로를 따라 office1~3.glb 건물, light.glb 가로등, flowerBed1~3.glb 화단,
  tree/tree2~4.glb 나무를 배치하는 `decorateGrass` 신설. 기존 `scatterTrees`/`loadTreeUnit`을
  통합해 재사용 가능한 `loadModelUnit`으로 일반화
- 신호등(T) 구역 벽 제거, 쓰이지 않게 된 벽 색 상수(`wall`·`tWall`·`tFloor` 등) 정리
- `assets/map/{office1,office2,office3,light,flowerBed1,flowerBed2,flowerBed3,tree2,tree3,tree4}.glb`,
  `docs/map/맵분위기.png` — 이 커밋에서 처음 커밋됨(§4 출처 표 참고)

**사람이 한 것**

- 참고 이미지(맵분위기.png)와 사용할 에셋 폴더를 지정해 방향을 좁힘
- 기존 나무 배치는 이동·제거해도 된다고 재량을 열어 둠
- 신호등 구역 벽 제거를 명시적으로 지시
- fence·fence2·fence3·barricade는 배경 장식에 쓰지 말라는 제약을 못박음(도로 연석 전용으로 남게 함)
- 브라우저로 렌더링 결과를 스크린샷 확인한 뒤 승인

---

### L-070 · 2026-08-08 · 코드

|      |                                                                |
| ---- | -------------------------------------------------------------- |
| 도구 | Claude Code (claude-opus-4-8)                                  |
| 담당 | A                                                              |
| 목적 | L-069에서 배치한 가로등이 시각적으로 과하게 커 보여 크기를 축소 |
| 반영 | 그대로                                                          |

**프롬프트** — `ai-log/raw/2026-08-08.md` 01:15

> 가로등이 너무 커요. 지금의 절반정도로 줄여주세요

**산출물** — 커밋 `7dc1a16` (L-069와 같은 커밋)

- `src/world.js` — 가로등 목표 높이 상수를 4.5 → 2.25로 축소(`decorateGrass` 내 가로등
  배치 스케일 계산)

**사람이 한 것**

- 가로등이 과하다는 시각적 문제를 지적하고 "절반 정도로"라는 구체적인 축소 비율을 지시
- 브라우저 스크린샷으로 축소 결과를 확인한 뒤 승인

---

### L-071 · 2026-08-08 · 코드

|      |                                                                                     |
| ---- | ----------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                       |
| 담당 | A                                                                                   |
| 목적 | `docs/map/맵 시간 배분.md`의 유효 9경로 중 다수에서 B7 이후 휴대폰 네비게이션 안내가 멈추는 원인을 분석하고 근본 수정 |
| 반영 | 그대로                                                                              |

**프롬프트** — `ai-log/raw/2026-08-08.md` 11:18

> @"docs/map/맵 시간 배분.md" 의 유효한 9개의 루트 중 5, 6, 9번 루트를 제외한 모든 루트들이
> B7부터 휴대폰 네비게이션 안내가 나오지 않습니다. 무엇이 문제인지 분석해주세요.

같은 시각에 "6, 7, 9번 루트를 제외한" 버전도 raw에 남아 있다 — 사용자가 직접 재현하며
정확한 제외 목록을 정정한 것으로 보인다(뒤에 온 "5, 6, 9번"이 실제 조사 기준).

**산출물** — 커밋 `9880d76`

`src/world.js` `update()`의 안내 큐 전진 판정에서 `prevS < front.s` 조건을 제거하고
`s >= front.s`만 보도록 수정(8줄).

브라우저에서 실제 게임을 구동해(디버그 훅으로 `world.update()`를 직접 호출하며 자동
조향 오토파일럿을 임시로 붙여 재현) route 1·7을 실행한 결과, `N4`처럼 실제로 꺾이는
갈림길 바로 뒤에 다음 안내(`T4` 신호등)가 `nodeS(i-1)+1` 클램프로 1유닛 간격까지 붙어
있으면, 코너를 도는 동안 `s`가 거의 안 나가다가 한 프레임에 몰아서 전진할 때 두 안내의
`s`를 동시에 넘는 경우가 있음을 확인했다. 이 경우 앞엣것만 `resolved`되고 뒤엣것은
`prevS`가 이미 자기 `s`를 지나친 채로 `front`가 되어 `prevS < front.s`를 다시는
만족하지 못한다 — 안내 큐가 거기서 통째로 멈춘다(B7·J3·T5 등 이후 전부 무음). 5·6·9번
경로만 멀쩡했던 이유는 그 세 경로만 `N4`를 직진으로 통과하거나(5·6) `N4` 자체를
지나지 않아서(9) 이 경합이 애초에 발생하지 않기 때문이다.

이 버그 계열은 `L-054`(2026-08-07)가 신호등/공사장 안내에 `+1` 클램프를 넣어 한 번
막았던 바로 그 종류의 정지였다 — 이번엔 "코너를 도는 중 한 프레임이 두 안내를 동시에
건너뛰는" 경로로 같은 증상이 재발했다. `prevS <` 요구를 아예 없애 재발 가능성 자체를
차단했다.

**사람이 한 것**

- 실제 플레이에서 관찰한 증상을 두 차례 보고하며 제외 루트 목록을 정정("6,7,9번
  제외" → "5,6,9번 제외") — 최종 목록이 원인 분석의 실질적 단서가 됨(N4를 직진
  통과/미경유하는 경로만 정상이라는 패턴)
- 분석·수정·브라우저 재검증을 확인한 뒤 커밋과 이 로그 기록을 지시

---

### L-072 · 2026-08-08 · 코드

|      |                                                                          |
| ---- | ------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                            |
| 담당 | A                                                                        |
| 목적 | 도착 지점의 "도착" 기둥·텍스트 배너를 실제 회사 건물(myCompany.glb)로 교체 |
| 반영 | 그대로                                                                   |

**프롬프트** — `ai-log/raw/2026-08-08.md` 15:55, 15:56(색상 참고 추가된 최종본)

> 현재 도착지의 "도착" 깃발을 @assets/map/myCompany.glb 로 바꿔주세요.
> - 바닥의 초록색 선을 그대로 유지해 주세요.
> - 초록색 선 뒤로 길을 0.5초 정도 연장해서 그 길 끝에 에셋을 설치해주세요.
> - 게임이 끝나는 것은 여전히 초록색 선에 도착했을 때입니다.(건물에 닿아야 끝나는 거 아님!)
> - 도착지가 총 두 곳이므로 둘 다 설치해야 합니다.
> - 에셋의 색은 @docs/map/맵분위기.png 를 참고하여 추가하세요

**산출물** — 커밋 `e9e564f`

`src/world.js`의 `buildGoalMarker`를 재작성: 초록 결승선(위치·색)과 실제 도착 판정
(`s >= totalLength - 1`)은 그대로 두고, 결승선 뒤로 `SPEED*0.5`(약 3유닛)만큼 도로를
같은 줄무늬 재질로 연장한 뒤 그 끝에 `myCompany.glb`를 배치. 기존 "도착" 기둥·텍스트
배너 코드(`goalBannerTexture` 포함)는 제거. 노드 색은 `docs/map/맵분위기.png` 팔레트와
office/tree 배색(`COLOR.bldgWall`·`bldgRoof`·`bldgWin`·`treeLeaf` 등)을 재사용해 새
팔레트를 만들지 않았다. `buildGoalMarker`가 원래도 "마지막 웨이포인트 좌표"만 보고
동작하는 구조라, 경로가 END2/END3 중 어느 쪽으로 끝나든 분기 없이 이 코드 하나로
두 도착지 모두 커버된다 — 요구사항의 "도착지 두 곳 모두"를 새 분기 없이 만족.

실제 도착까지 자동 주행해 눈으로 확인하지는 못했다(질의응답형 3D 러닝 게임이라
자동 주행 테스트 비용이 큼) — 로컬 서버로 씬 초기화까지는 구동해 `myCompany.glb`
로드 실패·콘솔 에러가 없음만 확인했고, 실제 스케일·방향 확인은 사용자 플레이테스트로
남겼다.

**사람이 한 것**

- 유지할 것(초록 결승선 위치·색, 도착 판정 지점)과 바꿀 것(기둥·배너 → 건물, 0.5초
  연장)을 조건 4개로 직접 명시. 색상은 참고 이미지(`맵분위기.png`)로 지정
- "건물에 닿아야 끝나는 거 아님!"이라고 판정 지점을 재확인해, 장식(건물)과 판정
  (결승선)을 코드에서 분리하도록 못박음
- 후속 질문("건물을 더 뒤로 보내려면 어떤 변수?")으로 `GOAL_EXT_SEC` 하나로 결승선→
  건물 거리가 정해지는지 확인 — 코드 변경 없이 위치
- "브랜치 바꿔서 커밋해줘" 지시 과정에서 세션과 무관한 `src/main.js`·`src/quiz.js`
  변경이 워킹 트리에 섞여 있는 것을 에이전트가 보고하자, `quiz.js`(B 소유 파일)는
  빼고 `main.js`는 포함하도록 직접 범위를 정정 — `feat/world-goal-building` 브랜치에
  world.js/에셋 커밋(`e9e564f`)과 main.js 커밋(`d501544`, 사람이 직접 작성한 결과
  화면 문구 변경이라 이 로그 대상 아님)을 분리해서 커밋

---

### L-073 · 2026-08-08 · 코드

|      |                                                              |
| ---- | ------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                |
| 담당 | A                                                             |
| 목적 | 좌상단 시간·부스터 게이지 막대에 아이콘(timer.png·booster.png)을 달아 뭘 나타내는 게이지인지 한눈에 구분되게 함 |
| 반영 | 부분 수정                                                     |

**프롬프트** — `ai-log/raw/2026-08-08.md` 18:04, 18:16, 18:26, 18:32, 18:36, 18:42

> @docs/map/image.png 참고해서 시간, 부스터 게이지를 꾸며주세요. 아이콘은
> @assets/map/gauge/timer.png, @assets/map/gauge/booster.png 사용하세요.

이후 다섯 차례 더 방향을 좁히는 대화가 이어졌다 — 상세는 "사람이 한 것" 참조.

**산출물** — 커밋 `854d0eb`

`src/main.js`의 `makeGaugeWrap`/`makeTimeBar`/`makeBoostBar`를 고쳐 각 게이지 막대
왼쪽 끝에 아이콘을 절반쯤 겹쳐 얹었다(`GAUGE_ICON_SIZE=34`, `GAUGE_ICON_OVERLAP=14`).
`timer.png`는 이미지 자체에 원판이 있어 그대로 쓰고, `booster.png`는 배경이 투명해
원형 배지(`#1B1D21`)를 씌워 크기감을 맞췄다. 막대 굵기만 20px → 25px로 키웠고,
길이(`min(320px,66vw)`)·아이콘 겹침 구조는 최초 형태 그대로다.

**사람이 한 것**

- 첫 요청은 `docs/map/image.png`(둥근 배경 안에 아이콘이 막대와 겹친 목업)를
  참고 자료로 지정 — 에이전트가 이걸 보고 아이콘-막대 겹침 레이아웃(v1)을 처음 만듦
- `src/image.png`(굵은 검정 테두리의 큰 흰 캡슐이 아이콘 원과 막대를 통째로 감싸는
  스케치)를 새로 제시하며 "이모티콘이랑 게이지바를 큰 흰색 바가 감싸고 있는
  형태로" 다시 요구 — 에이전트가 캡슐형(v2, 아이콘 36px·막대 굵기 자체도 확대)으로
  전면 재작성했으나, 곧바로 "오잉.. 아니야!"로 기각하고 "처음에 해줬던 거에서 흰
  배경만" 입히도록 범위를 좁힘(v3: v1 크기 유지 + 흰 배경 래퍼만 추가)
- "막대 높이만 커지면 좋겠다"는 질문에는 코드를 바로 고치지 않고 어떤 값
  (`makeGaugeWrap`의 `bar` `height:20px`)을 건드리면 되는지만 먼저 확인
- 곧이어 "그냥 처음으로 돌려줘 (…) 높이만 조금 키워줘"로 v3의 흰 배경 래퍼까지
  완전히 걷어내고 v1 레이아웃 + 높이만 20→25px로 확정(v4, 최종)
- "게이지 색을 바꾸고 싶으면?" 질문도 코드 변경 없이 위치(`fill.style.background`
  두 곳)만 먼저 확인
- 매 단계 실제 브라우저 스크린샷으로 결과를 검증한 뒤 다음 지시로 넘어감

---

### L-074 · 2026-08-08 · 코드

|      |                                                                |
| ---- | -------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                  |
| 담당 | A                                                                |
| 목적 | 신호등(T) 구역에서 안전 차선을 놓쳤을 때 끌려가는 구간이 펜스 없는 범위를 넘지 않게 하고, 그 경계에 실제 신호등 모델을 세운다 |
| 반영 | 그대로                                                          |

**프롬프트** — `ai-log/raw/2026-08-08.md` 19:26, 19:59

> 지금 신호등 endS가 너무 뒤야. 그래서 길을 잘 못 들었을 때 행인들한테 끌려가는
> 구간이 너무 길어. 옆에 팬스 없는데까지만 끌려가야해

> - 신호등 구역 바닥을 @docs/map/image.png 을 참고하여 변경해주세요.
> - 신호등 구역 끝지점의 우측 펜스 바로 앞에 @assets/map/trafficLight.glb 를
>   설치해 주세요. 높이는 현재 light에셋의 크기보다 1.3배 크게 해주세요.

**산출물** — 커밋 `74e70c8`

`src/world.js` 변경:

- `createEvents`의 T 이벤트 `endS`를 `TL_HALF_SEC` 대신 `ZONE_SEC` 기반
  (`zoneHalf`)으로 재계산 — `expandZones`가 펜스(fence2.glb)를 세우지 않는
  경계와 정확히 같은 좌표라, 안전 차선을 놓쳐도 딱 그 경계까지만 끌려간다
- 그 경계(`endS`)의 우측 펜스 바로 앞에 `trafficLight.glb`를 세우는
  `buildTrafficLightGroup` 추가 — fence/barricade와 같은 비동기 로드 패턴,
  높이는 가로등(`light.glb`, `LIGHT_TARGET_HEIGHT=2.25`로 상수화)의 1.3배
- `crosswalkTexture()`를 참고 이미지대로 검정 바탕 흰 줄 4개짜리 사다리
  무늬로 재작성, `buildRoadStrip`에서 T 구간만 `vRepeat=1` 고정(구간 길이에
  따라 무늬가 반복·왜곡되던 것을 막음)

같은 커밋에는 이 작업과 무관하게 **사람이 세션 중 직접 편집한 변경**도 함께
들어갔다 — `assets/map/myCompany.glb`를 `myCompany2.glb`로 교체하고
`MYCOMPANY_TARGET_HEIGHT`(9→20)·`GOAL_EXT_SEC`(0.5→0.8초)를 재조정한 것.
에이전트가 지시받아 만든 산출물이 아니라 이 엔트리의 "산출물"에는 포함하지
않지만, 커밋 해시가 같아 여기 밝혀 둔다.

실제 신호등 구역까지 자동 주행해 눈으로 확인하지는 못했다 — 로컬 서버로 씬
초기화까지 콘솔 에러 없음만 확인했고, 배치·바닥 무늬가 의도대로인지는
사용자 플레이테스트로 남겼다.

**사람이 한 것**

- "옆에 팬스 없는데까지만"이라는 한 문장으로 `endS`를 고칠 기준(펜스 유무)을
  직접 제시 — 에이전트가 `expandZones`의 펜스 경계 계산(`ZONE_SEC` 기반)과
  같은 값을 재사용하도록 스스로 찾아 연결
- `docs/map/image.png`(검정 바탕 흰 줄 4개 스케치)를 바닥 무늬의 유일한 근거로
  제시 — 픽셀 비율 해석은 에이전트가 "여백까지 포함해 5칸 검정·4칸 흰색이
  전부 같은 두께"로 단순화해 판단
- "현재 light에셋의 크기보다 1.3배"라는 상대 배율로 목표 높이를 지정해,
  가로등 목표 높이(2.25)를 상수로 뽑아내는 리팩터링까지 이어짐
- 신호등 위치를 "구역 끝지점의 우측 펜스 바로 앞"으로 지정해 좌표계(끝=`endS`,
  우측=+side, 펜스 앞=`ROAD_HALF` 바깥)를 코드가 그대로 따르게 함

---

### L-075 · 2026-08-08 · 아트

|      |                                                                                     |
| ---- | ----------------------------------------------------------------------------------- |
| 도구 | OpenAI 내장 이미지 생성(모델 버전 미확인) — Codex에서 호출                         |
| 담당 | A                                                                                   |
| 목적 | 출근길 주행과 업무 문자 응답을 한눈에 전달하는 정식 시작 표지용 무문자 키아트 제작 |
| 반영 | 부분 수정                                                                           |

**프롬프트** — [`ai-log/raw/2026-08-08.md` 18:53~19:26](./ai-log/raw/2026-08-08.md)

**산출물** — 커밋 `6a36de9`

`assets/cover/cover-keyart.png`(1672×941). 판교형 오피스 거리, 출근 인파,
전경의 스마트폰을 스타일라이즈드 3D 장면으로 구성했다. 제목·부제·조작법은
이미지에 넣지 않고 HTML로 분리할 수 있도록 키아트 자체는 무문자로 만들었다.

**사람이 한 것**

- 표지용 생성 이미지 사용을 허용하면서 이미지 안에는 텍스트를 넣지 않는 기준을 직접 지정
- 실제 화면 스크린샷으로 키아트와 정보 패널의 조합을 검토하고, 판교 분위기가 나도록 방향을 교정
- 최종 반영 범위를 메인 화면으로 한정하고 다른 월드 시각 변경은 최신 `main` 상태로 유지하도록 결정

---

### L-076 · 2026-08-08 · 코드

|      |                                                                                  |
| ---- | -------------------------------------------------------------------------------- |
| 도구 | Codex (gpt-5.6-terra / gpt-5.6-sol)                                            |
| 담당 | A                                                                                |
| 목적 | 지인 테스트용 임시 화면을 게임의 상황·조작·시작 행동만 남긴 정식 표지로 교체     |
| 반영 | 부분 수정                                                                        |

**프롬프트** — [`ai-log/raw/2026-08-08.md` 18:53~19:18, 19:52, 22:12~22:46](./ai-log/raw/2026-08-08.md)

**산출물** — 커밋 `6a36de9`

- `src/main.js`: 무문자 키아트 위 단일 패널, 제목 색 분리, 두 줄 상황 문구,
  2열 조작표, 강조한 `출근 시작` 버튼, 반응형·키보드 포커스·감소 모션 처리
- `index.html`: 브라우저 제목을 `BUSY MAN`으로 통일
- `DESIGN.md`: 표지의 정보 위계·판교 테크 오피스 시각 언어·접근성·반응형 기준 기록
- `assets/README.md`: 생성 키아트의 용도와 텍스트 분리 원칙 기록

**사람이 한 것**

- 첫 화면에서 없앨 장식과 남길 정보(제목·상황·조작·시작 행동)를 스크린샷 피드백으로 결정
- 부제를 `업무 문자`와 자연스러운 도착 문장으로 직접 교정하고, 답장 범위를 `숫자키 (1~4)`로 확정
- 한글 글자 두께, 조작키 크기, 제목의 독자적 색·스타일, 시작 버튼의 크기·굵기를 반복 검토
- 최신 `main`에서 새 브랜치를 만들고 메인 화면 외 변경을 제외하도록 최종 커밋 범위를 지정

---

## §3. 폐기 기록

`반영: 폐기`인 항목을 여기에도 남긴다. **지우지 않는다.**
제출물 4번의 "AI 산출물의 검증" 절이 이 표에서 나온다.

| #     | 날짜       | 무엇을                                                                                                          | 왜 폐기했나                                                                                                                                                                                                                                                                                 |
| ----- | ---------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | 2026-08-03 | `docs/ai-log/prompts/` 층 (긴 세션을 주제별 파일로 분리 보관)                                                   | 훅이 모든 원문을 `raw/`에 자동 저장하게 되면서 존재 이유가 사라졌다. 훅이 못 잡는 세션도 `raw/`에 직접 붙여넣는 것으로 규정되어 있어 중복. 층을 셋(raw→prompts→엔트리)으로 두는 것은 이 규모에서 과투자                                                                                     |
| D-002 | 2026-08-03 | 프롬프트 **직전** 커밋 해시를 기록하는 단일 훅                                                                  | 알고 싶은 것은 "이 지시가 반영된 커밋"인데 직전 해시는 그 반대였다. 2단계(UserPromptSubmit + Stop) 구조로 대체                                                                                                                                                                              |
| D-003 | 2026-08-03 | 스택형 브랜치 토폴로지 (①→②→③ 중첩)                                                                             | 세 PR의 파일 집합이 겹치지 않아 스택이 기여하는 바가 없는데 병합 순서만 강제했다. 실제로 3-way 병합으로 충돌 0을 확인한 뒤 main 기준 독립 브랜치로 대체                                                                                                                                     |
| D-004 | 2026-08-03 | feature 단위 skill 세트                                                                                         | 파일 5개를 3.5일에 각자 한두 번 쓰고 끝나는 규모에서 반복 워크플로가 성립하지 않는다. 같은 효과(경계 고정)를 `AGENTS.md` 한 장이 훨씬 싸게 낸다. `/ai-log`만 예외로 남김                                                                                                                    |
| D-005 | 2026-08-03 | develop 브랜치 도입안                                                                                           | **GitHub Pages는 저장소당 게시 소스가 하나**라 develop을 별도 URL로 띄울 수 없다. develop의 핵심 이점("미리 보고 main으로")이 원천 봉쇄된다. 게다가 8/7 동결 전까지 main이 깨져도 손해가 없고, 충돌은 이미 파일 소유권(§1)이 막고 있다. 대가만 확실하다 — 머지 2단계, 핫픽스도 develop 경유 |
| D-006 | 2026-08-03 | GitHub Actions 배포 워크플로                                                                                    | `docs/`·`prototype/`을 웹에서 감출 수 있다는 이점이 있으나, 워크플로 자체가 새 실패 지점이 된다. `team-plan.html` §02가 "배포 파이프라인이 마지막 날 백지를 만든다"고 경고한 실패 모드다. branch 방식(`main`/root)으로 대체하고 루트 전체 노출은 수용                                       |
| D-007 | 2026-08-03 | three.js CDN + import map 로딩안                                                                                | 저장소가 가벼워지는 대신 **심사 당일 외부 서버 상태에 게임이 종속된다.** CDN이 죽거나 막히면 백지이고 그건 통제할 수 없다. 대회 요건이 "심사 종료 시점까지 접근 가능"인데 제3자에 의존하게 된다. `vendor/` 동봉으로 대체 — 대가는 파일 하나(~1MB)                                           |
| D-008 | 2026-08-04 | 갈림길 구조를 새로 설계하는 접근(deep-interview + v1~v6, `docs/ai-log/specs/2026-08-04-map-route-balancing.md`) | 기존 `docs/aaa.jpg` 맵 구조를 그대로 유지하는 방향(L-011)으로 선회하면서 이 접근 전체가 안 쓰이게 됐다. 시행착오 기록은 specs 문서에 그대로 보존                                                                                                                                            |
| D-009 | 2026-08-04 | 좌우 회전 가능한 열린 도시(자유 이동·건물 진입) 버전 `world.js`                                                 | "길" 구조·이동감만 검증하는 최소 버전(L-014)으로 전면 대체됐다. 노드 좌표(`N[]`)와 도로 그래프(`ROADS`)는 이후 버전에도 그대로 재사용됨                                                                                                                                                     |
| D-010 | 2026-08-05 | 시간(초) 기반 자동 좌표 생성(`durationToDistance`/`walkRoute`/`computeLayout`)으로 맵 좌표를 재구성하는 접근      | 장식용 노드가 그래프의 서로 다른 갈래에서 각자 좌표를 받으며 벽 구멍·건물 배치 어긋남이 반복됐다. 7개 유효 경로가 그래프상 순환을 이루고 있어 하나의 정적 2D 배치로 전부를 동시에 정확히 맞추는 것이 구조적으로 불가능하다는 결론에 이르러, 기존 노드 좌표에 배율만 곱하는 `MAP_SCALE` 방식(L-017)으로 대체했다. 커밋 전에 되돌려져 저장소에는 남지 않음 |
| D-011 | 2026-08-08 | 게이지를 굵은 검정 테두리의 큰 흰 캡슐이 아이콘 원과 진행 막대를 통째로 감싸는 형태(L-073)로 다시 만든 시도 — 사용자가 손그림 스케치(`src/image.png`)까지 제시해 지시한 디자인이었다 | 구현 직후 사용자가 "오잉.. 아니야!"로 기각하고 "처음에 해줬던 거(아이콘이 막대에 겹쳐 얹힌 모양)에서 흰 배경만" 입히도록 범위를 다시 좁혔다. 이후 그 흰 배경 래퍼조차 "그냥 처음으로 돌려줘"로 걷어내, 최종적으로 흰 배경 없는 최초 레이아웃(막대 굵기만 20→25px)으로 정착했다(커밋 `854d0eb`). 스케치를 문자 그대로 구현한 결과가 실제로 보니 원하던 것과 달랐던 경우 — 커밋 전에 되돌려져 저장소에는 남지 않음 |

## §4. 외부 에셋·오픈소스 출처

에셋을 **쓰는 순간** 추가한다. 나중에 몰아서 정리하지 않는다.
**라이선스를 확인할 수 없는 에셋은 사용하지 않는다.**

| 파일 경로 | 종류 | 출처 URL | 라이선스 | 저작자 표기 의무 | 비고 |
| --------- | ---- | -------- | -------- | ---------------- | ---- |
| `vendor/three.module.js` | 라이브러리(렌더러) | https://unpkg.com/three@0.160.0/build/three.module.js | MIT | 파일 내 라이선스 헤더 유지 | three.js r160 ES 모듈 빌드. `world.js`에서 상대경로로 import |
| `docs/질문_이벤트_휴대폰_UI_예시.png` | 이미지 (UI 목업) | AI 생성 — Gemini / Nano Banana | 외부 저작물 아님 (생성물). 도구 약관 확인 필요 | 없음 | 배치·비율만 명세 근거. 안의 글자와 번호는 생성 산물이라 명세가 아니다 → `docs/ui-spec.md` |
| `vendor/GLTFLoader.js` | 라이브러리(glTF 로더) | https://github.com/mrdoob/three.js/blob/r160/examples/jsm/loaders/GLTFLoader.js | MIT | 없음(파일 내 헤더 없음 — three.js 저장소 LICENSE로 커버) | three.module.js와 같은 r160. import를 `'three'` → `'./three.module.js'`로, `'../utils/BufferGeometryUtils.js'` → `'./BufferGeometryUtils.js'`로 상대경로 수정 |
| `vendor/BufferGeometryUtils.js` | 라이브러리(GLTFLoader 의존) | https://github.com/mrdoob/three.js/blob/r160/examples/jsm/utils/BufferGeometryUtils.js | MIT | 없음(파일 내 헤더 없음 — three.js 저장소 LICENSE로 커버) | GLTFLoader.js가 `toTrianglesDrawMode`를 쓰기 위해 요구. import만 상대경로로 수정 |
| `assets/map/fence.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성·치수 제안 참고(L-055) | 자체 제작, 라이선스 문제 없음 | 없음 | 일반 벽 1차 시도. 최종 채택은 fence2.glb — 이터레이션 기록으로 보존 |
| `assets/map/fence2.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성·치수 제안 참고(L-055) | 자체 제작, 라이선스 문제 없음 | 없음 | 최종 채택. 일반 도로·공사장(C) 벽 공용 |
| `assets/map/fence3.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성·치수 제안 참고(L-055) | 자체 제작, 라이선스 문제 없음 | 없음 | "턱" 크기 시도. 현재 코드에서는 안 씀 — 이터레이션 기록으로 보존 |
| `assets/map/tree.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성·치수 제안 참고(L-055) | 자체 제작, 라이선스 문제 없음 | 없음 | 가로수. 원본 재질에 색이 없어 코드에서 줄기/잎별로 새로 입힘 |
| `assets/map/barricade.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성·치수 제안 참고(L-055) | 자체 제작, 라이선스 문제 없음 | 없음 | 공사장 바리게이트. 큐브 이름(검정/주황/하양) 기준으로 코드에서 색 입힘 |
| `assets/map/office1.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 사무실 건물 1종. 배경 도시 건물로 사용 |
| `assets/map/office2.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 사무실 건물 2종. 배경 도시 건물로 사용 |
| `assets/map/office3.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 사무실 건물 3종(간판·화분 포함). 배경 도시 건물로 사용 |
| `assets/map/light.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 가로등. 연석 옆에 배치(L-070에서 크기 축소) |
| `assets/map/flowerBed1.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 화단 1종 |
| `assets/map/flowerBed2.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 화단 2종 |
| `assets/map/flowerBed3.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 화단 3종 |
| `assets/map/tree2.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 나무 변종 2종(밑동 포함). tree.glb와 섞어 배치 |
| `assets/map/tree3.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 나무 변종 3종. tree.glb와 섞어 배치 |
| `assets/map/tree4.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-069) | 자체 제작, 라이선스 문제 없음 | 없음 | 나무 변종 4종. tree.glb와 섞어 배치 |
| `docs/map/맵분위기.png` | 이미지 (배치 참고 목업) | AI 생성 — GPT 이미지 생성 | 외부 저작물 아님(생성물). 도구 약관 확인 필요 | 없음 | 건물·가로등·화단·나무 배치 아이디어 참고용(L-069) |
| `assets/map/myCompany.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-072) | 자체 제작, 라이선스 문제 없음 | 없음 | **L-074에서 `myCompany2.glb`로 교체되며 저장소에서 삭제됨** — 기록은 지우지 않는다 |
| `assets/map/gauge/timer.png` | 이미지(아이콘) | 직접 제작(L-073) | 자체 제작, 라이선스 문제 없음 | 없음 | 시간 게이지 아이콘 |
| `assets/map/gauge/booster.png` | 이미지(아이콘) | 직접 제작(L-073) | 자체 제작, 라이선스 문제 없음 | 없음 | 부스터 게이지 아이콘 |
| `assets/map/myCompany2.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-074) | 자체 제작, 라이선스 문제 없음 | 없음 | 도착 지점 회사 건물 — `myCompany.glb`를 대체(사람이 직접 교체, L-074 참고) |
| `assets/map/trafficLight.glb` | 3D 모델(Blockbench) | 직접 제작 — 형태·치수는 GPT 이미지 생성 참고(L-074) | 자체 제작, 라이선스 문제 없음 | 없음 | 신호등 구역 끝 우측 펜스 앞에 설치 |
| `assets/cover/cover-keyart.png` | 이미지(정식 표지 키아트) | AI 생성 — OpenAI 내장 이미지 생성(L-075) | 외부 저작물 아님(생성물). 도구 약관 확인 필요 | 없음 | 출근길·스마트폰을 담은 무문자 스타일라이즈드 3D 이미지. 텍스트는 `src/main.js`에서 별도 렌더링 |

> 위 이미지는 게임에 들어가는 에셋이 아니라 **기획 단계의 참조 목업**이다. 그래도 여기 적는 것은
> §4가 "쓰는 순간" 적으라고 정하고 있고, 제출물 4번이 AI 산출물의 출처를 요구하기 때문이다.
> 같은 날 같은 도구로 뽑은 배치 예시 이미지가 한 장 더 있었으나 **저장소에 넣지 않았다** —
> 실시간 방식 안의 그림이라 모달로 가기로 한 뒤에는 오해만 만든다.

## §5. 게임 내 AI 기능 — 미탑재 판단

제출물 4번의 "게임 내 AI 기능" 절이 여기서 나온다.

**판단: 런타임 AI를 탑재하지 않는다.**

근거

- 실질 개발 기간이 3.5일(8/3~8/6)이고 버퍼가 없다 — `team-plan.html` §01
- 심사자가 별도 API 키·계정·결제 없이 플레이할 수 있어야 한다 — `대회_정보.md` 제출물 1번
- 유료 API 사용료는 참가자 부담이며, 심사 종료 시점까지 링크가 살아 있어야 한다
- 대신 **콘텐츠 생성(대화 로그·질문·정답·함정 단서)에 AI를 쓰고 정적 JSON으로 굽는다.**
  플레이 경험에는 AI 산출물이 그대로 들어가되 런타임 의존은 없다

_(판단이 바뀌면 지우지 말고 여기에 이어서 적는다.)_
