# AI 활용 로그

제출물 4번(AI 활용 기술 문서)의 **유일한 재료.** 작성 규칙은 `ai_log_guidelines.md`를 따른다.
갱신은 `/ai-log`로 한다. 직접 고쳐도 되지만 §1 색인과 §2 본문을 항상 함께 고친다.

- 최종 갱신: 2026-08-05
- 누적 항목 수: 15

> 이 파일은 **누적**이다. 기존 항목을 지우거나 요약해서 줄이지 않는다.
> 폐기된 산출물도 지우지 않고 `반영`을 `폐기`로 바꾼다.

---

## §0. 사용 도구 목록

새 도구를 처음 쓴 날 행을 추가한다. 모델·버전까지 적는다.

| 도구        | 버전·형태                                    | 최초 사용  | 주 용도                                           |
| ----------- | -------------------------------------------- | ---------- | ------------------------------------------------- |
| Claude Code | Claude Opus 5                                | 2026-08-03 | 저장소 초기화, 규약 문서, 로깅 자동화             |
| Claude Code | Claude Sonnet 5                              | 2026-08-04 | 맵 밸런싱, world.js 구현(길·건물 게이트), 커밋/PR |
| Gemini      | Nano Banana (이미지 생성) — 모델 버전 미확인 | 2026-08-04 | 질문 폰 UI 목업 이미지                            |

## §1. 엔트리 색인

§2와 **같은 순서로** 행을 추가한다. 색인과 본문이 어긋나면 안 된다.

| #     | 날짜       | 영역        | 도구        | 담당 | 반영        | 한 줄 요약                                                                     |
| ----- | ---------- | ----------- | ----------- | ---- | ----------- | ------------------------------------------------------------------------------ |
| L-001 | 2026-08-03 | 기획        | Claude Code | 공동 | 부분 수정   | 소크라테스식 문답으로 저장소 초기화 요구사항을 명세로 확정                     |
| L-002 | 2026-08-03 | 코드        | Claude Code | 공동 | 부분 수정   | 파일 소유 경계 스켈레톤과 Pages 진입점 생성                                    |
| L-003 | 2026-08-03 | 문서        | Claude Code | 공동 | 부분 수정   | AGENTS.md 정본 체계 수립, 저장소 밖 문서 이관·최신화                           |
| L-004 | 2026-08-03 | 도구·자동화 | Claude Code | 공동 | 부분 수정   | 프롬프트 원문 포획 훅과 /ai-log 스킬 구축                                      |
| L-005 | 2026-08-03 | 기획        | Claude Code | 공동 | 부분 수정   | 리뷰 지침·브랜치 전략·PR 규칙 요구사항을 명세로 확정                           |
| L-006 | 2026-08-03 | 도구·자동화 | Claude Code | 공동 | 부분 수정   | Copilot 리뷰 지침, PR 템플릿, pr-commit 스킬 구축                              |
| L-007 | 2026-08-03 | 문서        | Claude Code | 공동 | 부분 수정   | 브랜치·배포 결정 확정, 실제와 반대로 적힌 담당자 표기 정정                     |
| L-008 | 2026-08-04 | 문서        | Claude Code | 공동 | 부분 수정   | raw 보관 정책 확정, 훅 승인 안내, 명세·계획서 저장소 편입                      |
| L-009 | 2026-08-04 | 기획        | Claude Code | 공동 | 그대로      | 렌더러를 three.js(저장소 동봉)로 확정하고 문서에 반영                          |
| L-010 | 2026-08-04 | 레벨·밸런싱 | Claude Code | A    | 폐기        | 갈림길 구조를 새로 설계하는 접근(deep-interview + v1~v6) — 방향 전환으로 폐기  |
| L-011 | 2026-08-04 | 레벨·밸런싱 | Claude Code | A    | 부분 수정   | 기존 맵 구조 유지, 구간 시간 배정으로 유효 경로 7개 확정                       |
| L-012 | 2026-08-04 | 코드        | Claude Code | A    | 골격만 차용 | world.js 최초 구현(prototype 포팅) + 맵 구조 적용 범위·회전 여부 결정          |
| L-013 | 2026-08-04 | 코드        | Claude Code | A    | 폐기        | 좌우 회전 가능한 열린 도시(자유 이동·건물 진입) 버전 — 최소 버전으로 전면 대체 |
| L-014 | 2026-08-04 | 코드        | Claude Code | A    | 부분 수정   | "길" 구조·이동감만 검증하는 최소 버전 구현                                     |
| L-015 | 2026-08-05 | 코드        | Claude Code | A    | 부분 수정   | prototype 게이트를 재사용한 건물(게이트+상자) 구현                             |

**영역 분포** (균형 점검용 — `코드`·`아트`·`사운드`·`레벨·밸런싱`이 절반 이상이어야 한다)

| 영역        | 건수 |
| ----------- | ---- |
| 기획        | 3    |
| 코드        | 5    |
| 아트        | 0    |
| 사운드      | 0    |
| 레벨·밸런싱 | 2    |
| 문서        | 3    |
| 도구·자동화 | 2    |

> 8/4~8/5에 `코드`(1→5)와 `레벨·밸런싱`(0→2) 기록이 쌓이면서 15건 중 7건(47%)으로
> 올라왔다. 여전히 절반에는 못 미치지만 방향은 맞다 — `phone.js`·`quiz.js` 쪽 기록이
> 더해지면 넘어갈 것으로 본다.

## §2. 엔트리

아래 형식으로 `L-001`부터 이어 붙인다.

> 엔트리가 가리키는 `ai-log/raw/YYYY-MM-DD.md`는 **저장소에 없다. 노션에 보관한다.**
> 각자 로컬에 쌓이고, 문서 작성 시 내려받는다. 사유는 `ai-log/raw/README.md` 참조.

<!--
### L-0NN · YYYY-MM-DD · 영역

| | |
|---|---|
| 도구 | |
| 담당 | |
| 목적 | |
| 반영 | 그대로 / 부분 수정 / 골격만 차용 / 폐기 |

**프롬프트**

> 원문 그대로. 길면 ai-log/raw/의 날짜·시각으로 가리킨다.

**산출물**

**사람이 한 것** — 비워두지 않는다.
-->

### L-001 · 2026-08-03 · 기획

|      |                                                                                  |
| ---- | -------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5) — deep-interview                                     |
| 담당 | 공동                                                                             |
| 목적 | 초기 상태 저장소를 무엇으로 채울지, 어디까지가 뼈대이고 어디부터가 구현인지 확정 |
| 반영 | 부분 수정                                                                        |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (2)

에이전트가 한 번에 한 질문씩 던지고 답할 때마다 모호성 점수를 다시 계산하는 방식.
목표·제약·완료판정·맥락 네 축으로 점수를 매겨 임계값(20%) 아래로 내려갈 때까지 반복했다.

**산출물** — `docs/ai-log/specs/2026-08-03-repo-init.md` (182줄)

4라운드 만에 모호성 100% → 14%. 6개 최상위 컴포넌트, 수용 기준 17개,
드러난 가정 6건과 그 해소 과정이 기록되어 있다.

특히 뒤집힌 가정 두 개가 이후 설계를 갈랐다.

- "훅이 로그를 자동으로 채운다" → 가이드라인이 요구하는 `사람이 한 것`·`반영 판정`은
  사후 판단이라 훅이 알 수 없다. 훅의 고유 가치는 **복원 불가능한 원문의 포획**뿐이다
- "Pages는 배포만 되면 된다" → 진짜 리스크는 하위 경로 ES 모듈 해석이다

**사람이 한 것**

- 스켈레톤이 구현 코드를 잠식할 것을 우려해 **"디렉토리 + 빈 파일만"으로 범위를 잘라냄.**
  에이전트가 추천한 "접점만 실물, 나머지는 배선"보다 한 단계 더 보수적인 선택
- 컴포넌트 토폴로지에 **Pages 배포 관통**을 6번째로 추가
- 팀원 도구 환경(Claude Code 주력 + Codex 대비) 정보 제공 → AGENTS.md 정본 체계의 근거
- 훅이 커밋 해시도 남기도록 지시

---

### L-002 · 2026-08-03 · 코드

|      |                                                                                |
| ---- | ------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Opus 5)                                                    |
| 담당 | 공동                                                                           |
| 목적 | 두 사람이 각자 AI로 코드를 뽑아도 서로 덮어쓰지 않도록 파일 경계를 실물로 고정 |
| 반영 | 부분 수정                                                                      |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (4)

**산출물** — 커밋 `0a27d14` `44afc53` `2f5c1c0` `229bca7` `9ce58cf`

- `.gitignore` — `.omc/` 제외. `docs/ai-log/raw/`의 날짜 파일은 노션 보관 결정에 따라 제외
- `.gitattributes` — LF 통일. 8/5 병합일에 줄바꿈 충돌이 진짜 충돌과 섞이는 것 방지
- `src/{state,main,world,phone,quiz}.js` — 담당자와 착수 전 확인사항만. **실코드 3줄**
- `index.html` — `./src/main.js`를 module로 로드해 Pages 하위 경로 검증

`docs/team-plan.html`이 문서로만 합의했던 파일 분담을 실제 파일로 옮긴 것이다.
그 문서 자체가 "문서로 합의하면 지켜지지 않지만, 파일이 이미 있으면 둘 다 그걸
import 한다"고 적고 있다.

**사람이 한 것**

- **담당 A/B가 실제와 반대임을 지적**하고 `docs/역할 분담.txt`를 근거로 제시.
  에이전트는 team-plan.html만 보고 반대로 배정했었다
- 반전 범위가 **구현 파일에만 해당**함을 다시 정정 — 배포·제출·영상·문서 담당은 원안이 맞다.
  이 정정으로 "8/10 제출 담당이 그날 부재"라는 모순이 해소됨
- 커밋 단위와 메시지 분량 기준 제시

---

### L-003 · 2026-08-03 · 문서

|      |                                                                                      |
| ---- | ------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Opus 5)                                                          |
| 담당 | 공동                                                                                 |
| 목적 | 저장소 밖에 있어 팀원이 볼 수 없던 규약·대회 요건을 안으로 들이고 정본을 하나로 정리 |
| 반영 | 부분 수정                                                                            |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (4)

**산출물** — 커밋 `17d9593` `1b77681`

- `AGENTS.md` — 정본. 파일 소유권, 접점 4개, 확정/미결 구분, 커밋 규약, 실행·배포 함정
- `CLAUDE.md` — `@AGENTS.md` 임포트만
- `docs/대회_정보.md` — 상위 디렉토리에서 이관 + 최신화

에이전트 판단으로 상위 `CLAUDE.md`를 그대로 옮기지 않고 **사실(대회 요건)과
행동 규약(AGENTS.md)으로 분리**했다. 매 세션 주입되는 파일에 긴 사실 문서를 넣으면
컨텍스트만 소모하기 때문. 최신화 과정에서 "컨셉별 폴더 분리" 구조 서술과
"아직 결정되지 않은 것" 6항목이 이미 낡았음이 드러나 확정 표로 교체했다.

**사람이 한 것**

- 이관 여부·위치를 애초에 **판단 대상으로 지정**해서 물음 (지시가 아니라 검토 요청)
- **`CLAUDE.md`를 요약본이 아니라 `@import`로 바꾸도록 지시** — 에이전트는 핵심 3줄을
  요약해 뒀는데, 그 요약본도 결국 두 번째 진실이 된다는 판단
- 상위 원본 2개 삭제 결정 (이관 누락 여부를 대조로 확인한 뒤 실행)

---

### L-004 · 2026-08-03 · 도구·자동화

|      |                                                                             |
| ---- | --------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                 |
| 담당 | 공동                                                                        |
| 목적 | 제출물 4번의 재료인 프롬프트 원문이 사후 복원 불가능하므로 그 포획을 자동화 |
| 반영 | 부분 수정                                                                   |

**프롬프트** — `ai-log/raw/2026-08-03.md` (1), (3), (6)

**산출물** — 커밋 `307c381`, `374b4e3`

`307c381` — 로깅 인프라

- `.claude/hooks/log-prompt.mjs` — 2단계 훅.
  `UserPromptSubmit`이 원문을 적고 `Stop`이 그 사이 늘어난 커밋을 채운다
- `.claude/settings.json` — 훅 등록 (저장소 공유)
- `.claude/skills/ai-log/SKILL.md` — raw와 git 기록을 엔트리로 승격하는 절차
- `docs/ai_log_guidelines.md` — 상위에서 이관 + 원자재/엔트리 2층 구조 반영
- `docs/ai_usage_log.md` — 이 파일의 골격

`374b4e3` — 명세 보존 규칙. 첫 기록을 쓰다가 필요성이 드러나 뒤늦게 추가됐다

- `docs/ai-log/specs/` 신설 → **원자재 / 명세 / 엔트리 3층**으로 확장
- `ai_log_guidelines.md` §2에 트리거 추가, §4를 "원문과 중간 산출물 취급"으로 넓힘.
  절 번호를 재배열하지 않아 기존 참조가 그대로 유효하다
- `SKILL.md` 3단계에 `.omc/`의 명세를 저장소로 옮기는 절차 추가

> 이 로그 파일만 고치는 커밋은 여기 나열하지 않는다 — 갱신할 때마다 늘어나 끝이 없다.
> **인프라나 지침을 함께 고친 커밋만 적는다.**

자동화 범위를 원문 포획으로 한정한 것이 핵심 판단이다. 가이드라인이 요구하는
`사람이 한 것`·`반영` 필드는 사후 판단이라 훅이 알 수 없고, 반대로 원문은
훅만 알 수 있으며 그 순간이 지나면 복원되지 않는다.

검증: 커밋 2개 발생 / 커밋 없음 / 앞 항목 미훼손 / 깨진 JSON / 파일 부재 —
다섯 경우 모두 의도대로 동작하고 세션을 막지 않음(exit 0).

**사람이 한 것**

- **커밋 해시를 "직전"이 아니라 "반영된 커밋"으로 바꾸도록 지시.** 이 한 마디가
  단일 훅을 2단계 구조로 바꿨다. 프롬프트 시점에는 그 커밋이 아직 없기 때문
- **`SKILL.md`가 가이드라인 내용을 중복 서술함을 지적** → 규칙을 전부 참조로 돌리고
  절차만 남김
- **`prompts/` 층 폐지 결정** (§3 참조)
- 훅이 없는 도구(Codex)로 전환할 가능성을 상기시켜, 훅 없이도 성립하는 프로세스를 요구
- 명세 보존을 **일회성 조치가 아니라 지침의 규칙으로** 만들 것을 요구
  ("앞으로도 deep-interview 쓸 텐데, 이거도 지침에 포함해야 하는 거 아님?").
  에이전트는 명세 하나를 옮기는 것으로 끝내려 했다

---

### L-005 · 2026-08-03 · 기획

|      |                                                     |
| ---- | --------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5) — deep-interview        |
| 담당 | 공동                                                |
| 목적 | 리뷰 지침·브랜치 전략·PR 규칙을 코드 착수 전에 확정 |
| 반영 | 부분 수정                                           |

**프롬프트** — `ai-log/raw/2026-08-03.md` (8), (9)

**산출물** — `docs/ai-log/specs/2026-08-03-review-and-conventions.md` (186줄)

3라운드, 모호성 100% → 17.1%. 5개 컴포넌트.

**확인된 외부 사실 두 개가 결정을 갈랐다.**

- **GitHub Pages는 저장소당 게시 소스가 하나다.** develop을 별도 URL로 띄울 수 없으므로
  "develop에서 미리 보고 main으로" 흐름이 성립하지 않는다 → **develop 미도입**
- `.github/copilot-instructions.md`는 코드 리뷰가 읽으며 기본 활성이지만,
  **리뷰 언어 지정은 문서화되어 있지 않다** → 보장으로 취급하지 않고 실증 항목으로 남김

**사람이 한 것**

- 토폴로지를 3개 → 5개로 쪼개도록 지시. 브랜치 전략을 **"develop 여부"와 "Pages 배포 방식"**
  둘로 나누고 PR 템플릿을 별도 컴포넌트로 추가
- develop 미도입 결정. 동결 태그는 8/7로 미룸
- Pages를 branch 방식으로 선택 — **루트 전체가 웹에 노출된다는 것을 알고도 수용**.
  Actions는 실패 지점을 늘려 3.5일 일정에 맞지 않는다는 판단
- skill 범위를 "커밋 분할 → 메시지 → PR 본문 전 과정"으로 확정

---

### L-006 · 2026-08-03 · 도구·자동화

|      |                                                                                    |
| ---- | ---------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                        |
| 담당 | 공동                                                                               |
| 목적 | 둘 다 AI로 코드를 뽑으므로, 리뷰와 커밋·PR이 사람마다 다른 모양으로 쌓이지 않게 함 |
| 반영 | 부분 수정                                                                          |

**프롬프트** — `ai-log/raw/2026-08-03.md` (8), (9)

**산출물** — 커밋 `d79f68b`, `0bd956b`

- `.github/copilot-instructions.md` — 한국어 리뷰 지시 + 이 저장소에서 실제로 사고가 나는
  다섯 가지(소유 경계 침범, 접점 초과, 절대경로 모듈, 렌더러 미결 상태의 렌더 코드, 콘텐츠 하드코딩)
- `.github/pull_request_template.md` — 개요 / 변경 사항 / 체크리스트
- `.claude/skills/pr-commit/SKILL.md` (82줄) — 커밋 전 **소유 경계 침범 점검** 포함

**"지적하지 않아도 되는 것" 목록이 이 지침의 핵심이다.** 빈 스켈레톤, 번들러 부재,
포매터 부재가 전부 의도한 결정이라, 이걸 빼면 리뷰가 통째로 노이즈가 된다.

두 산출물 모두 규칙을 복사하지 않고 `AGENTS.md` §5를 참조만 한다. `ai-log` 스킬에서
중복을 걷어낸 선례를 따랐다.

**사람이 한 것**

- 리뷰를 **한국어로** 받을 것을 요구
- PR/commit 규칙을 skill로 만들 것을 요구
- PR 템플릿을 별도로 둘 것을 지시 — **독자가 다르다.** 템플릿은 사람이 GitHub UI에서,
  skill은 에이전트가 쓴다

---

### L-007 · 2026-08-03 · 문서

|      |                                                                               |
| ---- | ----------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                   |
| 담당 | 공동                                                                          |
| 목적 | 브랜치·배포 결정을 규약에 확정하고, 실제와 반대로 적힌 담당자 표기를 바로잡음 |
| 반영 | 부분 수정                                                                     |

**프롬프트** — `ai-log/raw/2026-08-03.md` (12), (13)

**산출물** — 커밋 `4048f95` `29613b1` `7cf7b0a` `c3543fd`

- **`team-plan.html`의 구현 담당 A/B 맞바꿈** (§03 카드·§04 일정). 그전까지는 `AGENTS.md`에
  경고를 달아 우회했는데, **팀원이 그 문서를 직접 열면 경고를 못 본다**
- develop 미도입 근거와 Pages 설정값을 `AGENTS.md`에 명시. `.nojekyll` 추가
- 문서를 실제로 고쳤으므로 `AGENTS.md`·`대회_정보.md`의 "라벨이 반대" 경고를 제거.
  경고가 남아 있으면 그것이 새 거짓이 된다

**사람이 한 것**

- `team-plan.html`을 **실제로 고치도록 지시** — 경고 우회로는 부족하다는 판단
- 반전 범위가 **구현 파일에만 해당**함을 짚음. 대외 산출물 담당은 원안이 맞다
- raw 원문의 저장소 공개를 **보류시킴.** 팀원과 상의 전이라는 이유

---

### L-008 · 2026-08-04 · 문서

|      |                                                                                         |
| ---- | --------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                                             |
| 담당 | 공동                                                                                    |
| 목적 | 보관 정책이 바뀐 뒤 따라오지 못한 문서를 실제와 일치시키고, 사라질 자산을 저장소로 들임 |
| 반영 | 부분 수정                                                                               |

**프롬프트** — `ai-log/raw/2026-08-03.md` 23:33 항목, `2026-08-04.md` (a), (b), (f)

**산출물** — 커밋 `0cbf10a` `dde0dfb` `50b6cb4`
및 `docs/ai-log/specs/2026-08-04-ai-log-policy-plan.md`

- **raw 보관 정책을 노션 수동 보관으로 확정.** 문서에는 "공개 여부 미결이라 잠시 보류"로
  적혀 있어 곧 커밋할 것으로 읽혔다. `raw/README.md`의 "왜 커밋하는가" 절은 정반대 내용이었다
- **훅 승인 절차 안내** — 저장소 훅은 승인 전까지 실행되지 않는데 그 사실이 어디에도 없었다.
  이번에 직접 겪은 문제다
- 두 번째 deep-interview 명세를 `.omc/`에서 저장소로 보존 (L-004에서 만든 규칙의 이행)
- 가이드라인의 `### 두 개의 층` 제목이 3행 표를 달고 있던 것도 함께 정정

**사람이 한 것**

- 보관 사유를 **"어투가 구어체라서"가 아니라 심사자 관점에서 성립하는 근거**로
  다시 쓰도록 요구 → "제출물의 원자재이지 소스가 아니다 / Pages가 루트를 서빙한다 /
  올린 뒤에는 정정할 수 없다"로 정리됨
- 노션 업로드 자동화를 **하지 않기로 결정** — 파일 일곱 개에 스크립트·토큰을 붙일 값이
  나오지 않는다는 판단
- 계획 문서를 저장소에 보존하도록 지시

---

### L-009 · 2026-08-04 · 기획

|      |                                                                      |
| ---- | -------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Opus 5)                                          |
| 담당 | 공동                                                                 |
| 목적 | 렌더러 미결이 `world.js` 착수를 규약상 막고 있어, 결정을 문서에 반영 |
| 반영 | 그대로                                                               |

**프롬프트** — `ai-log/raw/2026-08-04.md` (c), (d), (e)

> 이 지시는 `ExitPlanMode` 거부 시 전달되어 `UserPromptSubmit`을 거치지 않았다.
> **훅이 잡지 못하는 입력이 있다**는 것이 여기서 처음 드러났고, 같은 세션이라 대화에서
> 원문을 복원해 raw에 수동으로 채웠다. 선택지 응답도 마찬가지로 잡히지 않는다.

**산출물** — 커밋 `ced96f8`, `3014da1`

- `AGENTS.md` §3에서 렌더러를 **"열려 있음" → "확정"**으로 이동.
  남는 열린 항목은 **`content/day1.json` 스키마 하나**가 됐다
- §7의 "렌더러 결정 전에 렌더 코드 쓰기"를 **"CDN에서 three.js 불러오기"**로 교체.
  금지 항목이 사라진 것이 아니라 바뀐 것이다
- §1 소유권 표에 `vendor/` 행 추가 (공동 · 수정하지 않음)
- `copilot-instructions.md` 리뷰 포인트 교체, `대회_정보.md` 확정 표 갱신
- `src/world.js` 착수 주석 — 프로토타입은 Canvas 2D 자체 투영이라 **이식 대상이 아니라는 점**도 명시

**CDN 대신 저장소 동봉을 택한 것이 이 엔트리의 판단이다.** CDN은 심사 당일 외부 서버가
죽거나 막히면 게임이 백지가 되는데 그건 통제할 수 없다. `team-plan.html` §02가
"Vite를 끼우면 마지막 날 백지"라며 잘라낸 것과 같은 종류의 리스크이고, 대가는 파일 하나뿐이다.

**사람이 한 것**

- **three.js 최종 결정** (Canvas 2D와의 비교는 `renderer-comparison.html`에서 이미 이뤄짐)
- 동봉과 CDN의 차이를 **되물어 근거를 확인한 뒤** 동봉 채택. 에이전트 권고를 그대로 수용
- 범위가 작으니 진행 중이던 작업과 함께 처리하도록 지시

---

### L-010 · 2026-08-04 · 레벨·밸런싱

|      |                                                                                      |
| ---- | ------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5) — deep-interview                                       |
| 담당 | A                                                                                    |
| 목적 | 매 게임 랜덤 배정되는 루트가 시간상 균형 잡히도록, 맵의 갈림길 구조 자체를 새로 설계 |
| 반영 | 폐기                                                                                 |

**프롬프트** — `ai-log/raw/2026-08-04.md` 14:17~16:34 (7개 요청, deep-interview 1건 포함)

**산출물** — `docs/ai-log/specs/2026-08-04-map-route-balancing.md` (deep-interview 6라운드 +
v1~v6 시행착오 전문)

deep-interview로 시작해(모호도 15.75%로 임계값 통과) 손그림 그래프에서 균형 잡힌 조합을
골라내는 1차 설계를 했지만 38개 중 3~4개뿐이라 부족했다. 그래서 아예 갈림길 구조를
새로 설계하는 2차 접근(대칭 3레인, 3ⁿ 조합)으로 전환해 v1~v6까지 반복했다.

**사람이 한 것**

- 매 라운드 산출물을 실사용 관점에서 반려 — "완전히 같은 종류로 나오면 갈림길 만드는
  의미가 없다"(v1, 완전 대칭안 기각), "신호등이 너무 많다"(v3→v4), "회전했을 때 나오는
  길에 시간이 왜 없냐"(v4→v5), "회전 후 원래 길로 돌아오는 비용과, 직진도 건물까지
  걸어가는 접근 구간이 빠진 것 아니냐"(v5→v6) — 총 5차례 구체적 반례로 재설계를 유도
- 이후(18:19) 이 접근 전체를 접고 "기존 맵 구조를 그대로 쓴다"는 방향으로 전환 → L-011

---

### L-011 · 2026-08-04 · 레벨·밸런싱

|      |                                                                                                       |
| ---- | ----------------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                         |
| 담당 | A                                                                                                     |
| 목적 | 기존 맵(`docs/aaa.jpg`) 구조는 그대로 두고, 구간 시간만 배정해 어느 경로든 100초 내외가 되도록 밸런싱 |
| 반영 | 부분 수정                                                                                             |

**프롬프트** — `ai-log/raw/2026-08-04.md` 18:19, 18:35, 18:46, 19:16

> 현재 맵 구조는 그대로 유지하고, 시간을 조정해 어떤 경로를 가더라도 감속이 없다는
> 가정하에 약 100초 내외에 도착하도록 밸런싱한다. (…) S(이동 구간): 4~6초, T(신호등):
> 4초 고정, C(공사장): 3초 고정, B(건물): 10~15초

**산출물** — `docs/map/맵 시간 배분.md` (유효 경로 7개 + 구간별 확정 시간 + 제외된 경로와
그 사유)

**사람이 한 것**

- S/T/C/B 시간 배정 기준을 직접 제시해 계산 방향을 확정
- "모든 경우의 90~100초 사이에 도착하는 경로만 사용, 그 사이 값이 가장 많이 나오는
  시간 분배를 채택"이라는 최적화 기준을 직접 제시(정수계획법으로 풀도록 유도)
- "이미지 기호를 이용해서 정확한 경로를 정리해달라"고 요구해 채택/제외 양쪽 다 근거를
  남기게 함

---

### L-012 · 2026-08-04 · 코드

|      |                                                                                                     |
| ---- | --------------------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                                       |
| 담당 | A                                                                                                   |
| 목적 | `world.js` 최초 구현(prototype 포팅) 및 L-011의 맵 구조를 world.js에 어떻게 적용할지 범위·순서 결정 |
| 반영 | 골격만 차용 (코드 구조는 이후 전면 재작성됐지만 prototype 튜닝값은 계속 재사용됨)                   |

**프롬프트** — `ai-log/raw/2026-08-04.md` 13:27, 13:47, 13:57, 19:34, 19:45, 19:49, 20:09

**산출물** — 초기 `src/world.js`(복도 렌더·자동 전진·좌우 이동·보행자 충돌 감속),
`src/main.js` 연결

**사람이 한 것**

- "맵 구조만 넣는 건데 day1 스키마를 합의한 뒤에 해야 되냐"고 되물어, 맵 구조 적용은
  B(퀴즈 스키마) 담당과 무관하게 먼저 진행할 수 있다는 범위 판단을 이끌어냄
- 좌/우회전 카메라 전환을 되살리기로 결정 — `AGENTS.md` §3의 "확정 — 되돌리지 않는다"
  항목을 뒤집는 판단이라 에이전트가 먼저 확인을 구했고, 사람이 직접 승인

---

### L-013 · 2026-08-04 · 코드

|      |                                                                                           |
| ---- | ----------------------------------------------------------------------------------------- |
| 도구 | Claude Code (Claude Sonnet 5)                                                             |
| 담당 | A                                                                                         |
| 목적 | 좌우 회전이 가능한 열린 도시(자유 이동, 건물 내부 진입) 버전으로 `world.js`를 전면 재작성 |
| 반영 | 폐기                                                                                      |

**프롬프트** — `ai-log/raw/2026-08-04.md` 20:36, 21:38, 21:47

**산출물** — `N[]` 노드 좌표·`ROADS` 도로 그래프, 건물 충돌·시야 로직(이후 완전 재작성)

**사람이 한 것**

- "그냥 직진 코스만 나오는데"라는 버그 리포트로, 갈림길 판정이 "그 순간 누르고 있던
  키"에만 의존해 대부분 기본값(직진)으로 빠지던 결함을 발견하게 함
- "위치가 자꾸 바뀌고 건물 들어갈 때 시야가 이상하다" 리포트로, 건물 충돌 판정이
  z축(또는 x축) 하나만 겹쳐도 오탐하던 버그(먼 위치로 순간이동)를 발견하게 함
- 이후(22:51) 이 방향 자체를 접고 "길 구조·이동감만 검증하는 최소 버전"으로 전환 지시
  → L-014

---

### L-014 · 2026-08-04~2026-08-05 · 코드

|      |                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                                                                                                          |
| 담당 | A                                                                                                                                                      |
| 목적 | 게임성이 아니라 "길" 구조·이동감만 검증하는 최소 버전 구현 — 보행자·건물·이벤트 제거, 유효 경로 7개 중 무작위 1개만 실제 이동 가능, 갈림길 안내 토스트 |
| 반영 | 부분 수정                                                                                                                                              |

**프롬프트** — `ai-log/raw/2026-08-04.md` 22:51(조건 7개 명시), 23:02, 23:17, 23:32, 23:38,
23:46

> 1. 플레이어 외의 모든 보행자를 제거한다. 2. 건물(B)·신호등(T)·공사장(C)은 모두
>    제거하고 일반 도로(S)로 대체한다. (…) 6. 갈림길에서는 임시 알림(UI 또는 Toast)으로
>    "← 왼쪽", "↑ 직진", "→ 오른쪽"과 같이 안내한다. 7. 길을 벗어나지 않고 START부터
>    GOAL까지 자연스럽게 이동하면 성공이다.

**산출물** — 현재 `src/world.js`의 도로망·이동·안내 토스트 대부분. 커밋 `ed6e981`

**사람이 한 것**

- 7개 조건을 직접 작성해 범위를 못박음(보행자 제거, B/T/C→일반 도로, 경로 1개만 실사용,
  갈림길 토스트 안내)
- "선택 안 된 길도 UI에 나와야 한다"고 지시해, 실제 경로와 겉모습이 같은 기만적 UI를
  요구
- `docs/image.png`에 화살표로 직접 그려서 도로 이음매(모서리) 버그를 지적
- "벽이 실제로는 안 막혀 있어야, 유저 속이는 용도"라는 발언으로 설계 의도(안내 없이는
  진짜 경로를 구분 못 하게)를 명확히 함
- "이음매 버그가 다시 생겼다"는 리포트로 `mergeStraightRoads`의 회귀(그냥 지나가는
  지점까지 교차로로 오판)를 발견하게 함

---

### L-015 · 2026-08-05 · 코드

|      |                                                                                                              |
| ---- | ------------------------------------------------------------------------------------------------------------ |
| 도구 | Claude Code (Claude Sonnet 5)                                                                                |
| 담당 | A                                                                                                            |
| 목적 | prototype의 게이트(3통로 아치)를 재사용해 건물을 게이트+상자 형태로 되살리고, 어느 문을 통과해야 하는지 안내 |
| 반영 | 부분 수정                                                                                                    |

**프롬프트** — `ai-log/raw/2026-08-05.md` 00:03, 00:06, 00:31, 00:32

> 이제 여기에 건물을 추가해보자. `prototype/busy-man-prototype.html`에 게이트 있지?
> 일단 똑같이 설치해. 그리고 그 위에 그냥 건물처럼 보이도록 상자를 하나 올리는거야.
> 게이트랑 상자 색깔은 똑같게 해줘. (…) 다른 게이트 문은 유저 속이기용이니까 실제로
> 사용할 게이트 문이랑 UI 똑같이 해줘

**산출물** — `addGate`/`addBuildingBox` 등 건물 게이트 구현, `nearestArcLength`의
falsy-zero 버그 수정(`best === 0`을 "값 없음"으로 오판해 문 안내 토스트가 전혀 안 뜨던
원인). 커밋 `ed6e981`(같은 커밋에 포함), 이후 4개 커밋으로 분리해 커밋·push

**사람이 한 것**

- `docs/건물.png`·`docs/map/맵 시간 배분.md`를 근거 자료로 직접 지정
- "게이트/상자 색깔 똑같이", "시작·끝 둘 다 게이트" 등 구체적 스펙을 제시
- "문 안내 토스트가 안 뜬다"는 관찰로 `nearestArcLength` 버그를 발견하게 함
- "상자 사방 막기", "배치 안 맞는 건물 검토", "번호 크게" 등 후속 수정을 직접 지시
- 커밋 분할 기준과 메시지 초안을 직접 검토·승인 (4개 커밋으로 분리)

## §3. 폐기 기록

`반영: 폐기`인 항목을 여기에도 남긴다. **지우지 않는다.**
제출물 4번의 "AI 산출물의 검증" 절이 이 표에서 나온다.

| #     | 날짜       | 무엇을                                                                                                          | 왜 폐기했나                                                                                                                                                                                                                                                                                 |
| ----- | ---------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | 2026-08-03 | `docs/ai-log/prompts/` 층 (긴 세션을 주제별 파일로 분리 보관)                                                   | 훅이 모든 원문을 `raw/`에 자동 저장하게 되면서 존재 이유가 사라졌다. 훅이 못 잡는 세션도 `raw/`에 직접 붙여넣는 것으로 규정되어 있어 중복. 층을 셋(raw→prompts→엔트리)으로 두는 것은 이 규모에서 과투자                                                                                     |
| D-002 | 2026-08-03 | 프롬프트 **직전** 커밋 해시를 기록하는 단일 훅                                                                  | 알고 싶은 것은 "이 지시가 반영된 커밋"인데 직전 해시는 그 반대였다. 2단계(UserPromptSubmit + Stop) 구조로 대체                                                                                                                                                                              |
| D-003 | 2026-08-03 | 스택형 브랜치 토폴로지 (①→②→③ 중첩)                                                                             | 세 PR의 파일 집합이 겹치지 않아 스택이 기여하는 바가 없는데 병합 순서만 강제했다. 실제로 3-way 병합으로 충돌 0을 확인한 뒤 main 기준 독립 브랜치로 대체                                                                                                                                     |
| D-004 | 2026-08-03 | feature 단위 skill 세트                                                                                         | 파일 5개를 3.5일에 각자 한두 번 쓰고 끝나는 규모에서 반복 워크플로가 성립하지 않는다. 같은 효과(경계 고정)를 `AGENTS.md` 한 장이 훨씬 싸게 낸다. `/ai-log`만 예외로 남김                                                                                                                    |
| D-005 | 2026-08-03 | develop 브랜치 도입안                                                                                           | **GitHub Pages는 저장소당 게시 소스가 하나**라 develop을 별도 URL로 띄울 수 없다. develop의 핵심 이점("미리 보고 main으로")이 원천 봉쇄된다. 게다가 8/7 동결 전까지 main이 깨져도 손해가 없고, 충돌은 이미 파일 소유권(§1)이 막고 있다. 대가만 확실하다 — 머지 2단계, 핫픽스도 develop 경유 |
| D-006 | 2026-08-03 | GitHub Actions 배포 워크플로                                                                                    | `docs/`·`prototype/`을 웹에서 감출 수 있다는 이점이 있으나, 워크플로 자체가 새 실패 지점이 된다. `team-plan.html` §02가 "배포 파이프라인이 마지막 날 백지를 만든다"고 경고한 실패 모드다. branch 방식(`main`/root)으로 대체하고 루트 전체 노출은 수용                                       |
| D-007 | 2026-08-03 | three.js CDN + import map 로딩안                                                                                | 저장소가 가벼워지는 대신 **심사 당일 외부 서버 상태에 게임이 종속된다.** CDN이 죽거나 막히면 백지이고 그건 통제할 수 없다. 대회 요건이 "심사 종료 시점까지 접근 가능"인데 제3자에 의존하게 된다. `vendor/` 동봉으로 대체 — 대가는 파일 하나(~1MB)                                           |
| D-008 | 2026-08-04 | 갈림길 구조를 새로 설계하는 접근(deep-interview + v1~v6, `docs/ai-log/specs/2026-08-04-map-route-balancing.md`) | 기존 `docs/aaa.jpg` 맵 구조를 그대로 유지하는 방향(L-011)으로 선회하면서 이 접근 전체가 안 쓰이게 됐다. 시행착오 기록은 specs 문서에 그대로 보존                                                                                                                                            |
| D-009 | 2026-08-04 | 좌우 회전 가능한 열린 도시(자유 이동·건물 진입) 버전 `world.js`                                                 | "길" 구조·이동감만 검증하는 최소 버전(L-014)으로 전면 대체됐다. 노드 좌표(`N[]`)와 도로 그래프(`ROADS`)는 이후 버전에도 그대로 재사용됨                                                                                                                                                     |

## §4. 외부 에셋·오픈소스 출처

에셋을 **쓰는 순간** 추가한다. 나중에 몰아서 정리하지 않는다.
**라이선스를 확인할 수 없는 에셋은 사용하지 않는다.**

| 파일 경로 | 종류 | 출처 URL | 라이선스 | 저작자 표기 의무 | 비고 |
| --------- | ---- | -------- | -------- | ---------------- | ---- |
| `vendor/three.module.js` | 라이브러리(렌더러) | https://unpkg.com/three@0.160.0/build/three.module.js | MIT | 파일 내 라이선스 헤더 유지 | three.js r160 ES 모듈 빌드. `world.js`에서 상대경로로 import |
| `docs/질문_이벤트_휴대폰_UI_예시.png` | 이미지 (UI 목업) | AI 생성 — Gemini / Nano Banana | 외부 저작물 아님 (생성물). 도구 약관 확인 필요 | 없음 | 배치·비율만 명세 근거. 안의 글자와 번호는 생성 산물이라 명세가 아니다 → `docs/ui-spec.md` |

> 위 이미지는 게임에 들어가는 에셋이 아니라 **기획 단계의 참조 목업**이다. 그래도 여기 적는 것은
> §4가 "쓰는 순간" 적으라고 정하고 있고, 제출물 4번이 AI 산출물의 출처를 요구하기 때문이다.
> 같은 날 같은 도구로 뽑은 배치 예시 이미지가 한 장 더 있었으나 **저장소에 넣지 않았다** —
> 실시간 방식 안의 그림이라 모달로 가기로 한 뒤에는 오해만 만든다.

## §5. 게임 내 AI 기능 — 미탑재 판단

제출물 4번의 "게임 내 AI 기능" 절이 여기서 나온다.

**판단: 런타임 AI를 탑재하지 않는다.**

근거

- 실질 개발 기간이 3.5일(8/3~8/6)이고 버퍼가 없다 — `team-plan.html` §01
- 심사자가 별도 API 키·계정·결제 없이 플레이할 수 있어야 한다 — `대회_정보.md` 제출물 1번
- 유료 API 사용료는 참가자 부담이며, 심사 종료 시점까지 링크가 살아 있어야 한다
- 대신 **콘텐츠 생성(대화 로그·질문·정답·함정 단서)에 AI를 쓰고 정적 JSON으로 굽는다.**
  플레이 경험에는 AI 산출물이 그대로 들어가되 런타임 의존은 없다

_(판단이 바뀌면 지우지 말고 여기에 이어서 적는다.)_
