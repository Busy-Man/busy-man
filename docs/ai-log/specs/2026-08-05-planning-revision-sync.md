# Deep Interview Spec: 기획 1차 보완 + UI 예시 반영 문서 최신화

## Metadata

- Interview ID: `di-20260805-docsync`
- Rounds: 8 (Round 0 토폴로지 게이트 포함)
- Final Ambiguity Score: **10%**
- Type: brownfield
- Generated: 2026-08-05
- Threshold: 0.2
- Threshold Source: `default`
- Initial Context Summarized: no
- Status: **PASSED**

## Clarity Breakdown

| Dimension | Score | Weight | Weighted |
|-----------|-------|--------|----------|
| Goal Clarity | 0.92 | 0.35 | 0.322 |
| Constraint Clarity | 0.90 | 0.25 | 0.225 |
| Success Criteria | 0.88 | 0.25 | 0.220 |
| Context Clarity | 0.90 | 0.15 | 0.135 |
| **Total Clarity** | | | **0.902** |
| **Ambiguity** | | | **0.098** |

## Topology

| # | Component | Status | 설명 | 커버리지 |
|---|-----------|--------|------|----------|
| 1 | 확정 사항 재조정 | active | `기획_1차_보완.txt` ↔ `AGENTS.md` §3 충돌 6건 판정 | AC-1 ~ AC-3 |
| 2 | 휴대폰·질문 UI 명세 | active | 이미지 2장 기반 UI 문서화 | AC-4 |
| 3 | 접점 4개 계약 영향 | active | **판정 결과 무변경** — 모달 유지로 계약 불변 | AC-6 |
| 4 | 기획 문서 정본 정리 | active | 정본/기록 구분과 정정 블록 | AC-5, AC-7 |
| 5 | 규약 문서 구조 | active | `AGENTS.md` 분할 여부 판단 → **분할하지 않음** | AC-8 |
| 6 | 밸런스 검토 관리 | active | `game_balance_review.html` 처리 → **동결 + 판정표 분리** | AC-3, AC-9 |

Deferred: 없음.

---

## Goal

`docs/기획_1차_보완.txt`(2026-08-04)와 UI 예시 이미지 2장을 **새 정본**으로 받아들여,
`AGENTS.md` §3의 확정 목록을 재조정하고 질문 폰 UI 명세를 신설한다.
`docs/`의 기존 문서는 지우거나 고쳐 쓰지 않고 **시점 기록으로 보존**하며,
바뀐 판정은 저장소가 이미 쓰고 있는 **정정 블록 패턴**으로 덧붙인다.

---

## Constraints

- **`기획_1차_보완.txt`가 `AGENTS.md` §3을 이긴다.** 단, 그 안의 개별 항목 중 기각된 것이 있다(아래 판정표).
- **정본은 `AGENTS.md` §3 하나다.** `.github/copilot-instructions.md`가 *"`docs/`의 문서가 서로 어긋남 — 시점이 다른 기록이다. 현재 확정 사항은 `AGENTS.md` §3에 있다"*로 이미 못 박아 두었다.
- **문서를 지우지 않는다.** `AGENTS.md` §2 · `docs/team-plan.html` §03의 「2026-08-04 정정」 블록과 같은 방식으로 사유를 덧붙인다.
- **`AGENTS.md`를 분할하지 않는다.** `CLAUDE.md`가 스스로 적은 *"두 벌이 되면 반드시 어긋나고, 그때 어느 쪽이 맞는지 아무도 모르게 된다"*가 분할을 막는다. 224줄 / 12절은 분할을 정당화할 크기가 아니고, 변경 8건은 §2·§3·§5·§6에 골고루 흩어져 있어 "안정/불안정" 경계로도 갈리지 않는다.
- **`docs/game_balance_review.html`을 수정하지 않는다.** 2026-08-01 시점 메모로 동결한다. HTML은 diff가 읽히지 않고, 남은 실질 개발일은 8/5~8/6 이틀뿐이다.
- **`src/state.js`와 `AGENTS.md` §2를 수정하지 않는다.** Round 7에서 모달이 유지되어 `quizOpen`의 기존 의미("A는 이동·입력을 멈춘다")가 그대로 참이다.
- 확정 등급을 **3단계**로 나눈다 — 보완본이 스스로 물음표를 단 항목(`(변동 가능)`, `(눈으로 확인 필요)`, `(구현 후 경험으로 판단)`)을 §3의 *"확정 — 되돌리지 않는다"*와 같은 등급에 두면 8/6에 되돌릴 수 없게 된다.

---

## Non-Goals

- `AGENTS.md` 파일 분할 (검토했고 기각)
- `docs/game_balance_review.html` 본문 수정
- `docs/기획 초안.md` 재작성·삭제
- `src/*.js` 코드 구현 및 `src/state.js` 주석 변경
- 밸런스 수치(감속량·게이지량·질문 개수) 결정 — 구현 후 경험으로 조절
- 접점 5번째 신설

## 승인 시점 범위 추가 (2026-08-05)

인터뷰 종료 후 실행 승인 단계에서 3건이 추가됐다. 위 Non-Goals 중
"`기획_1차_보완.txt` 재작성"은 이 결정으로 **철회**된다.

- `docs/기획_1차_보완.txt` → **`docs/기획_1차_보완.md`로 확장자 변경**
- 그 문서에 이번 합의와 **다른 결정이 남아 있는 부분을 정정**한다 (기각된 게이지 감소·룰렛/카드, 미결로 남은 실시간/모달, 6초 표기 등)
- `game_balance_review.html`이 제기한 **찍기·무시 등 추후 밸런싱 요소를 목록으로 정리**한다

정리 위치는 `docs/balance-todo.md` **신설**로 한다 — `AGENTS.md` §3은 확정 사항의
자리이지 작업 목록의 자리가 아니고, 8/6 밸런싱 때 열어놓고 체크할 문서가 따로 필요하다.
정본 중복이 아니므로 §5에서 기각한 "규약 문서 분할"과 성격이 다르다.

---

## 확정 사항 판정표 (컴포넌트 1)

| # | 항목 | 기존 `AGENTS.md` §3 | 보완본 | **판정** | 등급 |
|---|---|---|---|---|---|
| 1 | 스테이지 분량 | 90초 | 1분 30초 ~ 2분 30초 | 보완본 채택 | **잠정** |
| 2 | 좌/우회전 | 잘라냄 (3통로 갈림길 대체) | 방향키 회전 + temple run식 카메라 | **부활** | **잠정** — 보완본이 `(눈으로 확인 필요)` 표기 |
| 3 | 맵 | 직선 복도 + 3통로 갈림길 | 사전 생성 7종 랜덤 | 보완본 채택 | **잠정** — 보완본이 `(변동 가능)` 표기 |
| 4 | 질문 방식 | 모달 · 6초 | 실시간/모달 미결 · 10초 | **모달 유지**, 시간 제한만 **6초 → 10초** | 확정(모달) |
| 5 | 오답 패널티 | 감속 3초 고정, 룰렛·카드 없음 | 감속/게이지감소 2중 랜덤 + 룰렛·카드 | **기각 — 감속 고정 유지** | 확정 |
| 6 | 스크랩 | 잘라냄 | "우선 제거, 고려 안 함" | 제거 유지 (충돌 없음) | 확정 |
| 7 | 가속 효과 | 미명시 | 무적(사람만 밀침) + 속도 증가 3초 | **신규 채택** | 확정 |
| 8 | 역할 | 직장인 1종 | 직장인 | 변경 없음 | 확정 |

### #5를 기각한 이유

보완본의 "게이지 감소" 패널티는 `AGENTS.md` §2와 `src/state.js:31-34`가 **명시적으로 금지**한 것이다
(*"`gauge`는 정답에만 +. 오답에는 건드리지 않는다"*). 그 금지에는 계산된 근거가 있다 —
`docs/game_balance_review.html` §02 「벌의 자기 해제 문제」가
*"게이지가 0이면 게이지 감소도 무효 … 못하는 플레이어일수록 오답이 저렴해지고 … **상벌이 실력과 역상관**"*
이라고 적었다. 같은 문서 §04①은 랜덤 자체도 반대했다 — *"룰렛을 3번 돌려서는 기댓값이 나오지 않는다."*
이번 스코프는 질문 4개라 표본이 더 적다.

기각된 두 안(게이지 감소, 룰렛·카드)은 폐기가 아니라 **밸런스 단계 재고 항목**으로 내린다.

### #4가 모달로 유지된 경위

Round 2에서 이미지 배치와 보완본을 근거로 **실시간을 잠정 채택**했으나,
Round 7에서 **모달 유지로 되돌렸다.** 사용자가 참고하라고 지목한 UI는
`docs/질문_이벤트_휴대폰_UI_예시.png`(질문 폰의 **구성**)이지
`docs/휴대폰_배치_UI_예시.png`(**배치**)가 아니었다.
실시간 전환은 **명시적 요청이 있을 때만** 한다.

이 되돌림 덕분에 `src/state.js`와 `AGENTS.md` §2는 손대지 않는다.

---

## 밸런스 검토 판정표 (컴포넌트 6)

`docs/game_balance_review.html`(2026-08-01)의 권고 대비 현재 설계.
**HTML은 수정하지 않고 이 판정만 `AGENTS.md` §3에 남긴다.**

| 검토 § | 권고 | 판정 |
|---|---|---|
| §03 | 스크랩 슬롯 5→3, 오답 시 랜덤 삭제 제거 | **채택 이상** — 스크랩 시스템 자체를 제거 |
| §04① | 랜덤 패널티는 표본 부족 → 결정적으로 | **채택** — 감속 3초 고정 유지 (위 #5) |
| §04② | 게이지 상한에서 정답 가치가 0 | **의도적으로 열어둠** — 보완본 60행이 *"게이지 관리는 유저 몫. 시스템이 고려하지 않음"*으로 명시 |
| §04③ | 부스트 중 사람 밀치기 (상을 진짜 상으로) | **채택** — 보완본 22행 *"무적(사람만 밀침) + 속도 증가 (3초)"* |
| §04④ | 생명 제거, 시간 단일 실패축 | **채택** — 보완본에 생명 개념 없음 |
| §05 | 무응답 페널티는 **작게** (I ≈ 0.2~0.4 × L) | **열어둠** — 아래 OPEN-1 참조 |

---

## 열린 항목 (합의 전 코드 금지)

| ID | 항목 | 근거 | 왜 지금 못 닫나 |
|---|---|---|---|
| **OPEN-1** | **타임아웃 페널티가 오답과 같다** | 보완본 40행이 *"### 오답 및 타임아웃 시"*로 둘을 하나로 묶었다 = `I = L`. `game_balance_review.html` §05는 `I ≈ 0.2~0.4 × L`을 권했고 `I`가 과하면 **"찍기가 최적 — 스크랩 무의미"** 구간으로 넘어간다고 계산해 두었다 | 실제 수치를 굴려봐야 안다. **밸런스 단계(8/6)에서 무응답·찍기 밸런스를 함께 본다** |
| **OPEN-2** | **질문 주기 6~8초 vs 질문 4개 vs 스테이지 90~150초** | 보완본 33행 *"주기를 가짐 6초~8초"*. 150초에 7초 주기면 질문이 **20개**가 되어 §3의 "질문 4개"와 한 자릿수 이상 어긋난다 | 세 수치 중 무엇이 파생값인지 미정. 셋 다 잠정 등급 |
| **OPEN-3** | **길 안내(네비)의 소유자** | 보완본 78행 *"경로가 더 이상 랜덤이 아니기 때문에, 역할 분담이 모호함. (A, B 모두에게 걸침)"* | `AGENTS.md` §1 파일 소유권에 직접 걸린다. 안내 데이터는 `content/*.json`(B)인데 갈림길 판정은 `world.js`(A) |
| **OPEN-4** | `content/day1.json` 스키마 | 기존 §3의 열린 항목. 이번 검토로 닫히지 않음 | 8/5 병합일에 드러난다 |

`game_balance_review.html` §04②(게이지 상한)는 **열린 항목이 아니라 의도적 기각**이다 — 유저 몫으로 남긴다.

---

## Acceptance Criteria

- [ ] **AC-1** `AGENTS.md` §3이 **확정 / 잠정 / 열림** 3등급 구조로 재구성되고, 잠정 항목에 보완본의 물음표 표기(`변동 가능` 등)가 근거로 인용된다
- [ ] **AC-2** 판정표 8건이 §3에 반영된다 — 특히 좌/우회전과 맵 7종이 *"잘라낸 것"*에서 **잠정 확정**으로 이동하고, 질문 시간 제한이 **10초**로 바뀐다
- [ ] **AC-3** §3에 「밸런스 검토 판정」 소절이 생겨 `game_balance_review.html` 6건의 채택/기각/열림이 한 표로 읽힌다
- [ ] **AC-4** `docs/ui-spec.md`가 신설되고 아래를 담는다:
  - 질문 폰 구성 — 상단 좌측 **시계**, 상단 우측 **10초 카운트다운 배지**, 문자 수신 말풍선 형태의 질문, 답장 입력란(장식), 하단 보기 3개
  - 답안 선택은 **마우스 클릭** (숫자키 아님 — 보기의 "1./2./3."은 표시용 번호)
  - 등장 연출: **화면 바깥에서 위로 올라옴**
  - 위치: **화면 중앙 모달**. 뜨는 동안 다른 UI 상호작용 불가 + 시간 정지
  - `휴대폰_배치_UI_예시.png`는 **폰 크기·화면 점유 참고용**이며 질문 모달 배치의 근거가 아니라는 단서
  - 대화 스트림 폰은 `phone.js`(B) 소유이고 space 유지 중에만 표시되며, **모달 중에는 접근 불가**
- [ ] **AC-5** `docs/team-plan.html` §02에 「2026-08-05 정정」 블록이 §03의 8/4 블록과 같은 패턴으로 추가된다 (잘라낸 것 표의 회전·맵 항목이 되살아났음을 명시)
- [ ] **AC-6** `src/state.js`와 `AGENTS.md` §2가 **한 글자도 바뀌지 않는다.** §3 어딘가에 "이번 재조정으로 접점 4개는 무변경"이 한 줄 남는다
- [ ] **AC-7** `docs/기획 초안.md` · `docs/기획_1차_보완.txt` · `docs/game_balance_review.html`이 **수정되지 않는다**
- [ ] **AC-8** `AGENTS.md`가 분할되지 않는다 (파일 개수 불변)
- [ ] **AC-9** OPEN-1 ~ OPEN-4가 §3의 "열려 있음"에 각각 근거 인용과 함께 올라간다
- [ ] **AC-10** 다른 문서를 인용할 때 `AGENTS.md` §5의 규칙을 지킨다 — **본문에서 처음 나올 때 파일명을 함께 적는다**
- [ ] **AC-11** `main`에 직접 push하지 않는다. `docs/…` 브랜치 → PR. 커밋 scope는 `docs`

---

## Assumptions Exposed & Resolved

| 가정 | 도전 | 결론 |
|---|---|---|
| "문서 최신화" = 단순 반영 작업 | 보완본이 §3의 *"되돌리지 않는다"* 6건을 되돌리고 있음 | **확정 사항 재개봉**으로 재정의. 문서 위상부터 결정 |
| 이미지 배치 = 실시간 방식의 근거 | 모달은 "화면 중앙"인데 이미지는 좌측 | Round 2에서 실시간 채택 → **Round 7에서 모달로 되돌림.** 참고 대상은 배치가 아니라 질문 폰 구성이었음 |
| 보기 "1./2./3." = 숫자키 입력 | A의 입력 라우팅이 필요하다고 추론 | **틀림.** 마우스 클릭이므로 라우팅 불필요 |
| 보완본이 이기면 6건 전부 채택 | 게이지 감소가 §2 규약·밸런스 검토 §02와 정면 충돌 | **1건 기각.** 감속 고정 유지 |
| `AGENTS.md` 수정이 잦다 = 분할 신호 (Contrarian) | 잦은 수정은 정본이 하나라는 **증거**. 분할은 "두 벌"을 만드는 행위 | **분할하지 않음.** §3만 3등급 표로 재구성 |
| `quizOpen` 접점을 지울 수 있다 (Simplifier) | 모달 유지로 원래 의미가 그대로 참 | **접점 4개 무변경** |

---

## Technical Context

- `AGENTS.md` 224줄 / 12절. 전체 40커밋 중 8건(20%)이 이 파일 — §2·§3·§5·§6에 분산
- 저장소에 이미 존재하는 정정 패턴: `AGENTS.md` §2, `docs/team-plan.html` §03의 「2026-08-04 정정」 블록
- `src/*.js`는 대부분 스켈레톤. `src/state.js`만 3.2KB로 실물 구현됨 (접점 4개)
- `docs/`는 루트가 그대로 Pages로 서빙되므로 URL로 직접 열린다 (`AGENTS.md` §6)
- 문서 언어는 한국어. 코드 식별자·경로만 원문 유지 (`.github/copilot-instructions.md`)

## Ontology (Key Entities)

| Entity | Type | 주요 속성 | 관계 |
|---|---|---|---|
| 기획 정본 | core domain | 위상, 시점 | `AGENTS.md` §3이 정본, `docs/*.txt`는 기록 |
| 확정 항목 | core domain | 등급(확정/잠정/열림), 근거 | §3이 소유 |
| 질문 이벤트 | core domain | 방식(모달), 제한시간(10초), 보기 3개 | 질문 폰으로 표시, 접점 상태를 통해 A에 전달 |
| 질문 폰 | core domain | 시계, 카운트다운 배지, 말풍선, 보기 버튼 | `quiz.js`(B) 소유 |
| 대화 스트림 폰 | supporting | space 유지 중 표시 | `phone.js`(B) 소유. 모달 중 접근 불가 |
| 접점 상태 | core domain | `speedMul` `gauge` `quizOpen` `onGate` | `src/state.js` 공동 소유. **무변경** |
| 패널티 | supporting | 감속 3초 고정 | 오답·타임아웃 공통 (OPEN-1) |
| 맵 | supporting | 사전 생성 7종 랜덤, 좌/우회전 | `world.js`(A) 소유 |
| 스크랩 | 폐기 | — | 제거 확정 |
| 밸런스 검토 판정 | reference | 채택/기각/열림 | `game_balance_review.html` 동결, 판정만 §3 |
| 규약 문서 구조 | reference | 단일 파일 유지 | `CLAUDE.md`가 `@AGENTS.md`로 참조 |

## Ontology Convergence

| Round | Entity Count | New | Changed | Stable | Stability |
|---|---|---|---|---|---|
| 1 | 8 | 8 | - | - | N/A |
| 2 | 9 | 1 | 0 | 8 | 89% |
| 3 | 11 | 2 | 0 | 9 | 82% |
| 4 | 11 | 0 | 0 | 11 | **100%** |
| 5 | 11 | 0 | 0 | 11 | **100%** |
| 6 | 11 | 0 | 0 | 11 | **100%** |
| 7 | 11 | 0 | 0 | 11 | **100%** |
| 8 | 11 | 0 | 0 | 11 | **100%** |

Round 4 이후 도메인 모델이 완전히 수렴했다.

## Interview Transcript

<details>
<summary>전체 Q&A (Round 0 + 8라운드)</summary>

### Round 0 — 토폴로지 확정
**Q:** 최상위 컴포넌트 4개(확정 재조정 / UI 명세 / 접점 계약 / 정본 정리)가 맞는가?
**A:** 4개 그대로 진행

### Round 1 — Goal Clarity
**Q:** `docs/기획_1차_보완.txt`는 `AGENTS.md` §3 확정 목록을 이기는 문서인가?
**A:** 이긴다 — 새 결정이다
**Ambiguity:** 55% (Goal 0.55 / Constraints 0.35 / Criteria 0.25 / Context 0.75)

### Round 2 — Constraint Clarity
**Q:** 질문 이벤트 방식을 무엇으로 적는가?
**A:** 기존 모달 확정이었으나 재논의 결과 **실시간 우선**, 추후 경험으로 모달 회귀 가능
**Ambiguity:** 46% (0.60 / 0.55 / 0.30 / 0.80)

### Round 3 — Criteria Clarity
**Q:** 산출물 구조를 어떻게 가져가는가?
**A:** §3 갱신 + `ui-spec.md` 신설 + team-plan 정정 블록. **추가 질문 2건 제기** — `AGENTS.md` 분할 필요성, `game_balance_review.html` 관리
**Ambiguity:** 36% (0.70 / 0.55 / 0.50 / 0.90) · 토폴로지 4 → 6

### Round 4 — Contrarian Mode
**Q1:** `AGENTS.md`를 분할하는가?
**A1:** 분할하지 않는다
**Q2:** `game_balance_review.html`을 어떻게 관리하는가?
**A2:** 동결 + 판정표 분리. 무응답·찍기 밸런스는 밸런스 단계에서 고려할 것을 명시
**Ambiguity:** 25% (0.80 / 0.60 / 0.75 / 0.90)

### Round 5 — Constraint Clarity
**Q:** 오답 패널티의 "게이지 감소"를 어떻게 적는가? (§2 규약 충돌)
**A:** 감속 고정 유지
**Ambiguity:** 21% (0.85 / 0.65 / 0.80 / 0.90)

### Round 6 — Simplifier Mode
**Q:** 실시간 전환 후 `state.quizOpen`의 의미를 무엇으로 재정의하는가?
**A:** 질문 폰은 화면 바깥에서 위로 올라옴. 답안 선택은 숫자키가 아닌 **마우스 클릭**
**Ambiguity:** 20% (0.85 / 0.62 / 0.85 / 0.90)

### Round 7
**Q1:** 마우스 클릭이라면 A는 `quizOpen`으로 무엇을 하는가?
**A1:** **아니다. 실시간이 아닌 모달로 계속 진행** (질문 폰 UI는 적용). 실시간 전환은 명시적 요청 시
**Q2:** `03:20`과 `카운트 다운` 배지는 각각 무엇인가?
**A2:** 시계 + 10초 타이머
**Ambiguity:** 16% (0.80 / 0.85 / 0.85 / 0.90)

### Round 8
**Q:** 모달 중 대화 스트림을 볼 수 있는가?
**A:** 볼 수 없다(기존 정의 그대로). 참고하라고 한 UI는 `질문_이벤트_휴대폰_UI_예시.png`였음
**Ambiguity:** **10%** (0.92 / 0.90 / 0.88 / 0.90) ✅

</details>
